# 屏幕切换代码模块 — 架构文档

## 概述

本文档描述情绪视频切换系统的完整代码架构，涵盖视频播放器扩展、情绪控制器、LLM 能力注册、HTTP API 和 Lua 绑定五个层次。系统核心目标：**根据 AI 对话语境或其他信号源，在运行时动态切换屏幕上播放的情绪动画视频**。

---

## 架构分层

```
┌──────────────────────────────────────────────────────────┐
│                    调用入口（多源）                         │
│  LLM Agent │ HTTP API │ Lua Script │ 内部代码（main.c）   │
└───────────────────────┬──────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
┌───────────────┐ ┌───────────┐ ┌───────────────┐
│  cap_emotion  │ │HTTP Server│ │ lua_module_   │
│  (claw_cap)   │ │emotion API│ │   emotion     │
└───────┬───────┘ └─────┬─────┘ └───────┬───────┘
        │               │               │
        └───────────────┼───────────────┘
                        ▼
        ┌───────────────────────────────┐
        │   emotion_video_controller    │  ← 新模块
        │   情绪枚举 → 文件路径映射      │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │       video_player (扩展)     │  ← 重构
        │  play_file / switch_to /      │
        │  get_current / 命令队列        │
        └───────────────┬───────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │     app_stream_adapter        │  ← 未修改
        │  set_file / start / stop      │
        └───────────────┬───────────────┘
                        │
        ┌───────────────┼───────────────┐
        ▼                               ▼
┌───────────────┐              ┌───────────────┐
│ hardware JPEG │              │ display_arbiter│
│   decoder     │              │ VIDEO/EMOTE/LUA│
└───────┬───────┘              └───────┬───────┘
        │                               │
        ▼                               ▼
┌───────────────────────────────────────────────────┐
│              LCD Panel (480×800 RGB565)            │
└───────────────────────────────────────────────────┘
```

---

## 模块详解

### 1. video_player（扩展）

**文件**: `components/video_player/video_player.c` / `video_player.h`

**定位**: 视频播放执行层，管理解码管线与显示输出。

#### 1.1 原有 API（保留兼容）

| 函数 | 用途 |
|------|------|
| `video_player_init()` | 初始化 LCD 面板、帧缓冲、VSync、触摸、**命令队列**、**stream adapter** |
| `video_player_start()` | 扫描 SD 卡根目录，构建播放列表，发送首个播放命令（兼容旧轮播模式） |
| `video_player_stop()` | 发送 STOP 命令，等待 task 退出，释放 display_arbiter |
| `video_player_set_button_callback()` | 注册触摸按钮回调 |

#### 1.2 新增 API

```c
esp_err_t video_player_play_file(const char *path, bool loop);
esp_err_t video_player_switch_to(const char *path);
esp_err_t video_player_get_current(char *path, size_t path_size);
```

- `play_file(path, loop)`: 播放指定文件，`loop=true` 时单文件循环
- `switch_to(path)`: `play_file(path, true)` 的快捷方式
- `get_current()`: 返回当前播放文件的完整路径

#### 1.3 命令队列架构（核心重构）

原有 `video_task` 是一个"启动后自动轮播"的单片循环。重构后改为**命令驱动的状态机**：

```
                         ┌──────────────────┐
                         │   IDLE（等待命令） │
                         └────────┬─────────┘
                                  │
                          xQueueReceive()
                                  │
                  ┌───────────────┼───────────────┐
                  │                               │
            VIDEO_CMD_STOP              VIDEO_CMD_SWITCH_FILE
                  │                               │
                  ▼                               ▼
            回到 IDLE                  display_arbiter_acquire(VIDEO)
                                              │
                                              ▼
                                    ┌──────────────────┐
                                    │    播放循环       │
                                    │  set_file/start   │
                                    │  每 50ms 检查:    │
                                    │  · EOS 到达?      │
                                    │  · 新命令到达?    │
                                    │  · 卡死检测(5帧)  │
                                    └────────┬─────────┘
                                             │
                         ┌───────────────────┼───────────────────┐
                         │                   │                   │
                    EOS + !loop        EOS + loop          新 SWITCH_FILE
                         │                   │                   │
                         ▼                   ▼                   ▼
                  release(VIDEO)        重新播放            stop 当前
                  回到 IDLE            同一文件           set 新文件
                                                         继续循环
```

**关键数据结构**:

```c
typedef enum {
    VIDEO_CMD_SWITCH_FILE,
    VIDEO_CMD_STOP,
} video_cmd_t;

typedef struct {
    video_cmd_t cmd;
    char path[MAX_FILENAME_LEN];  // 512 字节
    bool loop;
} video_cmd_msg_t;

#define VIDEO_CMD_QUEUE_LEN  4    // 4 槽位，约 2KB
```

**队列容量分析**: 4 个槽位足够应对三个生产者（HTTP API、LLM、Lua）的并发请求。正常使用场景下队列不会满。

#### 1.4 stream adapter 生命周期变更

| 旧行为 | 新行为 |
|--------|--------|
| `video_player_start()` 中初始化 | `video_player_init()` 中初始化 |
| `video_player_stop()` 中释放 | 全程复用，仅在系统关闭时释放 |
| 每次轮播时创建/销毁 task | video_task 常驻，等待命令 |

---

### 2. emotion_video_controller（新建）

**文件**: `components/emotion_video/emotion_video.c` / `include/emotion_video.h`

**定位**: 情绪→视频文件的映射层，维护情绪状态。

#### 2.1 情绪枚举

```c
typedef enum {
    EMOTION_VIDEO_M01_IDLE_SHAKE  = 0,  // 待机摇晃
    EMOTION_VIDEO_M02_SWAY        = 1,  // 摇摆
    EMOTION_VIDEO_M03_CALM        = 2,  // 平静
    EMOTION_VIDEO_M04_WAVE        = 3,  // 挥手
    EMOTION_VIDEO_M05_TIRED       = 4,  // 疲倦
    EMOTION_VIDEO_M06_SLIGHT_SWAY = 5,  // 轻微摇摆
    EMOTION_VIDEO_M07_BREATHING   = 6,  // 呼吸
    EMOTION_VIDEO_M08_HAND_MOVES  = 7,  // 手部动作
    EMOTION_VIDEO_M09_NOD         = 8,  // 点头
    EMOTION_VIDEO_M10_RELAXED     = 9,  // 放松
    EMOTION_VIDEO_COUNT           = 10,
} emotion_video_t;
```

#### 2.2 编译期映射表

```c
#define VIDEO_BASE_DIR "/sdcard/videos"

static const char *s_emotion_files[EMOTION_VIDEO_COUNT] = {
    [EMOTION_VIDEO_M01_IDLE_SHAKE]  = VIDEO_BASE_DIR "/m01_idle_shake.mp4",
    [EMOTION_VIDEO_M02_SWAY]        = VIDEO_BASE_DIR "/m02_sway.mp4",
    // ... 共 10 个
};

static const char *s_emotion_names[EMOTION_VIDEO_COUNT] = {
    [EMOTION_VIDEO_M01_IDLE_SHAKE]  = "m01_idle_shake",
    // ... 共 10 个
};
```

全部驻留在 Flash（.rodata），无运行时分配。

#### 2.3 公共 API

| 函数 | 用途 |
|------|------|
| `emotion_video_set(emotion)` | 切换到指定情绪视频（内部调用 `video_player_play_file(path, true)`） |
| `emotion_video_get_current()` | 返回当前情绪枚举值（mutex 保护） |
| `emotion_video_stop()` | 停止播放并释放 display |
| `emotion_video_get_name(emotion)` | 枚举 → 名称字符串 |
| `emotion_video_from_name(name)` | 名称字符串 → 枚举（strcmp 遍历） |

#### 2.4 线程安全

`s_current_emotion` 的读写由 `s_emotion_lock`（FreeRTOS mutex）保护，使用惰性初始化：

```c
static void ensure_lock(void)
{
    if (!s_emotion_lock) {
        s_emotion_lock = xSemaphoreCreateMutex();
    }
}
```

---

### 3. cap_emotion（LLM 能力注册）

**文件**: `components/claw_capabilities/cap_emotion/src/cap_emotion.c`

**定位**: 将情绪视频切换暴露为 LLM 可调用的 function calling 工具。

#### 3.1 注册模式

完全参考 `cap_voice` 的注册模式：

```c
static const claw_cap_group_t s_emotion_group = {
    .group_id = "cap_emotion",
    .descriptors = s_emotion_descriptors,
    .descriptor_count = 2,   // set_emotion + get_emotion
};

esp_err_t cap_emotion_register_group(void)
{
    if (claw_cap_group_exists(s_emotion_group.group_id)) {
        return ESP_OK;  // 幂等
    }
    return claw_cap_register_group(&s_emotion_group);
}
```

#### 3.2 LLM 工具描述符

**set_emotion**:

```c
{
    .id = "set_emotion",
    .family = "emotion",
    .kind = CLAW_CAP_KIND_CALLABLE,
    .cap_flags = CLAW_CAP_FLAG_CALLABLE_BY_LLM,
    .description = "Switch the on-screen emotion animation video...",
    .input_schema_json = {
        "type": "object",
        "properties": {
            "emotion": {
                "type": "string",
                "enum": ["m01_idle_shake", "m02_sway", ... "m10_relaxed"]
            }
        },
        "required": ["emotion"]
    },
    .execute = cap_emotion_set_execute,
}
```

**get_emotion**:

```c
{
    .id = "get_emotion",
    .family = "emotion",
    .kind = CLAW_CAP_KIND_CALLABLE,
    .cap_flags = CLAW_CAP_FLAG_CALLABLE_BY_LLM,
    .input_schema_json = "{\"type\":\"object\",\"properties\":{}}",
    .execute = cap_emotion_get_execute,
}
```

#### 3.3 执行流程

```
claw_cap_call("set_emotion", "{\"emotion\":\"m04_wave\"}", ctx, output, size)
    │
    ▼
cap_emotion_set_execute(input_json, ctx, output, output_size)
    │
    ├─ cJSON_Parse(input_json)
    ├─ cJSON_GetObjectItem(input, "emotion")
    ├─ emotion_video_from_name("m04_wave") → EMOTION_VIDEO_M04_WAVE
    ├─ emotion_video_set(EMOTION_VIDEO_M04_WAVE)
    │     └─ video_player_play_file("/sdcard/videos/m04_wave.mp4", true)
    │           └─ xQueueSend(s_video_cmd_queue, {SWITCH_FILE, path, loop=true})
    └─ snprintf(output, "Emotion set to m04_wave")
```

#### 3.4 LLM 可见性

在 `s_capability_group_entries[]` 中 `llm_visible_by_default = true`，LLM 启动时可自动发现 `set_emotion` 和 `get_emotion` 两个工具。

---

### 4. HTTP API 端点

**文件**: `components/http_server/http_server_emotion_api.c`

#### 4.1 路由

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/emotion` | 查询当前情绪 |
| POST | `/api/emotion` | 切换情绪 |

#### 4.2 请求/响应格式

**GET /api/emotion**:

```json
// Response:
{
  "emotion": "m01_idle_shake",
  "id": 0,
  "playing": true
}
```

**POST /api/emotion**:

```json
// Request:
{"emotion": "m04_wave"}

// Response (成功):
{"ok": true, "emotion": "m04_wave"}

// Response (失败):
// HTTP 400 + "Unknown emotion name"
```

#### 4.3 注册方式

参考 `http_server_status_api.c` 的模式，在 `http_server_start()` 中调用：

```c
http_server_register_emotion_routes(server)
    ├─ httpd_register_uri_handler(server, GET  /api/emotion)
    └─ httpd_register_uri_handler(server, POST /api/emotion)
```

---

### 5. Lua 绑定

**文件**: `components/lua_modules/lua_module_emotion/src/lua_module_emotion.c`

#### 5.1 Lua API

```lua
-- 切换情绪
emotion.set("m04_wave")  -- 返回 true（成功）或 false（失败）

-- 查询当前情绪
local name = emotion.get()  -- 返回 "m04_wave"
```

#### 5.2 注册方式

```c
esp_err_t lua_module_emotion_register(void)
{
    return cap_lua_register_module("emotion", luaopen_emotion);
}
```

`luaopen_emotion` 创建一个 Lua table，包含 `set` 和 `get` 两个 C 闭包。

---

## 数据流总览

```
LLM 对话语境
    │
    ▼
claw_cap_call("set_emotion", {emotion: "m04_wave"})
    │
    ▼
cap_emotion_set_execute()
    │
    ▼
emotion_video_from_name("m04_wave") → EMOTION_VIDEO_M04_WAVE
    │
    ▼
emotion_video_set(EMOTION_VIDEO_M04_WAVE)
    │
    ▼
video_player_play_file("/sdcard/videos/m04_wave.mp4", true)
    │
    ▼
xQueueSend(s_video_cmd_queue, {SWITCH_FILE, path, loop=true})
    │
    ▼
video_task 接收命令:
  1. display_arbiter_acquire(VIDEO)
  2. app_stream_adapter_stop()        ← 停止当前视频
  3. app_stream_adapter_set_file()    ← 设置新视频文件
  4. app_stream_adapter_start()       ← 启动解码+显示
  5. 播放循环 (检查 EOS / 新命令 / 卡死)
  6. EOS + loop=true → 回到步骤 3（重复播放）
```

**等效入口**:

| 入口 | 路径 |
|------|------|
| LLM | claw_cap_call → cap_emotion → emotion_video_set |
| HTTP | POST /api/emotion → emotion_post_handler → emotion_video_set |
| Lua | emotion.set("name") → lua_set_emotion → emotion_video_set |
| 内部代码 | main.c / 任意 task → emotion_video_set() 直接调用 |

---

## 线程安全模型

### 涉及的任务与优先级

| 任务 | 优先级 | 角色 |
|------|--------|------|
| `video_task` | 6 | 命令消费者，串行执行所有播放操作 |
| `extract_task` (内部) | 5 | MJPEG 解封装 + JPEG 硬件解码 |
| `vid_touch` | 4 | 触摸轮询（情绪播放模式下可选） |
| HTTP server tasks | 5 | 处理 /api/emotion 请求 |
| LLM agent tasks | 可变 | 执行 claw_cap call |

### 关键保护机制

| 资源 | 保护方式 | 说明 |
|------|----------|------|
| 视频切换请求 | FreeRTOS Queue（4 槽位） | 多生产者 → 单消费者（video_task） |
| `s_current_emotion` | FreeRTOS Mutex | `emotion_video_set()` 和 `emotion_video_get_current()` 持锁访问 |
| `display_arbiter` | 已有 FreeRTOS Mutex (1s 超时) | VIDEO 所有者身份在播放会话期间持续持有 |
| 帧缓冲 | 无竞态 | extract_task 写入，video_task 串行控制生命周期 |
| stream adapter | 内部 event group | `app_stream_adapter_set_file()` 内部先 stop 再 set，stop 等待 extract_task 确认 |

**video_task 不跨阻塞调用持有 mutex**，仅持有 display_arbiter 所有权。

---

## 内存影响

| 项目 | 位置 | 大小 |
|------|------|------|
| 命令队列 `s_video_cmd_queue` (4 × ~520B) | 内部 DRAM | ~2.1 KB |
| `s_current_file_path[512]` | BSS (DRAM) | 512 B |
| 新增标志位 | BSS (DRAM) | ~6 B |
| `s_emotion_lock` (mutex TCB) | 内部 DRAM | ~120 B |
| 字符串常量（路径 × 10 + 名称 × 10） | Flash (.rodata) | ~700 B |
| 新增代码总计 | Flash (.text) | ~6 KB |
| **DRAM 合计** | | **~2.7 KB** |
| **Flash 合计** | | **~6.7 KB** |

**无新增 PSRAM 分配**。现有帧缓冲（2 × 768KB = 1.5MB PSRAM）和 JPEG 解码缓冲（256KB）保持不变。

---

## 启动流程

### 变更前

```
app_main()
  ├─ video_player_init()
  ├─ video_player_start()     ← 扫描 /sdcard/，自动开始轮播
  ├─ init_fatfs()
  ├─ wifi_manager_init()
  ├─ http_server_init/start()
  └─ app_claw_start()
```

### 变更后

```
app_main()
  ├─ video_player_init()      ← 初始化 panel、帧缓冲、VSync、命令队列、stream adapter
  ├─ init_fatfs()
  ├─ wifi_manager_init()
  ├─ http_server_init/start()
  ├─ app_claw_start()         ← LLM agent 就绪，cap_emotion 已注册
  └─ emotion_video_set(M01_IDLE_SHAKE)  ← 系统就绪后播放待机动画
```

`video_player_start()` 保留但不再在启动路径调用；向后兼容旧轮播模式。

---

## 文件清单

### 新建文件（10 个）

| 文件 | 行数 | 定位 |
|------|------|------|
| `components/emotion_video/include/emotion_video.h` | 64 | 情绪枚举 + 公共 API 声明 |
| `components/emotion_video/emotion_video.c` | 126 | 情绪→视频映射 + 状态管理 |
| `components/emotion_video/CMakeLists.txt` | 5 | 构建定义 |
| `components/emotion_video/Kconfig` | 5 | Kconfig 开关 |
| `components/claw_capabilities/cap_emotion/include/cap_emotion.h` | 14 | 能力注册头文件 |
| `components/claw_capabilities/cap_emotion/src/cap_emotion.c` | 123 | set_emotion/get_emotion 执行回调 |
| `components/claw_capabilities/cap_emotion/CMakeLists.txt` | 5 | 构建定义 |
| `components/lua_modules/lua_module_emotion/src/lua_module_emotion.h` | 16 | Lua 模块头文件 |
| `components/lua_modules/lua_module_emotion/src/lua_module_emotion.c` | 50 | emotion.set()/get() 实现 |
| `components/lua_modules/lua_module_emotion/CMakeLists.txt` | 5 | 构建定义 |

### 修改文件（10 个）

| 文件 | 改动摘要 |
|------|----------|
| `components/video_player/include/video_player.h` | +3 API 声明 |
| `components/video_player/video_player.c` | 命令队列 + video_task 重构 + 新 API 实现 |
| `components/http_server/http_server_emotion_api.c` | POST/GET /api/emotion 处理器 |
| `components/http_server/http_server_priv.h` | +1 路由注册声明 |
| `components/http_server/http_server_core.c` | +1 路由注册调用 |
| `components/http_server/CMakeLists.txt` | 新增源文件 + emotion_video 依赖 |
| `components/common/app_claw/app_capabilities.c` | 添加 cap_emotion 条目 |
| `components/common/app_claw/app_lua_modules.c` | 添加 lua_module_emotion 条目 |
| `components/common/app_claw/Kconfig` | 添加 2 个 Kconfig 选项 |
| `main/main.c` | 启动流程：延迟到 app_claw 就绪后播放待机动画 |

---

## Kconfig 配置

### 新增配置项

```kconfig
config APP_CLAW_CAP_EMOTION
    bool "Enable emotion capability"
    default y
    help
        Register cap_emotion so the LLM can call set_emotion/get_emotion
        to switch between emotion video animations on the display.

config APP_CLAW_LUA_MODULE_EMOTION
    bool "Enable emotion Lua module"
    default y
    depends on APP_CLAW_CAP_LUA && APP_CLAW_CAP_EMOTION
    help
        Enable the emotion Lua module so Lua scripts can call
        emotion.set(name) and emotion.get() to control emotion videos.
```

### 依赖关系

```
APP_CLAW_CAP_EMOTION
    └─ 依赖: (无，直接可用)

APP_CLAW_LUA_MODULE_EMOTION
    └─ 依赖: APP_CLAW_CAP_LUA && APP_CLAW_CAP_EMOTION
```

---

## 已知限制与 TODO

| 项目 | 状态 | 说明 |
|------|------|------|
| PPA 硬件混合过渡 | **二期** | PPA 代码已就绪（ppa_blend.c/h），但缺少帧捕获机制。当前切换延迟 200-500ms，最后一帧保持在屏幕上直至新帧就绪 |
| 触摸按钮覆盖 | 可配置 | 情绪播放模式下按钮可能不期望出现。可添加 `video_player_set_button_enabled(bool)` 控制 |
| SD 卡未就绪 | 已处理 | `video_player_play_file()` 检查文件是否存在，返回 `ESP_ERR_NOT_FOUND` |
| 旧轮播模式兼容 | 已保留 | `video_player_start()` 仍可用，通过命令队列发送播放列表首文件 |
| display_arbiter 抢占 | 已处理 | video_task 每 50ms 检查 ownership，失去时停止渲染但不崩溃 |
