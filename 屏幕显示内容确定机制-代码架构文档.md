# 硬件屏幕显示内容确定机制 — 代码架构文档

## 概述

本文档详细阐述 ESP32 项目中**硬件屏幕上最终显示什么内容**是如何被决定的。核心问题：系统中有两个渲染源（视频播放、Lua 脚本绘图），在任一时刻到底谁的内容能真正显示在 LCD 面板上？

答案依赖于一个关键组件——**display_arbiter（显示仲裁器）**，它通过所有权机制确保同一时刻只有一个渲染源可以写入 LCD。

---

## 一、整体架构

```
                        ┌──────────────────────────┐
                        │     display_arbiter       │  ← 核心裁决者
                        │   当前所有者 (owner)       │
                        │   NONE / LUA / VIDEO      │
                        └──────────┬───────────────┘
                                   │
                   判定谁能真正写入 LCD 面板
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              │
            ┌──────────────┐ ┌──────────────┐    │
            │ video_player │ │  Lua display │    │
            │  (视频播放器) │ │  (脚本绘图)   │    │
            └──────┬───────┘ └──────┬───────┘    │
                   │               │             │
                   │     所有渲染源通过            │
                   │     esp_lcd_panel_draw_bitmap()
                   │     写入同一个 LCD panel_handle
                   │               │             │
                   └───────────────┘             │
                                ▼                │
                    ┌──────────────────────┐     │
                    │   LCD Panel 硬件      │     │
                    │  (480×800 RGB565)     │     │
                    └──────────────────────┘     │
                                                 │
                    每个渲染管线输出帧前检查:      │
                    display_arbiter_is_owner() ──┘
```

**核心原则**：只有一个 `panel_handle`（LCD 硬件句柄），所有渲染源共享它。display_arbiter 决定"此刻谁有权调用 `esp_lcd_panel_draw_bitmap()`"。

---

## 二、硬件初始化：LCD 面板从何而来

### 2.1 初始化入口

文件：[main/main.c](main/main.c#L337)

```c
void app_main(void)
{
    ESP_ERROR_CHECK(esp_board_manager_init());  // ← LCD 硬件在此初始化
    ...
}
```

### 2.2 板管理器初始化链路

`esp_board_manager_init()` 位于 `managed_components/espressif__esp_board_manager/src/esp_board_manager.c`，执行流程：

```
esp_board_manager_init()
  ├── esp_board_periph_init_all()       ← 初始化 SPI/DSI/I2C 总线
  └── esp_board_device_init_all()       ← 初始化所有设备
        └── dev_display_lcd_init()      ← 通用 LCD 初始化
              ├── sub_type_init()       ← SPI 或 DSI 特定初始化
              │     ├── [SPI] 创建 SPI panel IO + 调用板级 panel 工厂
              │     └── [DSI] 获取 LDO + DSI 总线 + 创建 DBI IO + 面板工厂
              ├── esp_lcd_panel_init()  ← 发送初始化命令序列到 LCD 控制器
              └── esp_lcd_panel_disp_on_off(true) ← 点亮屏幕
```

### 2.3 板级配置

不同板子通过 `board_devices.yaml` + `setup_device.c` 定义各自的 LCD 参数：

| 板子 | LCD 型号 | 分辨率 | 接口 |
|------|---------|--------|------|
| ESP32-P4-EYE | ST7789 | 240×240 | SPI |
| ESP32-P4-WiFi6-Touch-LCD-4.3 | ST7701 | 480×800 | DSI/MIPI |
| ESP32-P4-Function-EV | EK79007 | 1024×600 | DSI/MIPI |
| ESP-BOX-3 | ILI9341 | 320×240 | SPI |

### 2.4 全局获取 panel_handle

初始化完成后，任何模块都可以通过以下方式获取 LCD 面板句柄：

```c
void *lcd_handle = NULL;
esp_board_manager_get_device_handle(ESP_BOARD_DEVICE_NAME_DISPLAY_LCD, &lcd_handle);
dev_display_lcd_handles_t *handles = (dev_display_lcd_handles_t *)lcd_handle;
// handles->panel_handle  ← 这就是全局唯一的 LCD 面板句柄
// handles->io_handle     ← LCD 面板 IO 句柄
```

**关键点**：`panel_handle` 全局只有一个实例。所有渲染源（video_player、Lua display）都通过板管理器获取**同一个** `panel_handle`。谁能调用 `esp_lcd_panel_draw_bitmap(panel_handle, ...)` 完全由 `display_arbiter` 裁决。

---

## 三、核心裁决者：display_arbiter

### 3.1 文件位置

- 头文件：[display_arbiter.h](../../../../components/common/display_arbiter/include/display_arbiter.h)
- 实现：[display_arbiter.c](../../../../components/common/display_arbiter/display_arbiter.c)

### 3.2 所有权枚举

```c
typedef enum {
    DISPLAY_ARBITER_OWNER_NONE  = 0,  // 无人占用
    DISPLAY_ARBITER_OWNER_LUA   = 1,  // Lua 脚本（最高优先级）
    DISPLAY_ARBITER_OWNER_VIDEO = 3,  // 视频播放器（默认启动所有者）
} display_arbiter_owner_t;
```

所有权模型：**Lua > Video**（Lua 绝对优先，可抢占 Video；Video 无法在 Lua 持有时获取）

### 3.3 内部状态结构

```c
typedef struct {
    SemaphoreHandle_t lock;           // 互斥锁（1秒超时）
    display_arbiter_owner_t owner;    // 当前所有者
    uint32_t lua_depth;               // Lua 嵌套深度（支持嵌套 acquire）
    display_arbiter_owner_changed_cb_t callback;  // 所有者变更回调
    void *callback_user_ctx;
} display_arbiter_state_t;

static display_arbiter_state_t s_state = {
    .owner = DISPLAY_ARBITER_OWNER_VIDEO,  // 默认：视频拥有显示权
};
```

### 3.4 acquire 逻辑（获取显示权）

```
display_arbiter_acquire(owner)
  │
  ├── 获取互斥锁
  │
  ├── owner == LUA:
  │     ├── lua_depth++
  │     └── 如果当前 owner != LUA → 切换到 LUA，通知回调
  │
  └── owner == VIDEO:
        ├── 如果 lua_depth > 0 → 拒绝（返回 ESP_ERR_INVALID_STATE）
        └── 如果当前 owner != VIDEO → 切换到 VIDEO，通知回调
```

### 3.5 release 逻辑（释放显示权）

```
display_arbiter_release(owner)
  │
  ├── 获取互斥锁
  │
  ├── owner == LUA:
  │     ├── lua_depth--
  │     └── 如果 lua_depth == 0 且当前 owner == LUA → 切换到 NONE，通知回调
  │
  └── owner == VIDEO:
        └── 如果当前 owner == VIDEO → 切换到 NONE，通知回调
```

### 3.6 关键设计细节

1. **Lua 嵌套支持**：`lua_depth` 计数器允许多个 Lua 脚本嵌套获取显示权。只有当最外层 release 时（`lua_depth == 0`），所有权才真正变为 NONE。

2. **Lua 绝对优先**：当 `lua_depth > 0` 时，Video 的 acquire 调用直接返回 `ESP_ERR_INVALID_STATE`。这意味着 Lua 脚本在执行期间对屏幕有独占控制权。

3. **默认 owner = VIDEO**：系统启动时 `s_state.owner = DISPLAY_ARBITER_OWNER_VIDEO`，视频播放器拥有显示权。当 Video 释放后，owner 变为 NONE，video_task 通过轮询重新获取。

4. **所有者变更回调**：当 owner 发生变化时，注册的回调函数被调用。此机制用于在所有者切换时通知相关模块。

### 3.7 公共 API

| 函数 | 用途 |
|------|------|
| `display_arbiter_acquire(owner)` | 请求显示所有权，Lua 优先，失败时返回 `ESP_ERR_INVALID_STATE` |
| `display_arbiter_release(owner)` | 释放显示所有权 |
| `display_arbiter_get_owner()` | 查询当前所有者（无锁读取） |
| `display_arbiter_is_owner(owner)` | 检查调用者是否为当前所有者（无锁读取） |
| `display_arbiter_set_owner_changed_callback(cb, ctx)` | 注册所有者变更回调 |

---

## 四、两个渲染管线

### 4.1 video_player（视频播放器）

文件：[video_player.c](components/video_player/video_player.c)

#### 初始化

在 `app_main()` 中直接调用 `video_player_init()`：

```
video_player_init()
  ├── 获取 panel_handle（从板管理器获取）
  ├── 获取 DPI 帧缓冲区（或分配 PSRAM 缓冲区，2 × 768KB = 1.5MB）
  ├── 创建 VSync 信号量 + 注册 on_refresh_done 回调
  ├── 设置触摸按钮（左下角，80×50 像素）
  ├── 获取触摸手柄
  ├── 创建命令队列（4 槽位，约 2KB）
  ├── 预初始化 stream adapter（MJPEG 解码管线）
  ├── 创建 touch_task（优先级 4，轮询触摸）
  └── 创建 video_task（优先级 6，命令驱动的播放状态机）
```

#### video_task 状态机（核心）

`video_task` 是整个视频播放的中枢，它是一个命令驱动的状态机：

```
                         ┌──────────────────────────┐
                         │   等待命令（阻塞队列）      │
                         └──────────┬───────────────┘
                                    │
                         收到 VIDEO_CMD_SWITCH_FILE
                                    │
                                    ▼
                         ┌──────────────────────────┐
                         │ display_arbiter_acquire   │
                         │ (VIDEO) — 循环等待直到    │
                         │ 成功（Lua 可能占用中）     │
                         └──────────┬───────────────┘
                                    │
                                    ▼
                         ┌──────────────────────────┐
                         │     播放循环              │
                         │                          │
                         │  play_single_file(path)   │
                         │    ├── set_file()         │
                         │    └── start()            │
                         │                          │
                         │  每 50ms 检查:             │
                         │    ├── 新命令到达?         │
                         │    ├── EOS 到达?          │
                         │    ├── 卡死检测 (5帧不动)  │
                         │    └── 所有权丢失?         │
                         └──────────┬───────────────┘
                                    │
              ┌─────────────────────┼──────────────────────┐
              │                     │                      │
        EOS + loop           EOS + !loop          所有权丢失 / STOP
              │                     │                      │
              ▼                     ▼                      ▼
        重新播放              释放 VIDEO            stop + release
        同一文件              等待新命令            等待重新 acquire
                                                          │
                                              ┌───────────┴───────────┐
                                              │                       │
                                        收到 SWITCH_FILE          收到 STOP
                                              │                       │
                                              ▼                       ▼
                                        更新 active_path        退出播放
                                        继续循环                 等待新命令
```

#### 帧输出回调

```c
static esp_err_t display_decoded_frame(uint8_t *buffer, ...)
{
    if (!display_arbiter_is_owner(DISPLAY_ARBITER_OWNER_VIDEO)) {
        return ESP_OK;  // 不是所有者 → 跳过渲染但不报错
    }
    esp_lcd_panel_draw_bitmap(s_panel, 0, 0, width, height, buffer);
    // 叠加触摸按钮
    if (s_btn_buffer) {
        draw_button_overlay(...);
        esp_lcd_panel_draw_bitmap(s_panel, ..., s_btn_buffer);
    }
    // 等待 VSync（防撕裂）
    xSemaphoreTake(s_vsync_sem, portMAX_DELAY);
}
```

#### 播放模式

- **单文件模式**（`video_player_play_file`）: 播放单个文件，可选循环
- **播放列表模式**（`video_player_play_playlist`）: 按顺序播放多个文件，可选整体循环
- 视频文件通过命令队列（FreeRTOS Queue，4 槽位）发送给 video_task，支持多生产者（HTTP API、LLM、内部代码）→ 单消费者

#### Lua 抢占后的恢复机制

当 Lua 脚本获取显示权（owner → LUA）时，video_task 的播放循环检测到 `display_arbiter_is_owner(VIDEO)` 返回 false：

1. 停止当前播放（`stop_current_playback()`）
2. 进入重获取循环：每 500ms 尝试 `display_arbiter_acquire(VIDEO)`
3. 当 Lua 释放所有权后，`lua_depth` 归零，owner 变为 NONE
4. video_task 下一次轮询时成功获取 VIDEO，恢复播放
5. 如果在等待期间收到新的 SWITCH_FILE 命令，会更新播放目标文件

---

### 4.2 Lua Display（脚本绘图）

文件：[lua_module_display.c](../../../../components/lua_modules/lua_module_display/src/lua_module_display.c) / [display_hal.c](../../../../components/lua_modules/lua_module_display/src/display_hal.c)

#### 使用方式

Lua 脚本通过 `require("display")` 获取显示能力：

```lua
local display = require("display")
display.init(panel_handle, io_handle, width, height, panel_if)
-- display_arbiter_acquire(LUA) 在 init 内部完成
display.begin_frame(true, {r=0, g=0, b=0})
display.draw_text(10, 10, "Hello", {r=255, g=255, b=255, font_size=24})
display.fill_circle(100, 100, 50, 255, 0, 0)
display.present()
display.deinit()
-- display_arbiter_release(LUA) 在 deinit 内部完成
```

#### 渲染能力

`display_hal.c`（约 2400 行）提供自定义帧缓冲渲染引擎：
- 帧管理（begin_frame / present / present_rect / end_frame）
- 绘图基元（像素、线、矩形、圆、椭圆、圆弧、圆角矩形、三角形）
- 文本渲染（通过 esp_painter 引擎）
- 位图操作（RGB565 原始数据、裁剪、缩放）
- JPEG 解码（ROM tjpgd）
- PNG 解码（libpng → RGB565）

#### 与 display_arbiter 的交互

- `display.init()` 内部调用 `display_arbiter_acquire(DISPLAY_ARBITER_OWNER_LUA)`，lua_depth++
- `display.deinit()` 内部调用 `display_arbiter_release(DISPLAY_ARBITER_OWNER_LUA)`，lua_depth--
- 支持嵌套：多个 Lua 脚本可以嵌套 init/deinit，lua_depth 跟踪嵌套层数
- **运行时清理回调**（`lua_display_runtime_cleanup`）在脚本异常退出或强制停止时确保释放所有权

#### 生命周期保证（关键修复）

两个清理路径均**无条件释放 arthiter**，不依赖 HAL 操作的成功与否：

```c
// 正常 deinit 路径 (lua_display_deinit)
display_hal_destroy();
display_arbiter_release(DISPLAY_ARBITER_OWNER_LUA);  // 始终释放

// 运行时清理路径 (lua_display_runtime_cleanup)
display_hal_destroy();
display_arbiter_release(DISPLAY_ARBITER_OWNER_LUA);  // 始终释放
```

这确保了无论 `display_hal_destroy()` 是否成功，Lua 对 display_arbiter 的持有都能被正确释放，video_task 可以重新获取 VIDEO 所有权。

---

## 五、启动流程：从通电到显示内容

```
app_main()
  │
  ├─ 1. esp_board_manager_init()
  │     └── LCD 面板硬件初始化完成（SPI/DSI 总线、面板初始化序列、背光点亮）
  │         此时屏幕：空白（黑色），硬件刚上电
  │
  ├─ 2. video_player_init()
  │     ├── 获取 panel_handle
  │     ├── 创建 DPI 帧缓冲区
  │     ├── 创建 VSync 信号量
  │     ├── 设置触摸按钮
  │     ├── 创建 video_task（优先级6，等待命令）
  │     └── 预初始化 stream adapter
  │         此时屏幕：仍然是空白 —— video_task 在等待命令，还没开始播放
  │
  ├─ 3. init_fatfs()           ← 挂载文件系统
  ├─ 4. wifi_manager_init()    ← WiFi 初始化
  ├─ 5. http_server_init/start() ← HTTP 服务器启动
  ├─ 6. app_claw_start()       ← LLM agent 就绪
  │
  └─ 7. 最终屏幕：video_task 等待 SWITCH_FILE 命令。
         可通过 video_player_play_file() 或 video_player_play_playlist()
         向 video_task 发送命令开始播放。
         在此之前屏幕保持空白。
```

**关键时序**：
- 步骤 1-2 期间屏幕是**空白**的（约几百毫秒）
- video_task 创建后就绪，等待第一个播放命令
- 播放命令可通过 HTTP API、LLM function calling、Lua 脚本或内部代码发送

---

## 六、屏幕切换场景

### 6.1 场景一：视频文件切换

```
调用方: HTTP API / LLM / Lua / 内部代码
  │
  ▼
video_player_play_file("/sdcard/videos/new_video.mp4", true)
  │
  ▼
xQueueSend(s_video_cmd_queue, {SWITCH_FILE, path, loop=true})
  │
  ▼
video_task 收到命令:
  1. stop_current_playback()
  2. 更新 active_path = new_video.mp4, loop_enabled = true
  3. continue 回到播放循环顶部
  4. display_arbiter_acquire(VIDEO) → 可能已经是 owner，直接成功
  5. play_single_file("new_video.mp4") → 开始播放新视频
```

**屏幕结果**：从当前视频切换到新视频，循环播放。

### 6.2 场景二：Lua 脚本接管屏幕

```
Lua 脚本执行:
  display.init()
    └── display_arbiter_acquire(LUA)
          ├── lua_depth++ (0→1)
          └── 当前 owner = VIDEO → 切换到 LUA

此时 video_task 播放循环中:
  每 50ms 检查: display_arbiter_is_owner(VIDEO) → false
    ├── stop_current_playback()
    └── 进入重获取循环（每 500ms 重试一次）

屏幕：Lua 脚本执行绘图操作，Lua 的内容显示在屏幕上

Lua 脚本执行完毕（正常或异常退出）:
  display.deinit() 或 lua_display_runtime_cleanup()
    └── display_arbiter_release(LUA)
          ├── lua_depth-- (1→0)
          └── 当前 owner = LUA → 切换到 NONE

video_task 重获取循环:
  display_arbiter_acquire(VIDEO) → 成功（从 NONE 获取）
    └── play_single_file(active_path) → 恢复播放之前的视频

屏幕：自动恢复到 Lua 接管前正在播放的视频
```

**屏幕结果**：Lua 脚本期间显示 Lua 绘制的内容；脚本结束后**自动恢复视频播放**（最多 500ms 延迟）。

### 6.3 场景三：LLM 调用 lua_stop_all_jobs 停止 display 任务

```
LLM 调用 lua_stop_all_jobs(exclusive="display")
  │
  ▼
cap_lua_async_stop_all_jobs("display", ...)
  ├── 设置 stop_requested = true
  ├── 等待 Lua 任务终止
  │     └── cap_lua_timeout_hook 触发 → luaL_error("stopped by user")
  │         → luaL_dofile 返回错误
  │         → cap_lua_run_runtime_cleanups()
  │             └── lua_display_runtime_cleanup()
  │                 ├── display_hal_destroy()
  │                 └── display_arbiter_release(LUA)  → owner = NONE
  └── 返回
  │
  ▼
video_task 重获取循环:
  display_arbiter_acquire(VIDEO) → 成功 → 恢复播放
```

**屏幕结果**：Lua display 任务被强制停止后，屏幕自动恢复视频播放。不再有 Emote 中间状态。

---

## 七、线程安全模型

### 7.1 涉及的任务

| 任务 | 优先级 | 角色 |
|------|--------|------|
| `video_task` | 6 | 视频命令消费者，串行执行所有播放操作 |
| extract_task（stream adapter 内部） | 5 | MJPEG 解封装 + JPEG 硬件解码 |
| `vid_touch` | 4 | 触摸轮询（按钮交互） |
| HTTP server tasks | 5 | 处理 API 请求 |
| LLM agent tasks | 可变 | 执行 claw_cap call |

### 7.2 保护机制

| 资源 | 保护方式 | 说明 |
|------|----------|------|
| `display_arbiter.owner` | FreeRTOS Mutex（1s 超时） | acquire/release 串行化 |
| 视频切换请求 | FreeRTOS Queue（4 槽位） | 多生产者 → 单消费者（video_task） |
| 帧缓冲区 | 无竞态 | extract_task 写入，video_task 串行控制生命周期 |
| stream adapter | 内部 event group | set_file 内部先 stop 再 set，stop 等待 extract_task 确认 |
| `display_arbiter.lua_depth` | 同一把 Mutex | 与 owner 一起受保护 |

### 7.3 display_arbiter 的锁策略

- `acquire` 和 `release` 均在互斥锁保护下执行
- `get_owner` 和 `is_owner` **无锁读取**（`s_state.owner` 是 enum，单次内存访问是原子的）
- 回调通知在**释放锁之后**执行，避免死锁

---

## 八、display_arbiter 所有权状态机

```
                    ┌──────────────────────────┐
                    │                          │
                    ▼                          │
              ┌──────────┐                    │
         ┌───→│   NONE   │←──────────┐        │
         │    └────┬─────┘           │        │
         │         │                 │        │
         │   video_acquire      video_release  │
         │         │                 │        │
         │         ▼                 │        │
         │  ┌────────────┐          │        │
         │  │   VIDEO    │──────────┘        │
         │  │ (默认启动)  │                   │
         │  └─────┬──────┘                   │
         │        │                          │
         │        │ lua_acquire              │
         │        │ (lua_depth: 0→1)         │
         │        ▼                          │
         │  ┌────────────┐                   │
         │  │    LUA     │←── lua_acquire    │
         │  │ (绝对优先)  │    (lua_depth 增加)│
         │  └─────┬──────┘                   │
         │        │                          │
         │        │ lua_release              │
         │        │ (lua_depth 减到 0)        │
         │        │                          │
         └────────┘                          │
                                             │
         video_task 每 500ms 轮询重获取 ──────┘
```

**注意**:
- Lua 可以**抢占**任何状态（从 NONE/VIDEO 直接切到 LUA）
- 默认 boot 状态是 VIDEO，但如果没有视频被 queued，就没有实际渲染发生
- video_task 内部每 50ms 检查 `display_arbiter_is_owner(VIDEO)`，一旦丢失就停止渲染并进入重获取循环
- Lua 释放后 owner 自动变为 NONE，video_task 在 500ms 内自然恢复

---

## 九、关键文件索引

| 组件 | 文件路径 |
|------|---------|
| **display_arbiter 头文件** | [display_arbiter.h](../../../../components/common/display_arbiter/include/display_arbiter.h) |
| **display_arbiter 实现** | [display_arbiter.c](../../../../components/common/display_arbiter/display_arbiter.c) |
| **video_player** | [video_player.c](components/video_player/video_player.c) |
| **main.c 启动入口** | [main.c](main/main.c) |
| **app_claw** | [app_claw.c](../../../../components/common/app_claw/app_claw.c) |
| **Lua display 模块** | [lua_module_display.c](../../../../components/lua_modules/lua_module_display/src/lua_module_display.c) |
| **Lua display HAL** | [display_hal.c](../../../../components/lua_modules/lua_module_display/src/display_hal.c) |
| **cap_lua** | [cap_lua.c](../../../../components/claw_capabilities/cap_lua/src/cap_lua.c) |
| **板管理器 LCD 初始化** | [dev_display_lcd.c](../../managed_components/espressif__esp_board_manager/devices/dev_display_lcd/dev_display_lcd.c) |

---

## 十、一句话总结

**屏幕显示内容 = 当前 `display_arbiter` 所有者对应的渲染管线正在输出的帧。** 两个渲染管线（Video / Lua）共享同一个 LCD 硬件句柄，通过 `display_arbiter_acquire/release` 竞争写入权。Video 是默认所有者，Lua 具有绝对优先权（通过 lua_depth 支持嵌套）。每个渲染管线在输出帧前都会检查 `display_arbiter_is_owner()`，非所有者跳过渲染。Lua 释放所有权后，video_task 在 500ms 内自动重获取并恢复播放，无需外部干预。
