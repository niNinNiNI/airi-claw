# 硬件屏幕 X 轴（480 方向）镜像 — 排查记录

## 目标

将 ST7701 MIPI-DSI 面板（480x800）在 480 方向（X 轴 / 水平方向）做硬件镜像翻转。

## 环境

- 开发板：ESP32-P4-WiFi6-Touch-LCD-4.3
- 面板驱动芯片：ST7701（MIPI-DSI 接口）
- YAML 配置：`board_devices.yaml` 中 `mirror_x: true, mirror_y: true`
- 驱动层：`dev_display_lcd.c` → `esp_lcd_panel_mirror()` → `panel_st7701_mirror()`（`esp_lcd_st7701_mipi.c`）
- 配置生成：YAML → `gen_bmgr_config_codes.py` → `gen_board_device_config.c`（编译时实际使用的 C 代码）

---

## 尝试一：只改 YAML 配置

### 操作
将 `board_devices.yaml` 中 `display_lcd` 的 `mirror_x: false` 改为 `true`。

### 预期
`dev_display_lcd_init()` 在初始化流程中调用 `esp_lcd_panel_mirror(panel, true, true)`，ST7701 驱动通过 DBI 命令写入 SDIR 寄存器实现 X 镜像。

### 结果
**无效** — 画面无变化。

### 根因分析
`dev_display_lcd_init()` 的调用顺序为：
```
Reset → Init → Mirror
```
`esp_lcd_panel_init()` 内部调用 `panel_st7701_init()`，后者最终命令包括 `Display On (0x29)` 和 `st7701->init(panel)`（启动 MIPI DPI 视频流）。之后 `esp_lcd_panel_mirror()` 通过 DBI 接口（DSI Low-Power 模式）发送 Command2 寄存器命令。**面板在 DPI 视频流启动后可能忽略或丢弃 DBI 命令**。

---

## 尝试二：调整 init 调用顺序（dev_display_lcd.c）

### 操作
修改 `dev_display_lcd.c`，将 `mirror`、`swap_xy`、`invert_color` 从 `esp_lcd_panel_init()` 之后移到之前：
```
Reset → Mirror → Swap XY → Invert Color → Init
```

### 结果
**花屏（屏幕严重显示异常）**。

### 根因分析
`esp_lcd_panel_reset()` 执行硬件 GPIO 复位，面板回到默认状态。此时 DBI IO 句柄虽然有效，但 ST7701 可能处于需要特定唤醒时序才能正确接收 DBI 命令的状态。在 `panel_st7701_init()` 中读取 LCD ID 和发送初始化命令之前就发送 DBI 命令，导致面板状态机异常。

---

## 尝试三：SDIR 寄存器写入（init 命令序列中）

### 操作
在 `setup_device.c` 的 ST7701 初始化命令序列中，BK0（Command2 Bank 0）段内添加 SDIR 寄存器写入：
```c
{0xC7, (uint8_t[]){0x04}, 1, 0},  // SDIR SS_BIT: X-axis mirror
```
`0xC7` = SDIR 寄存器地址，`0x04` = SS_BIT（bit 2，源极驱动扫描方向反转）。

### 原理
SDIR 寄存器在 Command2 BK0 内，必须在 Command2 使能后才能访问。初始化序列中已在 BK0 段（`0xFF` 命令选择了 BK0 页），此时 SDIR 的写入发生在 DPI 视频流启动之前，理论上应该能被面板接受。

### 结果
**无花屏，但 X 镜像不生效**。

---

## 尝试四：MADCTL MX 位（标准 MIPI DCS 方法）

### 操作
在初始化命令序列中，Command2 关闭后、Sleep Out 之前，添加 MADCTL 寄存器写入：
```c
{0x36, (uint8_t[]){0x50}, 1, 0},  // MADCTL: MX(0x40) | ML(0x10)
```
`0x50` = `LCD_CMD_MX_BIT (0x40)` | `LCD_CMD_ML_BIT (0x10)`。

### 原理
ST7701 驱动使用非标准的 X 镜像方式（SDIR 寄存器），而标准 MIPI DCS 协议使用 MADCTL 的 MX 位（bit 6）控制列地址方向（左到右 vs 右到左）。尝试通过标准 DCS 命令绕过 ST7701 专用寄存器。

此命令放在 Command2 已关闭之后（`is_command2_disable = true`），`panel_st7701_init()` 会拦截该命令：同时更新内部 `madctl_val` 并将命令发送到面板。命令在 Sleep Out（0x11）和 Display On（0x29）之前发送，此时面板仍在命令模式。

### 结果
**无花屏，但 X 镜像不生效**。

---

## 尝试五：SDIR + MADCTL MX + dev_display_lcd.c 改动（三合一）

### 操作
同时应用：
- 尝试二的 `dev_display_lcd.c` 改动
- 尝试三的 SDIR 写入
- 尝试四的 MADCTL MX 写入

### 结果
**花屏** — 与尝试二相同，确认 `dev_display_lcd.c` 改动是花屏的根因。

---

## 尝试六：MADCTL MX 单独（无 dev_display_lcd.c 改动）

### 操作
仅保留尝试四的 MADCTL MX 位写入，回退 `dev_display_lcd.c`。

### 结果
**无花屏，但 X 镜像不生效** — 确认 MADCTL MX 位在 ST7701 上不工作。

---

## ST7701 镜像机制总结

| 寄存器 | 地址 | 位 | 作用 | 所在域 | 是否生效 |
|--------|------|-----|------|--------|----------|
| SDIR | 0xC7 | SS (bit 2) | X 轴镜像（源极驱动扫描方向） | Command2 BK0 | **否**（不花屏但不镜像） |
| MADCTL | 0x36 | MX (bit 6) | X 轴镜像（列地址方向，标准 DCS） | 标准 DCS | **否**（不花屏但不镜像） |
| MADCTL | 0x36 | ML (bit 4) | Y 轴镜像（行地址/刷新方向） | 标准 DCS | **是**（Y 镜像正常） |

Y 轴镜像（ML 位）通过 MADCTL 正常工作，X 轴镜像无论是 SDIR 还是 MX 位均不生效。推测此 ST7701 面板变体不支持 X 轴硬件镜像，或需要其他未在驱动中实现的寄存器配置。

---

## 最终方案：接受原生竖屏方向（参考 10_mp4_player 项目）

### 参考项目（10_mp4_player）分析

ESP-IDF 官方示例 `10_mp4_player` 在同一块开发板上实现了正常显示。其策略是：

- **不做任何硬件镜像**：`esp_lcd_panel_mirror(lcd_panel, true, true)` 被注释掉，完全跳过 mirror 调用
- **使用面板原生方向**：BSP 将面板定义为 480x800 竖屏（`BSP_LCD_H_RES=480`, `BSP_LCD_V_RES=800`）
- **DPI 视频时序匹配原生方向**：`h_size=480, v_size=800`
- **`data_endian` 使用默认值**：`LCD_RGB_DATA_ENDIAN_BIG`（结构体零初始化）
- **触摸无 mirror/swap**：`mirror_x=0, mirror_y=0, swap_xy=0`

### 当前项目与参考项目的关键差异

深入对比后发现以下差异导致显示行为不一致：

| 配置项 | 当前项目（改前） | 参考项目 | 影响 |
|--------|-----------------|----------|------|
| `mirror_x` | true | 不调用 mirror | 即使 false 也会发送 DBI 命令，干扰面板 |
| `data_endian` | `LCD_RGB_DATA_ENDIAN_LITTLE` | `LCD_RGB_DATA_ENDIAN_BIG`（默认） | 字节序不匹配可能导致显示异常 |
| 触摸 `mirror_x/y` | true/true | false/false | 触摸坐标与显示不一致 |
| `esp_lcd_panel_mirror()` 调用 | 始终调用 | **不调用**（注释掉） | ST7701 在 DPI 流运行期间接收 DBI 命令会干扰面板状态 |

**核心问题**：`dev_display_lcd.c` 在 `esp_lcd_panel_init()` 之后调用 `esp_lcd_panel_mirror()`。ST7701 MIPI 驱动的 `panel_st7701_mirror()` 即使参数是 `(false, false)`，仍会：
1. 通过 DBI 打开 Command2 BK0
2. 发送 SDIR=0x00
3. 关闭 Command2
4. 发送 MADCTL

这些 DBI 操作在 DPI 视频流运行期间发送，干扰面板扫描方向状态。

### 另一个关键发现：生成代码未同步

YAML 配置通过 `gen_bmgr_config_codes.py` 脚本生成 `gen_board_device_config.c`。只修改 YAML **不会自动重新生成 C 代码**。编译时实际使用的是预生成的 C 文件，必须手动同步。

### 最终改动清单

共改动 **4 个文件**：

#### 1. `boards/espressif/esp32_p4_wifi6_touch_lcd_4_3/board_devices.yaml`
- `display_lcd.mirror_x`: `true` → `false`
- `display_lcd.data_endian`: `LCD_RGB_DATA_ENDIAN_LITTLE` → `LCD_RGB_DATA_ENDIAN_BIG`
- `lcd_touch.flags.mirror_x`: `true` → `false`
- `lcd_touch.flags.mirror_y`: `true` → `false`

#### 2. `components/gen_bmgr_codes/gen_board_device_config.c`
- 与 YAML 同步：`mirror_x: false`, `mirror_y: false`, `data_endian: BIG`, 触摸 `mirror_x/y: false`

#### 3. `managed_components/espressif__esp_board_manager/devices/dev_display_lcd/dev_display_lcd.c`
- `mirror_x || mirror_y` 为 false 时跳过 `esp_lcd_panel_mirror()` 调用
- `swap_xy` 为 false 时跳过 `esp_lcd_panel_swap_xy()` 调用

#### 4. `boards/espressif/esp32_p4_wifi6_touch_lcd_4_3/setup_device.c`
- **无需修改**（初始化命令序列已与参考项目一致，且回退了所有 MADCTL/SDIR 尝试）

---

## 关键文件清单

| 文件 | 说明 |
|------|------|
| `boards/.../board_devices.yaml` | 面板 mirror_x/mirror_y/data_endian 配置（YAML 源） |
| `components/gen_bmgr_codes/gen_board_device_config.c` | **编译时实际使用的 C 配置**（从 YAML 生成，需手动同步） |
| `boards/.../setup_device.c` | ST7701 初始化命令序列 |
| `managed_components/.../dev_display_lcd.c` | 面板初始化总控（mirror 调用时机优化） |
| `managed_components/.../esp_lcd_st7701_mipi.c` | ST7701 MIPI 驱动（panel_st7701_mirror 实现） |
| `managed_components/.../esp_lcd_st7701_interface.h` | SDIR/MADCTL 寄存器常量定义 |
| `managed_components/.../dev_display_lcd_sub_dsi.c` | DSI 子设备初始化流程 |
| `managed_components/.../gen_bmgr_config_codes.py` | YAML → C 代码生成器 |
