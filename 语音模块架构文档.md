# 语音模块架构文档

## 1. 概述

语音模块（`cap_voice`）是 AIRI 系统中负责**文本转语音（Text-to-Speech, TTS）** 的 Capability 组件。它通过 WebSocket 连接本地 TTS 服务器，接收 Opus 编码的音频流，解码后通过板载音频 DAC 播放。语音模块属于 `CLAW_CAP_KIND_CALLABLE` 类型的 Capability，即仅支持同步调用，不产生事件。

### 1.1 核心职责
- 接收文本输入和可选的语音类型参数
- 与外部 TTS 服务器通过 WebSocket 通信
- 解码 Opus 编码的音频数据
- 将音频重采样为 48kHz 立体声 PCM 格式
- 通过板载音频 DAC 驱动扬声器播放
- 向调用方返回执行结果

### 1.2 组件位置

```
components/claw_capabilities/cap_voice/
├── CMakeLists.txt           # 组件构建配置
├── include/
│   └── cap_voice.h          # 公开 API 头文件
├── skills/
│   └── cap_voice/
│       └── SKILL.md         # LLM Skill 文档
└── src/
    └── cap_voice.c          # 核心实现 (~650行)
```

---

## 2. 架构层次

语音模块采用**分层架构**，从底层到顶层依次为：

```
┌─────────────────────────────────────────────────┐
│                   Layer 5                        │
│              Capability 接口层                     │
│         (描述符定义 / 注册 / 执行回调)              │
├─────────────────────────────────────────────────┤
│                   Layer 4                        │
│               TTS 请求管理层                        │
│    (请求构造 / 发送 / 同步等待 / 超时处理)           │
├─────────────────────────────────────────────────┤
│                   Layer 3                        │
│               WebSocket 通信层                     │
│    (连接管理 / 事件处理 / 二进制 & 文本帧解析)        │
├─────────────────────────────────────────────────┤
│                   Layer 2                        │
│               音频解码 & 重采样层                    │
│    (Opus 解码 / Mono→Stereo / 48kHz 重采样)        │
├─────────────────────────────────────────────────┤
│                   Layer 1                        │
│              PCM 播放引擎 & 音频驱动                 │
│     (环形缓冲区 / 播放任务 / codec_dev 抽象层)       │
└─────────────────────────────────────────────────┘
```

---

## 3. 各层详解

### 3.1 Layer 1 — PCM 播放引擎 & 音频驱动

#### 3.1.1 音频设备初始化 (`ensure_audio_dev`)

从板级管理器中获取音频 DAC 设备句柄，配置为 **48kHz / 双声道 / 16 位** 格式并打开设备：

```c
esp_codec_dev_sample_info_t fs = {
    .sample_rate = 48000,
    .channel = 2,
    .bits_per_sample = 16,
};
esp_codec_dev_open(s_codec_dev, &fs);
esp_codec_dev_set_out_vol(s_codec_dev, 80);  // 音量 80%
```

**依赖组件：**
- `esp_board_manager` — 获取板载音频 DAC 设备句柄
- `esp_codec_dev` — 音频编解码设备抽象层

#### 3.1.2 PCM 环形缓冲区

使用 **SPIRAM** 分配的环形缓冲区存储待播放的 PCM 数据：

| 参数 | 值 |
|------|------|
| 缓冲区大小 | 48000 × 4 = 192,000 个 sample（约 4 秒 @ 48kHz 立体声 16bit = ~750KB） |
| 数据类型 | `int16_t`（交错 L/R 布局） |
| 存储位置 | `MALLOC_CAP_SPIRAM`（外部 PSRAM） |
| 访问保护 | `s_playback_mutex`（互斥信号量） |

#### 3.1.3 播放任务 (`playback_task`)

独立的 FreeRTOS 任务（`voice_play`，栈大小 4096，优先级 5），负责：

1. 轮询环形缓冲区是否有可用数据
2. 每次最多取 960 个 sample（约 10ms @ 48kHz）写入 codec
3. 通过 `esp_codec_dev_write` 将 PCM 数据送入 DAC
4. **非阻塞写入**：当缓冲区满时静默丢弃数据，避免阻塞 WebSocket 任务

```
[Opus解码] → [重采样] → push_pcm_async() → [环形缓冲区]
                                                     ↓
                                           playback_task → esp_codec_dev_write() → [DAC] → [扬声器]
```

---

### 3.2 Layer 2 — 音频解码 & 重采样层

#### 3.2.1 Opus 解码器初始化 (`ensure_opus_decoder`)

| 配置项 | 值 |
|--------|------|
| 采样率 | 24000 Hz |
| 声道数 | 1 (Mono) |
| 帧时长 | 20ms |
| 自定界 | 否（由帧边界判断） |

**依赖组件：** `esp_audio_codec` / `esp_opus_dec`

解码流程：
```c
esp_opus_dec_register();           // 注册 Opus 解码器
esp_opus_dec_open(&cfg, ...);      // 打开解码器实例
esp_opus_dec_decode(dec, &in_raw, &out_frame, &dec_info);  // 解码
```

#### 3.2.2 重采样 (`resample_mono_to_stereo_48k`)

TTS 服务器发送的是 **24kHz 单声道 Opus** 音频，而 DAC 需要 **48kHz 立体声 PCM**，因此需要进行重采样和声道复制：

| 转换方向 | 说明 |
|----------|------|
| 输入 | 24kHz Mono PCM (int16_t) |
| 输出 | 48kHz Stereo PCM (int16_t, L/R 相同) |
| 算法 | 线性插值 (Linear Interpolation) |

**工作原理：**
- 计算采样步长 `step = src_rate / 48000 = 0.5`
- 对每个输出位置通过线性插值从源数据中计算采样值
- 每个输出 sample 复制到左右声道

```c
// 伪代码
for (pos = 0; pos < src_samples; pos += step) {
    val = linear_interpolate(src, pos);
    dst[L] = val;
    dst[R] = val;
}
```

---

### 3.3 Layer 3 — WebSocket 通信层

#### 3.3.1 连接管理

**服务器 URL：** `ws://<server_ip>:<server_port>`

通过 `cap_voice_set_server(ip, port)` 在系统初始化时配置。

**连接配置：**
| 参数 | 值 |
|------|------|
| 自动重连 | 开启 |
| 重连超时 | 5000ms |
| 缓冲区大小 | 16384 字节 |
| 网络超时 | 10000ms |
| 任务栈大小 | 12288 字节 |

**连接流程：**
1. 创建 `esp_websocket_client` 实例
2. 注册事件回调 `ws_event_handler`
3. 启动客户端
4. 等待 `WEBSOCKET_EVENT_CONNECTED` 事件（最长 10 秒）
5. 连接成功后设置 `s_ws_connected = true`

#### 3.3.2 WebSocket 事件处理 (`ws_event_handler`)

| 事件类型 | 处理逻辑 |
|----------|----------|
| `WEBSOCKET_EVENT_CONNECTED` | 标记连接状态，释放连接信号量 |
| `WEBSOCKET_EVENT_DISCONNECTED` | 标记断开，如 TTS 进行中则标记错误并释放完成信号量 |
| `WEBSOCKET_EVENT_ERROR` | 记录错误日志 |
| `WEBSOCKET_EVENT_DATA` (Binary) | Opus 音频帧解码 & 播放 |
| `WEBSOCKET_EVENT_DATA` (Text) | JSON 控制消息解析 |

**文本帧协议：**

服务器通过文本帧发送控制消息，格式为 JSON：

```json
// TTS 结束
{"type": "tts_end"}

// 错误通知
{"type": "error", "message": "error description"}

// 状态更新
{"type": "status", "message": "..."}

// 心跳响应
{"type": "pong"}
```

每个控制消息都会更新 TTS 状态并可能释放完成信号量 `s_tts_done_sem`。

---

### 3.4 Layer 4 — TTS 请求管理层

#### 3.4.1 TTS 请求发送 (`send_tts_request`)

构造 JSON 请求并通过 WebSocket 发送：

```json
{
  "type": "tts_request",
  "text": "需要朗读的文本",
  "voice": "zh_female_qingxin"
}
```

**参数说明：**
| 字段 | 必须 | 说明 | 默认值 |
|------|------|------|--------|
| `text` | 是 | 要转为语音的文本 | - |
| `voice` | 否 | 语音类型 ID | `zh_female_qingxin` |

**发送超时：** 3000ms

#### 3.4.2 TTS 生命周期管理

```
┌──────────────────┐
│  s_tts_active    │ ← true：标记 TTS 会话进行中
├──────────────────┤
│  s_tts_error     │ ← 初始 false
├──────────────────┤
│  s_tts_result_text│ ← 结果字符串缓冲区 (256字节)
└──────────────────┘

TTS 完成信号量：s_tts_done_sem (Binary Semaphore)
TTS 超时时间：30000ms
```

**TTS 执行时序：**

```
1. 设置 s_tts_active = true
2. 发送 TTS 请求
3. 等待 s_tts_done_sem（最多 30 秒）
   ├─ tts_end 到达 → s_tts_error = false → 成功返回
   ├─ error 到达 → 记录错误信息 → 失败返回
   ├─ WebSocket 断开 → s_tts_error = true → 失败返回
   └─ 超时 → s_tts_active = false → TIMEOUT 返回
```

在等待期间，WebSocket 回调异步接收 Opus 音频帧并送入播放引擎，无需等待 TTS 完成即可开始播放。

---

### 3.5 Layer 5 — Capability 接口层

#### 3.5.1 描述符定义

```c
static const claw_cap_descriptor_t s_voice_descriptors[] = {
    {
        .id = "get_tts",
        .name = "get_tts",
        .family = "voice",
        .description = "Convert text to speech via local WebSocket TTS server...",
        .kind = CLAW_CAP_KIND_CALLABLE,
        .cap_flags = CLAW_CAP_FLAG_CALLABLE_BY_LLM,
        .input_schema_json = "{...}",  // JSON Schema
        .execute = cap_voice_execute,
    },
};
```

**Capability 元数据：**
| 属性 | 值 |
|------|------|
| `id` | `get_tts` |
| `family` | `voice` |
| `kind` | `CALLABLE`（非事件源） |
| `cap_flags` | `CALLABLE_BY_LLM` |
| 输入 Schema | `{text: string (required), voice: string (optional)}` |

#### 3.5.2 Group 注册

```c
static const claw_cap_group_t s_voice_group = {
    .group_id = "cap_voice",
    .descriptors = s_voice_descriptors,
    .descriptor_count = 1,
};
```

**重要特征：** 该模块**不定义生命周期钩子**（`init`/`start`/`stop` 均为 NULL）。WebSocket 连接在每次 `get_tts` 调用时按需建立。

#### 3.5.3 注册与配置入口

在 `app_capabilities.c` 中：

```c
// 注册前的准备：配置 TTS 服务器地址
cap_voice_set_server(CONFIG_APP_CLAW_VOICE_SERVER_IP,
                     (uint16_t)CONFIG_APP_CLAW_VOICE_SERVER_PORT);

// 注册到 claw_cap 系统
return cap_voice_register_group();
```

**编译宏控制：**
- `CONFIG_APP_CLAW_CAP_VOICE` — 启用语音模块
- `CONFIG_APP_CLAW_VOICE_SERVER_IP` — TTS 服务器 IP
- `CONFIG_APP_CLAW_VOICE_SERVER_PORT` — TTS 服务器端口

---

## 4. 调用路径

```
用户/LLM/Console
      │
      ▼
claw_cap_call_from_core(id="get_tts", input_json)
      │
      ▼
cap_voice_execute(input_json, ctx, output, output_size)
      │
      ├─ 1. 检查服务器配置
      ├─ 2. 建立 WebSocket 连接 (ensure_ws_connected)
      ├─ 3. 初始化 Opus 解码器 (ensure_opus_decoder)
      ├─ 4. 初始化音频设备 (ensure_audio_dev)
      ├─ 5. 启动播放任务 (start_playback_task)
      ├─ 6. 发送 TTS 请求 (send_tts_request)
      ├─ 7. 等待 TTS 完成或超时 (xSemaphoreTake)
      │       └── [WebSocket 异步接收 Opus 音频 → 解码 → 重采样 → 播放]
      └─ 8. 输出结果字符串
```

### 4.1 支持的三条调用路径

| 调用源 | 调用方式 | 说明 |
|--------|----------|------|
| **LLM 工具调用** | `claw_cap_call_from_core` | LLM 通过 `get_tts` 工具触发 |
| **Console 命令** | `cap call get_tts '{"text":"..."}'` | 终端直接调用 |
| **Event Router 规则** | `call_cap` 动作 | 自动规则触发（需配置） |

---

## 5. 依赖关系

### 5.1 编译依赖

| 依赖组件 | 用途 |
|----------|------|
| `claw_cap` | Capability 框架核心 |
| `json` (cJSON) | JSON 解析与构造 |
| `esp_codec_dev` | 音频编解码设备抽象 |
| `esp_board_manager` | 获取板载 DAC 设备句柄 |
| `esp_websocket_client` | WebSocket 客户端（基于 esp-tls） |
| `esp_audio_codec` | Opus 音频编解码器 |
| `FreeRTOS` | 任务、信号量、互斥量 |
| `heap_caps` | SPIRAM 分配 |

### 5.2 运行时依赖

| 组件 | 条件 | 说明 |
|------|------|------|
| 音频 DAC | 必须 | 板载扬声器/DAC |
| TTS 服务器 | 必须 | 本地或局域网 WebSocket 服务器 |
| Wi-Fi 连接 | 必须 | 与 TTS 服务器通信 |
| SPIRAM | 推荐（非强制） | 音频环形缓冲区分配 |

---

## 6. 关键数据结构

### 6.1 静态全局变量

| 变量 | 类型 | 用途 |
|------|------|------|
| `s_server_url[128]` | `char[]` | TTS WebSocket 服务器 URL |
| `s_ws_client` | `esp_websocket_client_handle_t` | WebSocket 客户端句柄 |
| `s_ws_connected` | `volatile bool` | WebSocket 连接状态 |
| `s_ws_connect_sem` | `SemaphoreHandle_t` | 连接同步信号量 |
| `s_opus_dec` | `void*` | Opus 解码器实例 |
| `s_opus_ready` | `bool` | Opus 解码器初始化状态 |
| `s_tts_done_sem` | `SemaphoreHandle_t` | TTS 完成同步信号量 |
| `s_tts_result_text[256]` | `char[]` | TTS 执行结果字符串 |
| `s_tts_error` | `volatile bool` | TTS 错误标记 |
| `s_tts_active` | `volatile bool` | TTS 会话进行中标记 |
| `s_codec_dev` | `esp_codec_dev_handle_t` | 音频 DAC 设备句柄 |
| `s_playback_task` | `TaskHandle_t` | 播放任务句柄 |
| `s_playback_running` | `volatile bool` | 播放任务运行状态 |
| `s_playback_buf` | `int16_t*` | PCM 环形缓冲区指针 |
| `s_playback_write_idx` | `volatile size_t` | 缓冲区写索引 |
| `s_playback_read_idx` | `volatile size_t` | 缓冲区读索引 |
| `s_playback_available` | `volatile size_t` | 缓冲区可用数据量 |
| `s_playback_mutex` | `SemaphoreHandle_t` | 缓冲区互斥锁 |

### 6.2 常量

| 常量 | 值 | 说明 |
|------|-----|------|
| `VOICE_DEFAULT_VOICE_TYPE` | `"zh_female_qingxin"` | 默认语音类型 |
| `WS_CONNECT_TIMEOUT_MS` | 10000 | WebSocket 连接超时 |
| `WS_SEND_TIMEOUT_MS` | 3000 | TTS 请求发送超时 |
| `TTS_TIMEOUT_MS` | 30000 | TTS 完成等待超时 |
| `OPUS_DECODE_BUF_SIZE` | 8192 | Opus 解码缓冲区大小 |
| `PLAYBACK_BUF_SAMPLES` | 192000 | PCM 环形缓冲区采样数（4秒） |

---

## 7. Skill 集成

语音模块提供了配套的 Skill 文档（`skills/cap_voice/SKILL.md`），用于向 LLM 暴露使用指引。

### 7.1 Skill 元数据

```json
{
  "name": "cap_voice",
  "description": "Convert text to speech (TTS) and play through speaker...",
  "metadata": {
    "cap_groups": ["cap_voice"],
    "manage_mode": "readonly"
  }
}
```

### 7.2 LLM 可见性

默认情况下，`cap_voice` **不在 LLM 初始可见的工具集中**（`llm_visible_by_default = false`）。LLM 需要通过调用 `activate_skill` 激活 `cap_voice` Skill 后才能使用 `get_tts` 工具。这符合 AIRI 的**渐进式披露**设计原则——仅在需要时加载对应工具，以控制 LLM 上下文中的 token 数量。

### 7.3 SKILL.md 内容要点

Skill 文档中为 LLM 提供了：
- 使用场景判断：何时应使用语音输出而非文字
- 调用规则：参数格式、必填字段
- 输出说明：成功/错误字符串格式
- 常见失败原因及排查指引
- 调用示例

---

## 8. 数据流图

```
┌─────────────┐     {"text":"你好", "voice":"zh_female_qingxin"}
│  LLM / Console │ ────────────────────────────────────────────────→
└─────────────┘                                                    │
                                                                    ▼
┌──────────────────────────────────────────────────────────────────────┐
│                        cap_voice_execute()                           │
│  ┌──────────────┐   ┌──────────────────┐   ┌────────────────────┐   │
│  │ WebSocket    │──→│ ensure_ws_connected│→ │ 连接 TTS 服务器     │   │
│  │ 连接管理     │   └──────────────────┘   └────────────────────┘   │
│  ├──────────────┤   ┌──────────────────┐   ┌────────────────────┐   │
│  │ TTS 请求     │──→│ send_tts_request │→ │ {"type":"tts_      │   │
│  │ 管理         │   └──────────────────┘   │  request", ...}    │   │
│  ├──────────────┤                          └────────────────────┘   │
│  │ 同步等待     │──→ xSemaphoreTake(s_tts_done_sem, 30s)           │
│  └──────────────┘                                                   │
└──────────────────────────────────────────────────────────────────────┘
                          │
                          ▼ 异步接收 (WebSocket 回调)
┌──────────────────────────────────────────────────────────────────────┐
│ ws_event_handler (WEBSOCKET_EVENT_DATA)                              │
│                                                                      │
│  ┌──────────┐   ┌──────────────┐   ┌────────────┐   ┌──────────┐    │
│  │ Binary   │──→│ Opus 解码    │──→│ 重采样     │──→│ 推入     │    │
│  │ 音频帧   │   │ (24kHz Mono) │   │ (48kHz     │   │ 环形     │    │
│  │          │   │              │   │  Stereo)   │   │ 缓冲区   │    │
│  └──────────┘   └──────────────┘   └────────────┘   └──────────┘    │
│                                                                      │
│  ┌──────────┐                                                       │
│  │ Text     │──→ JSON 解析 → 处理 tts_end/error/status/pong         │
│  │ 控制帧   │        │                                              │
│  └──────────┘        ├─ tts_end → 释放完成信号量                     │
│                      └─ error  → 记录错误, 释放完成信号量             │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│ playback_task (独立 FreeRTOS 任务, 优先级 5)                         │
│                                                                      │
│  while (running) {                                                   │
│      if (ring_buf 有数据) {                                          │
│          chunk = min(avail, 960 samples);                            │
│          esp_codec_dev_write(dac, pcm, chunk * sizeof(int16_t));      │
│          update ring_buf read index;                                 │
│      }                                                               │
│      vTaskDelay(10ms);                                                │
│  }                                                                    │
└──────────────────────────────────────────────────────────────────────┘
                          │
                          ▼
                ┌─────────────────────┐
                │  音频 DAC (Codec)   │
                │  48kHz / 双声道     │
                │  16位 PCM           │
                └─────────┬───────────┘
                          │
                          ▼
                ┌─────────────────────┐
                │     扬声器           │
                └─────────────────────┘
```

---

## 9. 错误处理策略

| 错误场景 | 处理方式 | 返回消息 |
|----------|----------|----------|
| TTS 服务器未配置 | 立即返回 | `Error: voice server not configured` |
| WebSocket 连接超时 | 等待 10 秒后超时 | `Error: WebSocket connection failed` |
| Opus 解码器初始化失败 | 立即返回 | `Error: Opus decoder init failed` |
| 无音频输出设备 | 立即返回 | `Error: no audio output device available` |
| 输入 JSON 解析失败 | 立即返回 | `Error: invalid JSON` |
| 缺少 `text` 字段 | 立即返回 | `Error: text is required` |
| TTS 请求发送失败 | 立即返回 | `Error: failed to send TTS request` |
| TTS 服务器返回错误 | 返回服务器错误消息 | `Error: <server_message>` |
| TTS 超时（30秒） | 超时返回 | `Error: TTS request timed out` |
| 播放任务初始化失败 | 立即返回 | `Error: failed to start playback (...)` |
| 播放缓冲区满 | 静默丢弃数据 | 不返回错误（非阻塞设计） |
| WebSocket 中途断开 | 标记错误并释放信号量 | `Error: ...` |

---

## 10. 性能与安全考虑

### 10.1 内存使用

| 分配 | 大小 | 位置 |
|------|------|------|
| PCM 环形缓冲区 | ~750 KB (`192000 × 2 bytes`) | SPIRAM (PSRAM) |
| Opus 解码缓冲区 | 8 KB | Heap |
| WebSocket 缓冲区 | 16 KB | Heap |
| WebSocket 任务栈 | 12 KB | Heap |
| 播放任务栈 | 4 KB | Heap |

> **注意：** PCM 缓冲区约 750KB 是主要内存开销。若无 SPIRAM，播放任务初始化可能因内存不足失败。

### 10.2 并发模型

- **多生产者单消费者：** WebSocket 回调（ISR-like）、解码线程向环形缓冲区写入数据；播放任务从环形缓冲区读取数据
- **互斥保护：** 环形缓冲区的读写索引和可用数据量通过 `s_playback_mutex` 保护
- **信号量同步：** WebSocket 连接完成和 TTS 完成分别通过独立的 Binary Semaphore 同步

### 10.3 安全设计

| 方面 | 策略 |
|------|------|
| 缓冲区溢出 | 环形缓冲区满时静默丢弃新数据，不阻塞生产者 |
| 重入保护 | 每个 TTS 调用通过 `s_tts_active` 标记确保串行执行 |
| 超时保护 | 所有 I/O 操作均有超时限制（连接 10s、发送 3s、TTS 30s） |
| WebSocket 重连 | 启用 `disable_auto_reconnect = false`，自动重连（间隔 5s） |
| 输入验证 | 严格的 JSON Schema 校验（`text` 必填、字符串类型） |

---

## 11. 依赖组件关系图

```
cap_voice (语音模块)
    │
    ├── claw_cap ───────── Capability 框架核心
    │                       描述符注册、Group 管理、调用路由
    ├── cJSON ──────────── JSON 解析与序列化
    │                       请求构造、响应解析
    ├── esp_websocket_client ── WebSocket 客户端
    │                           TTS 服务器通信通道
    ├── esp_audio_codec ──── Opus 音频解码器
    │   └── esp_opus_dec       Opus 解码注册与解码
    ├── esp_board_manager ── 板级设备管理器
    │                          获取板载 DAC 设备句柄
    ├── esp_codec_dev ────── 音频编解码设备抽象
    │                          配置、打开、写入音视频数据
    ├── FreeRTOS ────────── RTOS 基础
    │    ├── Task             播放任务、WebSocket 任务
    │    ├── Semaphore        TTS 完成、连接同步
    │    └── Mutex            环形缓冲区保护
    └── heap_caps ───────── 堆内存分配器
                                SPIRAM 分配
```

---

## 12. 配置项 (Kconfig)

| 配置宏 | 说明 | 示例值 |
|--------|------|--------|
| `CONFIG_APP_CLAW_CAP_VOICE` | 启用语音模块 | `y` |
| `CONFIG_APP_CLAW_VOICE_SERVER_IP` | TTS 服务器 IP 地址 | `"192.168.1.100"` |
| `CONFIG_APP_CLAW_VOICE_SERVER_PORT` | TTS 服务器端口 | `8081` |

---

## 13. 扩展建议

### 13.1 可能的功能扩展

1. **语音识别 (ASR)**：新增 `cap_voice_asr` 组件，使用 WebSocket 发送麦克风音频到服务器进行语音识别
2. **语音唤醒**：集成 ESP-SR 等本地唤醒词引擎，作为 `EVENT_SOURCE` Capability
3. **流式 TTS 输出**：当前实现在 TTS 完成前已可播放音频；可进一步优化为逐帧回调进度
4. **多 TTS 服务器支持**：扩展 `cap_voice_set_server` 为支持多个备选服务器
5. **音频缓存**：对常用短句的 TTS 结果进行本地缓存，减少 WebSocket 通信

### 13.2 当前架构的局限性

1. **无生命周期钩子**：WebSocket 连接在每次 `get_tts` 调用时按需建立，无长连接预热机制，首次调用有额外延迟
2. **单线程处理**：TTS 请求的同步等待阻塞了调用线程，可能影响其他 Capability 的并发调用
3. **无音量控制参数**：当前音量硬编码为 80%，用户无法通过工具参数调节
4. **仅支持 TTS**：无语音识别功能，无法实现语音对话

---

## 14. 总结

`cap_voice` 是一个典型的 **CALLABLE Capability** 实现，展示了 AIRI 系统中 Capability 组件的标准架构模式：

1. **组件化**：独立的目录结构、构建配置、头文件/实现分离
2. **框架集成**：通过 `claw_cap_descriptor_t` 和 `claw_cap_group_t` 注册到核心系统
3. **异步 I/O**：WebSocket 异步通信 + FreeRTOS 多任务
4. **硬件抽象**：通过 `esp_board_manager` 和 `esp_codec_dev` 实现设备无关性
5. **Skill 配套**：提供 LLM 使用的 Skill 文档，遵循渐进式披露原则
6. **平台无关**：依赖板级管理器获取设备句柄，可在不同 ESP 开发板上复用
