# 语音模块架构文档

## 1. 概述

语音模块是 AIRI 系统中负责**音频输入/输出**的子系统，由三个互补组件构成：

| 组件 | 类型 | 职责 |
|------|------|------|
| **cap_voice** | Capability（`CALLABLE`） | 文本转语音（TTS），通过 WebSocket 连接 TTS 服务器，经 LLM 工具调用触发 |
| **cap_asr** | Capability（`CALLABLE`） | 语音识别（ASR），采集麦克风音频通过 WebSocket 发送到服务器进行云端识别 |
| **lua_module_audio** | Lua 原生模块 | 通用音频 I/O，提供录制、播放、音调生成、频谱分析、回路监听等功能，供 Lua 脚本调用 |

`cap_voice` 通过 WebSocket 连接本地语音服务器，接收 Opus 编码的音频流，在独立处理任务中解码并重采样后，通过板载音频 DAC 播放。`cap_asr` 采集 ES7210 麦克风音频（16kHz mono），经 WebSocket 流式发送 PCM 到同一语音服务器进行云端 ASR 识别，返回文本结果。二者共享同一个 WebSocket 连接（由 `voice_ws_common` 组件管理），形成完整的语音对话闭环。`lua_module_audio` 则直接通过 `esp_codec_dev` 抽象层操作音频 Codec，支持输入（ADC/麦克风）和输出（DAC/扬声器）双向音频流，并集成了 FFT 频谱分析能力。

### 1.1 cap_voice 核心职责
- 接收文本输入和可选的语音类型参数
- 与外部 TTS 服务器通过 WebSocket 通信
- 通过 **Opus 帧队列** 解耦 WebSocket 接收与音频解码
- 在独立的 **语音处理任务** 中批量解码 Opus 音频帧
- 将音频重采样为 48kHz 立体声 PCM 格式
- 通过板载音频 DAC 驱动扬声器播放
- 提供完整资源清理机制，支持与视频播放器共享音频 Codec
- 向调用方返回执行结果

### 1.2 cap_asr 核心职责
- 通过板级管理器获取板载 ADC 设备句柄（ES7210 麦克风）
- 对于共享编解码器的板型（如 ES8311），先打开 DAC 提供麦克风偏置电压，再配置 I2S RX 为 16kHz stereo
- de-interleave 提取 mono PCM
- 通过 **voice_ws_common** 共享 WebSocket 连接发送音频数据
- 将 640B（20ms）PCM 块流式发送到语音服务器
- 支持 **静音自动停止**（连续 50 个全零块，约 1 秒）和 **最大录制时长**（默认 10 秒）
- 通过 WebSocket 文本回调接收 ASR 识别结果
- 通过 **信号量** 通知调用方（claw_core）识别完成
- 提供完整的资源清理机制，ASR 结束后关闭 ADC 设备
- 与 cap_voice 互斥使用（ASR 录制和 TTS 播放不能同时进行）

### 1.3 voice_ws_common 核心职责
- 管理到语音服务器的**单一 WebSocket 连接**（cap_voice 和 cap_asr 共享）
- 提供连接管理 API：`voice_ws_set_server`、`voice_ws_connect`
- 提供消息发送 API：`voice_ws_send_text`、`voice_ws_send_binary`
- 多订阅者回调分发：文本帧按 `type` 字段路由到注册的处理函数
- 维护 TCP Keep-Alive（30s 空闲 / 10s 间隔 / 3 次探测）和 Ping（15s）
- 自动重连（5s 间隔）
- 最多支持 4 个回调/类型（`MAX_HANDLERS = 4`）

### 1.4 lua_module_audio 核心职责
- 创建音频输入/输出句柄（通过 `esp_codec_dev` 抽象层）
- WAV 文件录制（ADC → 文件）和播放（文件 → DAC）
- 正弦音调生成与播放（无需外部音频文件）
- 音频回路监听（输入直通输出，用于实时监控）
- 音量/增益/静音控制
- 麦克风电平和峰值检测（RMS / Peak）
- FFT 频谱分析（Hann 窗 + 对数频带划分）
- 通过 Lua 元表实现句柄生命周期管理（GC 自动关闭设备）

### 1.5 组件位置

```
# TTS Capability
components/claw_capabilities/cap_voice/
├── CMakeLists.txt           # 组件构建配置
├── include/
│   └── cap_voice.h          # 公开 API 头文件
├── skills/
│   └── cap_voice/
│       └── SKILL.md         # LLM Skill 文档
└── src/
    └── cap_voice.c          # 核心实现 (~900行)

# ASR Capability
components/claw_capabilities/cap_asr/
├── CMakeLists.txt           # 组件构建配置
├── include/
│   └── cap_asr.h            # 公开 API 头文件
├── skills/
│   └── cap_asr/
│       └── SKILL.md         # LLM Skill 文档
└── src/
    └── cap_asr.c            # 核心实现 (~330行)

# 共享 WebSocket 组件
components/common/voice_ws_common/
├── CMakeLists.txt           # 组件构建配置
├── include/
│   └── voice_ws_common.h    # 公开 API 头文件
└── src/
    └── voice_ws_common.c    # 核心实现 (~220行)

# Lua 音频模块
components/lua_modules/lua_module_audio/
├── CMakeLists.txt           # 组件构建配置
├── idf_component.yml        # 组件清单
├── README.md                # 使用文档
├── src/
│   ├── lua_module_audio.h   # 注册接口头文件
│   └── lua_module_audio.c   # 核心实现 (~1100行)
└── test/
    └── audio_record_play.lua # 录制/播放测试脚本
```

---

## 2. 架构层次

语音模块采用**分层架构**，从底层到顶层依次为：

```
┌─────────────────────────────────────────────────┐
│                   Layer 5                        │
│              Capability 接口层                     │
│  (描述符定义 / 注册 / 执行回调)                       │
│  cap_voice: get_tts   cap_asr: start_asr         │
├─────────────────────────────────────────────────┤
│                   Layer 4                        │
│           语音请求管理层（TTS + ASR）                │
│  TTS: 请求构造 / 发送 / 同步等待 / 超时处理          │
│  ASR: ADC 打开 / 录制控制 / 静音检测 / 结果等待      │
├─────────────────────────────────────────────────┤
│                   Layer 3                        │
│          voice_ws_common 共享 WebSocket 层         │
│  (连接管理 / 文本帧 type 分发 / 二进制帧分发)         │
│  cap_voice 和 cap_asr 共用单一 WS 连接              │
│  多订阅者回调注册模式                                │
├─────────────────────────────────────────────────┤
│                   Layer 2                        │
│            语音处理任务层                            │
│  TTS: voice_proc (Opus 帧队列 / 解码 / 重采样)      │
│  ASR: asr_capture (I2S RX / de-interleave /      │
│       静音检测 / PCM 流式上传)                      │
├─────────────────────────────────────────────────┤
│                   Layer 1                        │
│          音频驱动 & codec_dev 抽象层                 │
│  DAC (ES8311) 用于 TTS 播放                       │
│  ADC (ES7210) 用于 ASR 录制                       │
│  播放任务 / 环形缓冲区 / 设备句柄管理                 │
└─────────────────────────────────────────────────┘
```

### 2.1 关键设计决策

#### 2.1.1 共享 WebSocket 连接（voice_ws_common）

语音服务器（`local_server.ts`）只接受单一 ESP32 WebSocket 连接。因此 cap_voice 和 cap_asr 通过共享组件 `voice_ws_common` 共用同一连接，采用多订阅者回调注册模式分发不同类型的消息。

```
voice_ws_common (单一 WS 连接)
  ├── tts_text_handler    → cap_voice (tts_end/error/tts_start/status/pong)
  ├── tts_binary_handler  → cap_voice (Opus 音频帧)
  ├── tts_state_handler   → cap_voice (断连时通知错误)
  ├── asr_text_handler    → cap_asr  (asr_result/error)
  └── asr_state_handler   → cap_asr  (断连时通知错误)
```

每种回调类型最多支持 4 个注册（`MAX_HANDLERS = 4`），通过数组遍历分发。

#### 2.1.2 解耦 WebSocket 接收与音频解码（TTS）

**为什么需要 Opus 帧队列？**

| 问题 | 后果 | 解决方案 |
|------|------|----------|
| Opus 解码 + 重采样每帧耗时 2-5ms | WebSocket 回调执行时间过长，阻塞事件循环 | 在回调中只做 `malloc + memcpy` 入队，轻量返回 |
| TTS 服务器可能突发发送大量帧 | 解码跟不上接收速度，丢帧 | 256 帧队列深度（约 5 秒音频），吸收突发 |
| WebSocket 回调在特定任务上下文中执行 | 不允许长时间阻塞 | 解码转移到专用 `voice_proc` 任务（优先级 6） |

**数据流（TTS）：**

```
[WebSocket 回调] ──(轻量: malloc+memcpy+入队)──→ [Opus 帧队列]
                                                       ↓
                                              [voice_proc 任务]
                                              批量解码 + 重采样
                                                       ↓
                                              [PCM 环形缓冲区]
                                                       ↓
                                              [playback_task] → [Codec DAC]
```

**数据流（ASR）：**

```
[ES7210 MIC] → [I2S RX 16kHz stereo] → [asr_capture 任务]
  → de-interleave stereo→mono → 640B PCM chunks
  → voice_ws_send_binary() → [WebSocket] → [Voice Server]
  → Volcano ASR API → asr_result JSON
  → asr_text_handler() → 信号量通知 → 返回文本给调用者
```

#### 2.1.3 I2S 采样率互斥与麦克风偏置

ES8311（DAC）和 ES7210（ADC）共享 I2S MCLK/BCLK/WS 总线。ASR 录制（16kHz）和 TTS 播放（48kHz）不能同时进行。由于语音对话流程是串行的（录制 → 识别 → LLM → 播放），每次操作前重新配置设备采样率：`ensure_adc_dev()` 打开 ADC 为 16kHz stereo，`ensure_audio_dev()` 打开 DAC 为 48kHz stereo。

**特殊处理：** 对于共享 ADC/DAC Codec 的板型（如使用 ES8311 单芯片的板子），ADC 麦克风通道依赖于 DAC 部分提供的偏置电压（MIC Bias）。`ensure_adc_dev()` 在打开 ADC 之前会先尝试打开 DAC，配置为相同采样率（16kHz）并静音输出，以确保麦克风正常工作。对于独立 ADC 的板型（如使用 ES7210 + ES8311 分离方案的板子），DAC 句柄获取失败时会跳过此步骤，不影响 ADC 的正常打开。

---

## 3. 各层详解

### 3.1 Layer 1 — PCM 播放引擎 & 音频驱动

#### 3.1.1 音频设备初始化 (`ensure_audio_dev`)

从板级管理器中获取音频 DAC 设备句柄，配置为 **48kHz / 双声道 / 16 位** 格式并打开设备：

```c
esp_codec_dev_sample_info_t fs = {
    .sample_rate = 48000,
    .channel = 2,
    .bits_per_sample = 16,
};
esp_codec_dev_open(s_codec_dev, &fs);
esp_codec_dev_set_out_vol(s_codec_dev, 80);  // 音量 80%
```

> **重要：** 每次 TTS 调用都会重新打开 Codec 设备。这是因为视频播放器可能已更改共享音频 Codec 的配置（不同采样率/声道数），必须重新配置为 TTS 格式。

**依赖组件：**
- `esp_board_manager` — 获取板载音频 DAC 设备句柄
- `esp_codec_dev` — 音频编解码设备抽象层

#### 3.1.2 PCM 环形缓冲区

使用 **SPIRAM** 分配的环形缓冲区存储待播放的 PCM 数据：

| 参数 | 值 |
|------|------|
| 缓冲区大小 | `48000 × 4 = 192,000` 个 int16_t sample |
| 音频时长 | 约 4 秒（192,000 ÷ 48,000 = 4.0s） |
| 实际内存 | `192,000 × 2 = 384 KB`（int16_t 是 2 字节） |
| 数据类型 | `int16_t`（交错 L/R 布局） |
| 存储位置 | `MALLOC_CAP_SPIRAM`（外部 PSRAM） |
| 访问保护 | `s_playback_mutex`（互斥信号量） |

#### 3.1.3 播放任务 (`playback_task`)

独立的 FreeRTOS 任务（`voice_play`，栈大小 **3072**，优先级 5），负责：

1. 轮询环形缓冲区是否有可用数据
2. 每次最多取 960 个 sample（约 10ms @ 48kHz）写入 codec
3. 通过 `esp_codec_dev_write` 将 PCM 数据送入 DAC
4. **写错误处理**：每次写入失败时递增计数器，仅在出错时打印日志（前 3 次 + 每 100 次）；连续错误不会导致崩溃，播放任务继续轮询
5. 任务退出时置 `s_playback_task = NULL`

```
[voice_proc 解码+重采样] → push_pcm_async() → [环形缓冲区]
                                                     ↓
                                           playback_task → esp_codec_dev_write() → [DAC] → [扬声器]
```

---

### 3.2 Layer 2 — 语音处理任务层 (voice_proc)

这是语音模块的核心处理层，专为 ESP32-P4 多核环境设计的流水线处理引擎。

#### 3.2.1 Opus 帧队列

| 参数 | 值 | 说明 |
|------|-----|------|
| 队列深度 | 256 帧 | 每个 Opus 帧 ~20ms 音频；256 帧 ≈ 5 秒缓冲 |
| 数据类型 | `opus_frame_t` | `{uint8_t *data, size_t len}` |
| 创建时机 | `start_voice_process_task()` 首次调用时 |
| 销毁时机 | `voice_cleanup()` 清理时 |

队列项定义：
```c
typedef struct {
    uint8_t *data;
    size_t len;
} opus_frame_t;
```

`data` 指针由 `tts_binary_handler` 通过 `malloc` 分配，`voice_process_task` 解码后通过 `free` 释放。如果队列满了，入队超时后直接 `free` 丢弃。

#### 3.2.2 语音处理任务 (`voice_process_task`)

独立的 FreeRTOS 任务（`voice_proc`，栈大小 **8192**，优先级 **6**），负责接收 Opus 帧并解码播放。

**核心作用：** 将 WebSocket 回调中的繁重计算工作（Opus 解码 + 重采样）卸载到独立任务，确保 WebSocket 回调轻量快速（仅 `malloc + memcpy + xQueueSend`）。

**任务停止机制：** 当 `voice_cleanup()` 被调用时，会向队列发送一个 `{ .data = NULL, .len = 0 }` 的停止帧。`voice_process_task` 收到 `data == NULL` 的帧后，会释放 Opus 解码器并退出任务。由于队列可能已满（256 帧），发送停止帧时最多重试 5 次，每次从队列中 drain 一个旧帧腾出空间。

#### 3.2.3 批量处理与 WDT 保护

**关键设计：** 为防止任务看门狗（Task WDT）超时，语音处理任务采用**批量处理 + 让出 CPU** 策略：

```
每批最多处理 10 帧
   ↓
处理完一批后 vTaskDelay(1) 让出 CPU
   ↓
IDLE 任务运行，重置 Task WDT
   ↓
继续处理下一批
```

| 参数 | 值 | 说明 |
|------|-----|------|
| 批量大小上限 | 10 帧 | 每帧解码 ~2-5ms，一批 ≈ 20-50ms CPU 时间 |
| 让出间隔 | 1 tick (~10ms @ 100 Hz) | 足够 IDLE 任务重置 5 秒 WDT |
| 空队列行为 | 首次阻塞等待 `portMAX_DELAY` | 非空时立即返回，不阻塞 |

**伪代码逻辑：**
```c
while (1) {
    frames_this_batch = 0;
    
    while (frames_this_batch < 10) {
        frame = xQueueReceive(queue, timeout=0);
        if (队列空) {
            if (本批未处理任何帧) 阻塞等待;
            else break;  // 结束本批
        }
        
        // 解码 Opus
        // 重采样 24kHz Mono → 48kHz Stereo
        // push_pcm_async() 推入环形缓冲区
        frames_this_batch++;
    }
    
    vTaskDelay(1);  // 让出 CPU, 重置 WDT
}
```

#### 3.2.4 Opus 解码器初始化 (`ensure_opus_decoder`)

| 配置项 | 值 |
|--------|------|
| 采样率 | 24000 Hz |
| 声道数 | 1 (Mono) |
| 帧时长 | 20ms |
| 自定界 | 否（由帧边界判断） |

**依赖组件：** `esp_audio_codec` / `esp_opus_dec`

解码流程：
```c
esp_opus_dec_register();           // 注册 Opus 解码器
esp_opus_dec_open(&cfg, ...);      // 打开解码器实例
esp_opus_dec_decode(dec, &in_raw, &out_frame, &dec_info);  // 解码
```

**解码缓冲区：** 8KB（`OPUS_DECODE_BUF_SIZE`）

#### 3.2.5 重采样 (`resample_mono_to_stereo_48k`)

TTS 服务器发送的是 **24kHz 单声道 Opus** 音频，而 DAC 需要 **48kHz 立体声 PCM**，因此需要进行重采样和声道复制：

| 转换方向 | 说明 |
|----------|------|
| 输入 | 24kHz Mono PCM (int16_t) |
| 输出 | 48kHz Stereo PCM (int16_t, L/R 相同) |
| 算法 | 线性插值 (Linear Interpolation) |

**工作原理：**
- 计算采样步长 `step = src_rate / 48000 = 0.5`
- 对每个输出位置通过线性插值从源数据中计算采样值
- 每个输出 sample 复制到左右声道

**内存分配：** 重采样的目标缓冲也使用 `MALLOC_CAP_SPIRAM`，避免耗尽 DRAM。

```c
// 伪代码
for (pos = 0; pos < src_samples; pos += step) {
    val = linear_interpolate(src, pos);
    dst[L] = val;
    dst[R] = val;
}
```

---

### 3.3 Layer 3 — voice_ws_common 共享 WebSocket 层

#### 3.3.1 设计动机

语音服务器（`local_server.ts`）在同一时刻只接受一个 ESP32 WebSocket 连接。为避免 cap_voice（TTS）和 cap_asr（ASR）之间的连接冲突，将 WebSocket 管理提取为独立的共享组件 `voice_ws_common`。

#### 3.3.2 多订阅者回调模式

`voice_ws_common` 维护一个 WebSocket 客户端句柄，通过回调注册机制分发不同类型的消息：

```
                    ┌─────────────────────────┐
                    │    voice_ws_common      │
                    │  (单一 WS 连接管理)      │
                    │                         │
                    │  ws_event_handler()     │
                    │    ├─ op_code=0x01      │
                    │    │   text frame →     │
                    │    │   cJSON解析type    │
                    │    │   → 遍历text_handler│
                    │    │                     │
                    │    └─ op_code=0x02      │
                    │        binary frame →   │
                    │        → 遍历binary_handler│
                    └──┬──────┬──────┬────────┘
                       │      │      │
              ┌────────▼┐ ┌──▼────┐ ┌▼─────────┐
              │ cap_voice│ │cap_asr│ │(可扩展)   │
              │ tts_text │ │asr_txt│ │          │
              │ tts_bin  │ │asr_st │ │          │
              │ tts_state│ │       │ │          │
              └──────────┘ └───────┘ └──────────┘
```

每种回调类型最多支持 4 个注册（`MAX_HANDLERS`），通过数组顺序遍历分发。如果同一个 type 需要被多个组件处理（如 `error` 和 `state` 变化），所有注册的回调都会收到通知。

**回调注册的模块化设计：**
- cap_voice 在 `ensure_ws_connected()` 中注册 TTS 回调（`s_ws_handlers_registered` 标记）
- cap_asr 在 `cap_asr_execute()` 中注册 ASR 回调（`s_asr_handlers_registered` 标记）
- 两者互不依赖，各自维护自己的 "已注册" 标记

#### 3.3.3 公开 API

| 函数 | 用途 |
|------|------|
| `voice_ws_set_server(ip, port)` | 配置服务器地址 |
| `voice_ws_connect(timeout_ms)` | 建立 WebSocket 连接 |
| `voice_ws_is_connected()` | 查询连接状态 |
| `voice_ws_send_text(json)` | 发送 JSON 文本帧 |
| `voice_ws_send_binary(data, len)` | 发送二进制帧 |
| `voice_ws_register_text_handler(cb, user_data)` | 注册文本帧回调（按 type 字段路由） |
| `voice_ws_register_binary_handler(cb, user_data)` | 注册二进制帧回调 |
| `voice_ws_register_state_handler(cb, user_data)` | 注册连接状态变化回调 |

#### 3.3.4 连接管理

**服务器 URL：** `ws://<server_ip>:<server_port>`

通过 `cap_voice_set_server(ip, port)` 在系统初始化时配置（同时调用 `voice_ws_set_server` 同步配置 ASR 的服务器地址）。

**连接配置：**
| 参数 | 值 |
|------|------|
| 自动重连 | 开启 (`disable_auto_reconnect = false`) |
| 重连超时 | 5000ms |
| 缓冲区大小 | 16384 字节 |
| 网络超时 | 10000ms |
| 任务栈大小 | 12288 字节 |
| Keep-Alive | 开启 |
| Keep-Alive 空闲 | 30 秒 |
| Keep-Alive 间隔 | 10 秒 |
| Keep-Alive 探测次数 | 3 次 |
| Ping 间隔 | 15 秒 |
| 最大回调数 | 4 个/类型 |

#### 3.3.5 WebSocket 事件处理

| 事件类型 | 处理逻辑 |
|----------|----------|
| `WEBSOCKET_EVENT_CONNECTED` | 标记连接状态，释放连接信号量，通知所有 state_handler |
| `WEBSOCKET_EVENT_DISCONNECTED` | 标记断开，通知所有 state_handler |
| `WEBSOCKET_EVENT_ERROR` | 记录错误日志 |
| `WEBSOCKET_EVENT_DATA` (Binary, `op_code=0x02`, `fin=1`) | 遍历调用所有 binary_handler 回调 |
| `WEBSOCKET_EVENT_DATA` (Text, `op_code=0x01`, `fin=1`) | cJSON 解析 type 字段 → 遍历调用所有 text_handler 回调 |

文本帧处理中增加了长度保护（`data_len > 0 && data_len < 1024`）防止超大 JSON 导致内存问题。

#### 3.3.6 完整协议（服务器 ↔ ESP32）

**ESP32 → 服务器（文本帧）：**

| 消息 | 用途 | 发送方 |
|------|------|--------|
| `{"type":"tts_request","text":"...","voice":"..."}` | 请求 TTS 合成 | cap_voice |
| `{"type":"asr_start"}` | 开始 ASR 录制 | cap_asr |
| `{"type":"asr_stop"}` | 结束 ASR 录制 | cap_asr |

**ESP32 → 服务器（二进制帧）：**

| 内容 | 用途 | 发送方 |
|------|------|--------|
| PCM 音频 (16kHz mono 16-bit, 640B/chunk) | ASR 麦克风音频流 | cap_asr |

**服务器 → ESP32（文本帧）：**

| 消息 | 处理方 |
|------|--------|
| `{"type":"tts_start","sample_rate":24000,"channels":1}` | cap_voice |
| `{"type":"tts_end"}` | cap_voice |
| `{"type":"asr_result","text":"...","is_final":true/false}` | cap_asr |
| `{"type":"error","message":"..."}` | cap_voice / cap_asr（按 s_tts_active / s_asr_active 区分） |
| `{"type":"status","message":"..."}` | cap_voice |
| `{"type":"pong"}` | cap_voice |

**服务器 → ESP32（二进制帧）：**

| 内容 | 用途 | 处理方 |
|------|------|--------|
| 裸 Opus 音频包 | TTS 合成音频 | cap_voice → tts_binary_handler |

**Ogg/Opus 容器头过滤：**

TTS 服务器可能在二进制帧前发送 `OpusHead` 和 `OpusTags` 元数据包。cap_voice 的 `tts_binary_handler` 通过检查前 8 字节跳过这些非音频帧。

---

### 3.4 Layer 4 — TTS 请求管理层

#### 3.4.1 TTS 请求发送 (`send_tts_request`)

构造 JSON 请求并通过 WebSocket 发送：

```json
{
  "type": "tts_request",
  "text": "需要朗读的文本",
  "voice": "zh_female_qingxin"
}
```

**参数说明：**
| 字段 | 必须 | 说明 | 默认值 |
|------|------|------|--------|
| `text` | 是 | 要转为语音的文本 | - |
| `voice` | 否 | 语音类型 ID | `zh_female_qingxin` |

**发送超时：** 3000ms

#### 3.4.2 TTS 生命周期管理

```
┌──────────────────┐
│  s_tts_active    │ ← true：标记 TTS 会话进行中
├──────────────────┤
│  s_tts_error     │ ← 初始 false
├──────────────────┤
│  s_tts_result_text│ ← 结果字符串缓冲区 (256字节)
└──────────────────┘

TTS 完成信号量：s_tts_done_sem (Binary Semaphore)
TTS 超时时间：45000ms (45秒)
```

**TTS 执行时序：**

```
1. 设置 s_tts_active = true
2. 发送 TTS 请求
3. 等待 s_tts_done_sem（最多 45 秒）
   ├─ tts_end 到达 → s_tts_error = false → 成功返回
   ├─ error 到达 → 记录错误信息 → 失败返回
   ├─ WebSocket 断开 → s_tts_error = true → 失败返回
   └─ 超时 → s_tts_active = false → TIMEOUT 返回
```

在等待期间：
- WebSocket 回调轻量接收 Opus 帧并入队列
- `voice_proc` 任务异步解码并推入环形缓冲区
- `playback_task` 从环形缓冲区读取数据送入 DAC 播放
- **音频播放与 TTS 等待并行**：无需等待 TTS 完成即可开始播放

#### 3.4.3 `push_pcm_async` — 带反压的 PCM 推送

与旧版静默丢弃不同，新版实现了**带超时的反压机制**：

```c
static esp_err_t push_pcm_async(const int16_t *pcm, size_t samples)
{
    while (remaining > 0) {
        // 检查剩余空间
        space = PLAYBACK_BUF_SAMPLES - s_playback_available;
        if (space == 0) {
            // 缓冲区满：等待播放任务消费（最多 2 秒）
            if (超时) return ESP_ERR_TIMEOUT;
            vTaskDelay(pdMS_TO_TICKS(10));
            continue;
        }
        
        // 写入环形缓冲区
        memcpy(...);
        s_playback_available += chunk;
    }
}
```

| 参数 | 值 |
|------|-----|
| 推送超时 | 2000ms |
| 等待粒度 | 10ms |
| 静默丢弃 | ❌ 不再使用（改为反压等待） |

**保护检查：** 函数在每次循环前检查 `s_playback_running` 状态。如果播放任务已停止，立即返回 `ESP_ERR_INVALID_STATE`，避免在清理过程中写入已释放的环形缓冲区。

---

### 3.5 Layer 5 — Capability 接口层

#### 3.5.1 描述符定义

```c
static const claw_cap_descriptor_t s_voice_descriptors[] = {
    {
        .id = "get_tts",
        .name = "get_tts",
        .family = "voice",
        .description = "Convert text to speech via local WebSocket TTS server...",
        .kind = CLAW_CAP_KIND_CALLABLE,
        .cap_flags = CLAW_CAP_FLAG_CALLABLE_BY_LLM,
        .input_schema_json = "{...}",  // JSON Schema
        .execute = cap_voice_execute,
    },
};
```

**Capability 元数据：**
| 属性 | 值 |
|------|------|
| `id` | `get_tts` |
| `family` | `voice` |
| `kind` | `CALLABLE`（非事件源） |
| `cap_flags` | `CALLABLE_BY_LLM` |
| 输入 Schema | `{text: string (required), voice: string (optional)}` |

#### 3.5.2 Group 注册

```c
static const claw_cap_group_t s_voice_group = {
    .group_id = "cap_voice",
    .descriptors = s_voice_descriptors,
    .descriptor_count = 1,
};
```

**注意：** 模块**不定义生命周期钩子**（`init`/`start`/`stop` 均为 NULL）。WebSocket 连接在每次 `get_tts` 调用时按需建立，调用完成后通过 `voice_cleanup()` 释放所有资源。

#### 3.5.3 注册与配置入口

在 `app_capabilities.c` 中：

```c
// 注册前的准备：配置 TTS 服务器地址
cap_voice_set_server(CONFIG_APP_CLAW_VOICE_SERVER_IP,
                     (uint16_t)CONFIG_APP_CLAW_VOICE_SERVER_PORT);

// 注册到 claw_cap 系统
return cap_voice_register_group();
```

**编译宏控制：**
- `CONFIG_APP_CLAW_CAP_VOICE` — 启用语音模块
- `CONFIG_APP_CLAW_VOICE_SERVER_IP` — TTS 服务器 IP
- `CONFIG_APP_CLAW_VOICE_SERVER_PORT` — TTS 服务器端口

---

## 4. 调用路径

```
用户/LLM/Console
      │
      ▼
claw_cap_call_from_core(id="get_tts", input_json)
      │
      ▼
cap_voice_execute(input_json, ctx, output, output_size)
      │
      ├─ 1. 检查服务器配置
      ├─ 2. 建立 WebSocket 连接 (ensure_ws_connected)
      ├─ 3. 启动语音处理任务 (start_voice_process_task)
      │      ├─ 创建 Opus 帧队列
      │      └─ 创建 voice_proc 任务
      ├─ 4. 初始化音频设备 (ensure_audio_dev)
      ├─ 5. 启动播放任务 (start_playback_task)
      ├─ 6. 解析 JSON 输入 (text + voice)
      ├─ 7. 发送 TTS 请求 (send_tts_request)
      ├─ 8. 等待 TTS 完成或超时 (xSemaphoreTake, 45s)
      │       └── [异步流水线] ws回调→队列→voice_proc解码→环形缓冲区→playback_task→DAC
      └─ 9. voice_cleanup (根据成功/失败选择 graceful/immediate)
            → 释放所有资源
      └─ 10. 输出结果字符串
```

### 4.1 支持的三条调用路径

| 调用源 | 调用方式 | 说明 |
|--------|----------|------|
| **LLM 工具调用** | `claw_cap_call_from_core` | LLM 通过 `get_tts` 工具触发 |
| **Console 命令** | `cap call get_tts '{"text":"..."}'` | 终端直接调用 |
| **Event Router 规则** | `call_cap` 动作 | 自动规则触发（需配置） |

---

## 5. 依赖关系

### 5.1 编译依赖

| 依赖组件 | 用途 |
|----------|------|
| `claw_cap` | Capability 框架核心 |
| `cJSON` | JSON 解析与构造 |
| `esp_codec_dev` | 音频编解码设备抽象 |
| `esp_board_manager` | 获取板载 DAC/ADC 设备句柄 |
| `esp_websocket_client` | WebSocket 客户端（基于 esp-tls） |
| `esp_audio_codec` (esp_opus_dec) | Opus 音频编解码器 |
| `FreeRTOS` | 任务、信号量、互斥量、队列 |
| `heap_caps` | SPIRAM 分配 |

### 5.2 运行时依赖

| 组件 | 条件 | 说明 |
|------|------|------|
| 音频 DAC | 必须 | 板载扬声器/DAC，用于 TTS 播放 |
| 音频 ADC | 必须（ASR 时） | 板载麦克风/ADC，用于 ASR 录制 |
| TTS/ASR 服务器 | 必须 | 本地或局域网 WebSocket 服务器 |
| Wi-Fi 连接 | 必须 | 与语音服务器通信 |
| SPIRAM | **强烈推荐** | 音频环形缓冲区 + 重采样缓冲分配 |

---

## 6. cap_asr 能力详解

cap_asr 是语音识别（ASR）能力组件，通过 I2S 麦克风采集音频并通过 shared WebSocket 发送到云端进行识别。与 cap_voice 共享 `voice_ws_common` 连接和 Kconfig 服务器配置。

### 6.1 架构概览

```
┌──────────────────────────────────────────────────────────────┐
│                    cap_asr_execute()                          │
│                                                              │
│  1. 解析输入 JSON (max_duration_ms, silence_timeout_ms)      │
│  2. 检查 WebSocket 连接（如未连接则自动建立）                   │
│  3. 注册 ASR 回调 (如未注册)                                   │
│  4. ensure_adc_dev() → 打开 DAC (提供 MIC Bias) + 打开 ADC   │
│  5. 创建完成信号量                                             │
│  6. 启动 asr_capture 任务                                     │
│  7. 阻塞等待信号量 (最多 max_duration + 15s)                   │
│  8. asr_cleanup() → 停止任务、关闭 ADC、释放资源               │
│  9. 返回识别文本                                               │
└──────────────────────────────────────────────────────────────┘
```

### 6.2 ADC 初始化与麦克风偏置 (`ensure_adc_dev`)

```
ensure_adc_dev():
  │
  ├─ 1. 按 16kHz/2ch/16bit 配置采样格式
  │
  ├─ 2. 尝试打开 DAC (MIC Bias 供给)
  │      ┌─ esp_board_manager 获取 DAC 句柄
  │      ├─ 成功 → 打开 DAC 设为 16kHz stereo
  │      │         → 静音输出 (esp_codec_dev_set_out_mute)
  │      │         → 保存 s_dac_dev 句柄
  │      │         → 日志 "DAC opened (16kHz) for mic bias"
  │      └─ 失败 → 日志警告 "Failed to open DAC, mic may not work"
  │                  (对于 ES7210 独立 ADC 的板子，此步骤跳过)
  │
  ├─ 3. 打开 ADC
  │      ┌─ esp_board_manager 获取 ADC 句柄
  │      ├─ 成功 → esp_codec_dev_open(dev, &fs)
  │      │         → 设置麦克风增益 30dB
  │      │         → 保存 s_adc_dev 句柄
  │      │         → 日志 "ADC opened (16kHz stereo 16-bit)"
  │      └─ 失败 → 返回 ESP_ERR_NOT_FOUND
  │
  └─ 4. 返回 ESP_OK
```

> **增益覆盖说明：** ES7210 驱动在 `es7210_open()` 中设置 30dB 增益，但 `es7210_start()` 会调用 `es7210_mic_select()` 覆盖为默认 0dB。`ensure_adc_dev()` 在打开 ADC 后再次调用 `esp_codec_dev_set_in_gain(dev, 30.0)` 确保硬件寄存器使用正确的增益值。

### 6.3 ASR 采集任务 (`asr_capture_task`)

独立的 FreeRTOS 任务（`asr_capture`，栈大小 **4096**，优先级 5）：

```
asr_capture_task():
  1. 分配 mono 缓冲区 (640B) + stereo 缓冲区 (1280B)
  2. 发送 asr_start 到服务器
  3. while (s_recording):
       a. esp_codec_dev_read(ADC, stereo_buf, 1280B)
       b. 诊断日志：首帧和每 50 帧打印前 8 个 sample 值
       c. de-interleave: stereo → mono (只取左声道)
       d. 全零检测 → 更新 silence_count
          └─ 若全零：记录 "MIC chunk #N is ALL ZERO" 警告日志
       e. voice_ws_send_binary(mono_buf, 640B) 发送 PCM 到服务器
       f. 检查静音超时 (silence_count >= silence_timeout_chunks) → break
       g. 检查最大时长 (elapsed_ticks >= max_duration_ms) → break
  4. 发送 asr_stop 到服务器
  5. xSemaphoreTake(s_asr_done_sem, 15s) 等待 ASR 结果
  6. 释放缓冲区、退出任务
```

**I2S 读取行为：** `esp_codec_dev_read` 返回状态码（0=OK），不是字节数。读取 1280 bytes = 640 个 stereo samples = 20ms @ 16kHz。

### 6.4 De-interleave 算法

ES7210 输出 left-channel-only stereo 数据：(L0, 0, L1, 0, L2, 0, ...)。软件提取偶数索引的 int16_t 值得到 mono：

```
for (i = 0; i < mono_samples; i++) {
    mono_buf[i] = stereo_buf[i * 2];  // 只取左声道
}
```

其中 `mono_samples = 1280 / (2 * 2) = 320` samples。

### 6.5 静音检测与自动停止

| 参数 | 默认值 | 可配置 |
|------|--------|--------|
| 检测方法 | 全零检测（所有 320 个 sample 均为 0） | — |
| 静音超时块数 | 50 块 (1 秒) | 通过 `silence_timeout_ms` 参数 |
| 最大录制时长 | 10000ms | 通过 `max_duration_ms` 参数 (最大 30000) |

**静音保护逻辑：**
- 连续全零块计数在遇到非零块时重置为 0
- `silence_timeout_ms = 0` 可禁用静音自动停止
- 静音检测使用 `is_all_zero()` 辅助函数

### 6.6 ASR 结果回调 (`asr_text_handler`)

由 `voice_ws_common` 的文本帧分发调用，只会在 `s_asr_active == true` 时处理接收到的消息：

| 消息类型 | 处理逻辑 |
|----------|----------|
| `asr_result` | 更新 `s_asr_result`，若 `is_final=true` 则设置 `s_asr_active=false`，释放完成信号量 |
| `error` (s_asr_active 时) | 记录错误消息到 `s_asr_result`，设置 `s_asr_error=true`，设置 `s_asr_active=false`，释放完成信号量 |

**部分结果处理：** `is_final=false` 的 `asr_result` 消息持续更新 `s_asr_result` 缓冲区但不释放信号量，允许调用方在最终结果到达后获取最新的识别文本。

### 6.7 资源清理 (`asr_cleanup`)

```
asr_cleanup():
  1. s_recording = false (通知 asr_capture_task 退出循环)
  2. 等待 capture 任务退出 (最多 500ms, 50×10ms 轮询)
     └─ 若未退出则强制 vTaskDelete()
  3. s_capture_task = NULL
  4. 关闭 ADC 设备 (release_adc_dev → esp_codec_dev_close)
  5. 删除完成信号量 (vSemaphoreDelete)
  6. s_asr_active = false
```

与 cap_voice 类似，ASR 结束后必须关闭 ADC 设备，释放与视频播放器等组件的共享硬件资源。

### 6.8 能力描述符

```c
static const claw_cap_descriptor_t s_asr_descriptors[] = {
    {
        .id = "start_asr",
        .name = "start_asr",
        .family = "voice",
        .description = "Listen to microphone and convert speech to text "
                       "via cloud ASR. Captures audio until silence or "
                       "timeout, returns transcribed text.",
        .kind = CLAW_CAP_KIND_CALLABLE,
        .cap_flags = CLAW_CAP_FLAG_CALLABLE_BY_LLM,
        .input_schema_json = {
            max_duration_ms:    integer (optional, default 10000, max 30000)
            silence_timeout_ms: integer (optional, default 1000, 0=disable)
        },
    },
};
```

### 6.9 ASR 调用时序

```
LLM / Console
      │
      ▼
claw_cap_call_from_core(id="start_asr", input_json)
      │
      ▼
cap_asr_execute(input_json, ctx, output, output_size)
      │
      ├─ 1. 解析参数 (max_duration_ms, silence_timeout_ms)
      ├─ 2. 检查 voice_ws_is_connected()
      │     └─ 未连接 → voice_ws_connect(10000) 自动建立
      ├─ 3. 注册 ASR 回调 (首次调用时注册一次)
      ├─ 4. ensure_adc_dev() → DAC (MIC Bias) + ADC 16kHz stereo
      ├─ 5. 创建完成信号量
      ├─ 6. xTaskCreate(asr_capture_task, prio=5, stack=4096)
      │       └── [异步] asr_start → PCM 流式上传 → asr_stop
      │                  → 等待 asr_result → 释放信号量
      ├─ 7. xSemaphoreTake(s_asr_done_sem, max_duration + 15s + 5s)
      └─ 8. asr_cleanup() → 释放所有资源
            → 返回识别文本字符串
```

### 6.10 与 cap_voice 的对比

| 维度 | cap_voice (TTS) | cap_asr (ASR) |
|------|-----------------|---------------|
| **方向** | 输出 (文本 → 语音) | 输入 (语音 → 文本) |
| **音频设备** | DAC (ES8311, 48kHz stereo) | ADC (ES7210, 16kHz mono) |
| **DAC 辅助** | 直接使用 DAC 播放 | 先打开 DAC 提供 MIC Bias，再打开 ADC |
| **WebSocket 上行** | JSON 文本帧 (tts_request) | 二进制帧 (PCM 640B/chunk) + JSON 文本帧 (asr_start/stop) |
| **WebSocket 下行** | 二进制帧 (Opus 音频) + JSON 文本帧 (tts_end/error) | JSON 文本帧 (asr_result/error) |
| **处理任务** | voice_proc (Opus 解码 + 重采样) | asr_capture (I2S 读取 + de-interleave) |
| **缓冲区** | 384KB 环形缓冲区 (SPIRAM) | 无持久缓冲区 (1.2KB 临时 PCM 缓冲，直通发送) |
| **任务栈** | voice_proc=8192, voice_play=3072 | asr_capture=4096 |
| **超时** | 45 秒 | max_duration + 15 秒 |
| **能力 ID** | get_tts | start_asr |
| **Codec 冲突** | 与 ASR 互斥（串行使用） | 与 TTS 互斥（串行使用） |
| **注册标记** | `s_ws_handlers_registered` | `s_asr_handlers_registered` |

### 6.11 语音对话闭环

cap_voice 和 cap_asr 共同构成完整的语音对话流水线：

```
用户说话 → [cap_asr: start_asr] → 云端 ASR 识别 → 返回文本
  → LLM 处理 → [cap_voice: get_tts] → TTS 合成 → 扬声器播放
  → 用户听到回复
```

两个能力共享同一个 WebSocket 连接（`voice_ws_common`）和同一个服务器配置（`CONFIG_APP_CLAW_VOICE_SERVER_IP/PORT`），互斥使用 I2S 总线。

---

## 7. 关键数据结构

### 7.1 cap_voice 静态全局变量

| 变量 | 类型 | 用途 |
|------|------|------|
| `s_server_url[128]` | `char[]` | TTS WebSocket 服务器 URL |
| `s_ws_handlers_registered` | `bool` | TTS WebSocket 回调已注册标记 |
| `s_opus_dec` | `void*` | Opus 解码器实例 |
| `s_opus_ready` | `bool` | Opus 解码器初始化状态 |
| `s_tts_done_sem` | `SemaphoreHandle_t` | TTS 完成同步信号量 |
| `s_tts_result_text[256]` | `char[]` | TTS 执行结果字符串 |
| `s_tts_error` | `volatile bool` | TTS 错误标记 |
| `s_tts_active` | `volatile bool` | TTS 会话进行中标记 |
| `s_codec_dev` | `esp_codec_dev_handle_t` | 音频 DAC 设备句柄 |
| `s_playback_task` | `volatile TaskHandle_t` | 播放任务句柄 |
| `s_playback_running` | `volatile bool` | 播放任务运行状态 |
| `s_playback_buf` | `int16_t*` | PCM 环形缓冲区指针 |
| `s_playback_write_idx` | `volatile size_t` | 缓冲区写索引 |
| `s_playback_read_idx` | `volatile size_t` | 缓冲区读索引 |
| `s_playback_available` | `volatile size_t` | 缓冲区可用数据量 |
| `s_playback_mutex` | `SemaphoreHandle_t` | 缓冲区互斥锁 |
| `s_opus_frame_queue` | `QueueHandle_t` | Opus 帧队列 |
| `s_voice_process_task` | `volatile TaskHandle_t` | 语音处理任务句柄 |

### 7.2 cap_asr 静态全局变量

| 变量 | 类型 | 用途 |
|------|------|------|
| `s_adc_dev` | `esp_codec_dev_handle_t` | ADC 设备句柄 |
| `s_dac_dev` | `esp_codec_dev_handle_t` | DAC 设备句柄（仅用于 MIC Bias） |
| `s_recording` | `volatile bool` | 录制状态标记 |
| `s_capture_task` | `volatile TaskHandle_t` | 采集任务句柄 |
| `s_asr_done_sem` | `SemaphoreHandle_t` | ASR 完成同步信号量 |
| `s_asr_result[512]` | `char[]` | ASR 识别结果字符串 |
| `s_asr_error` | `volatile bool` | ASR 错误标记 |
| `s_asr_active` | `volatile bool` | ASR 会话进行中标记 |
| `s_max_duration_ms` | `uint32_t` | 最大录制时长 |
| `s_silence_timeout_chunks` | `uint32_t` | 静音超时块数 |
| `s_asr_handlers_registered` | `bool` | ASR WebSocket 回调已注册标记 |

### 7.3 重要类型定义

```c
/* Opus 帧队列项 */
typedef struct {
    uint8_t *data;    // 帧数据指针（malloc 分配）
    size_t len;       // 帧数据长度
} opus_frame_t;
```

### 7.4 常量

| 常量 | 值 | 说明 |
|------|-----|------|
| `VOICE_DEFAULT_VOICE_TYPE` | `"zh_female_qingxin"` | 默认语音类型 |
| `WS_CONNECT_TIMEOUT_MS` | 10000 | WebSocket 连接超时 |
| `TTS_TIMEOUT_MS` | 45000 | TTS 完成等待超时 |
| `ASR_RESULT_TIMEOUT_MS` | 15000 | ASR 识别结果等待超时 |
| `ASR_CHUNK_BYTES` | 640 | ASR mono PCM 块大小 (20ms) |
| `ASR_STEREO_READ_BYTES` | 1280 | I2S stereo 读取块大小 (20ms) |
| `OPUS_DECODE_BUF_SIZE` | 8192 | Opus 解码缓冲区大小 |
| `PLAYBACK_BUF_SAMPLES` | 192000 | PCM 环形缓冲区采样数 (4秒) |
| `OPUS_FRAME_QUEUE_LEN` | 256 | Opus 帧队列深度 (~5秒音频缓冲) |
| `VOICE_PROCESS_STACK` | 8192 | 语音处理任务栈大小 |
| `MAX_HANDLERS` | 4 | 每种回调类型的最大注册数 |

---

## 8. 资源清理机制 (`voice_cleanup`)

### 8.1 设计动机

语音模块调用结束时必须释放所有资源，原因如下：

1. **音频 Codec 共享：** `esp_codec_dev` 是系统级设备（与视频播放器共享），必须在 TTS 完成后关闭，允许视频播放器重新配置
2. **DRAM 回收：** 任务栈（12KB + 8KB + 3KB）和 TCB 占用内部 DRAM，这是 ESP 平台上的稀缺资源
3. **PSRAM 回收：** 重采样缓冲（动态分配）和环形缓冲区（384KB）占用 SPIRAM

### 8.2 清理阶段

`voice_cleanup(graceful)` 函数按顺序执行以下步骤：

```
voice_cleanup(bool graceful)
  │
  ├─ Phase 1: 等待 Opus 帧队列排空（仅 graceful=true）
  │     最多等待 2 秒（40 次 × 50ms）
  │     轮询 uxQueueMessagesWaiting() 直到队列为空
  │
  ├─ Phase 2: 等待环形缓冲区排空（仅 graceful=true）
  │     连续 5 次检测到 s_playback_available == 0 且最多等待 2 秒
  │     需要连续 5 次采样都为空才继续（防止短暂间隙误判）
  │
  ├─ Phase 3: 停止播放任务
  │     s_playback_running = false
  │     等待任务退出并置 s_playback_task = NULL（最多 500ms）
  │     强制 vTaskDelete 删除（如未退出）
  │
  ├─ Phase 4: 停止语音处理任务
  │     发送 NULL 停止帧（{ .data = NULL, .len = 0 }）
  │     ⚠️ 若队列已满，重试最多 5 次
  │        每次失败时先 drain 一个帧腾出空间再重试
  │     等待任务退出并置 s_voice_process_task = NULL（最多 500ms）
  │     强制 vTaskDelete 删除（如未退出）
  │
  ├─ Phase 5: 关闭音频 Codec（关键！）
  │     esp_codec_dev_close(s_codec_dev)
  │     允许视频播放器重新使用
  │
  ├─ Phase 6: 释放 Opus 解码器
  │     esp_opus_dec_close(s_opus_dec)
  │     置 s_opus_dec = NULL, s_opus_ready = false
  │
  ├─ Phase 7: 释放环形缓冲区
  │     free(s_playback_buf)
  │     置 s_playback_buf = NULL
  │
  └─ Phase 8: 删除队列和信号量
        ├─ Drain Opus 帧队列中所有剩余帧（逐个 free 释放 data 内存）
        ├─ vQueueDelete(s_opus_frame_queue)
        └─ vSemaphoreDelete(s_playback_mutex)
             置 s_tts_active = false
```

### 8.3 调用时机

| 调用场景 | `graceful` 参数 | 说明 |
|----------|----------------|------|
| TTS 正常完成 | `true` | 等待音频播完再释放资源 |
| TTS 出错（服务器错误、WebSocket 断开） | `false` | 立即停止，不等待排空 |
| TTS 超时 | `false` | 立即停止，释放资源后返回 |

---

## 9. Skill 集成

语音模块的 Capability 组件都提供了配套的 Skill 文档，用于向 LLM 暴露使用指引。

### 9.1 cap_voice Skill 元数据

```json
{
  "name": "cap_voice",
  "description": "Convert text to speech (TTS) and play through speaker...",
  "metadata": {
    "cap_groups": ["cap_voice"],
    "manage_mode": "readonly"
  }
}
```

### 9.2 cap_asr Skill 元数据

```json
{
  "name": "cap_asr",
  "description": "Record microphone audio and convert speech to text via cloud ASR...",
  "metadata": {
    "cap_groups": ["cap_asr"],
    "manage_mode": "readonly"
  }
}
```

### 9.3 LLM 可见性

默认情况下，`cap_voice` **不在 LLM 初始可见的工具集中**。LLM 需要通过调用 `activate_skill` 激活 `cap_voice` Skill 后才能使用 `get_tts` 工具。这符合 AIRI 的**渐进式披露**设计原则——仅在需要时加载对应工具，以控制 LLM 上下文中的 token 数量。

### 9.4 SKILL.md 内容要点

Skill 文档中为 LLM 提供了：
- 使用场景判断：何时应使用语音输出而非文字
- 调用规则：参数格式、必填字段
- 输出说明：成功/错误字符串格式
- 常见失败原因及排查指引
- 调用示例

---

## 10. 数据流图（更新版）

```
┌─────────────┐     {"text":"你好", "voice":"zh_female_qingxin"}
│  LLM / Console │ ────────────────────────────────────────────────→
└─────────────┘                                                    │
                                                                    ▼
┌──────────────────────────────────────────────────────────────────────┐
│                        cap_voice_execute()                           │
│  ┌──────────────┐   ┌──────────────────┐   ┌────────────────────┐   │
│  │ WebSocket    │──→│ ensure_ws_connected│→ │ 连接 TTS 服务器     │   │
│  │ 连接管理     │   └──────────────────┘   └────────────────────┘   │
│  ├──────────────┤   ┌──────────────────┐   ┌────────────────────┐   │
│  │ 语音处理任务  │──→│ voice_proc 启动  │→ │ 创建 Opus 帧队列    │   │
│  │ 初始化       │   └──────────────────┘   └────────────────────┘   │
│  ├──────────────┤   ┌──────────────────┐   ┌────────────────────┐   │
│  │ TTS 请求     │──→│ send_tts_request │→ │ {"type":"tts_      │   │
│  │ 管理         │   └──────────────────┘   │  request", ...}    │   │
│  ├──────────────┤                          └────────────────────┘   │
│  │ 同步等待     │──→ xSemaphoreTake(s_tts_done_sem, 45s)            │
│  └──────────────┘                                                   │
└──────────────────────────────────────────────────────────────────────┘
                          │
                          ▼ 异步接收 (WebSocket 回调, 轻量处理)
┌──────────────────────────────────────────────────────────────────────┐
│ ws_event_handler (WEBSOCKET_EVENT_DATA)                              │
│                                                                      │
│  ┌──────────┐       ┌──────────────────────────────────────┐         │
│  │ Binary   │──→ 检查: 非 Ogg 容器头 + 合法大小 + s_tts_active       │
│  │ 音频帧   │       └──────────────────────────────────────┘         │
│  │          │──→ malloc + memcpy → xQueueSend(s_opus_frame_queue)   │
│  └──────────┘                                                       │
│                                                                      │
│  ┌──────────┐                                                       │
│  │ Text     │──→ JSON 解析 → 处理 tts_start/tts_end/error/status/pong│
│  │ 控制帧   │        │                                              │
│  └──────────┘        ├─ tts_start → 检查 sample_rate/channels       │
│                      ├─ tts_end   → 释放完成信号量                   │
│                      └─ error     → 记录错误, 释放完成信号量           │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼  Opus 帧队列
┌──────────────────────────────────────────────────────────────────────┐
│  Opus 帧队列 (256 深度, 约 5 秒缓冲)                                 │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐                │
│  │帧 1│→│帧 2│→│帧 3│→│... │→│... │→│帧 N│→│... │                │
│  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘ └────┘                │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼  voice_proc 任务 (优先级 6)
┌──────────────────────────────────────────────────────────────────────┐
│ voice_process_task (批量解码, 每批 ≤10 帧)                           │
│                                                                      │
│  while (1) {                                                         │
│      frames = 0;                                                     │
│      while (frames < 10) {                                           │
│          frame = xQueueReceive(queue, 0);                            │
│          if (队列空) { frames==0 ? 阻塞等待 : break; }               │
│          if (frame.data == NULL) { 退出清理; }                      │
│                                                                      │
│          // 1. Opus 解码                                             │
│          esp_opus_dec_decode(dec, &raw, &out_frame, &dec_info);      │
│                                                                      │
│          // 2. 重采样 (24kHz Mono → 48kHz Stereo)                    │
│          resample_mono_to_stereo_48k(src, src_samples, 24000,        │
│                                       dst, max_dst);                │
│                                                                      │
│          // 3. 推入环形缓冲区 (带反压, 超时 2s)                       │
│          push_pcm_async(playback_pcm, out_samples);                  │
│          frames++;                                                   │
│      }                                                               │
│      vTaskDelay(1);  // 让出 CPU, 重置 WDT                          │
│  }                                                                    │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│ playback_task (独立 FreeRTOS 任务, 优先级 5)                         │
│                                                                      │
│  while (running) {                                                   │
│      avail = s_playback_available;                                   │
│      if (avail > 0) {                                                │
│          chunk = MIN(avail, 960);  // ~10ms @ 48kHz                  │
│          written = esp_codec_dev_write(dac, pcm, chunk * 2);         │
│          update ring_buf read index;                                 │
│          if (written <= 0) 递增写失败计数, 打印错误日志;              │
│      }                                                               │
│      vTaskDelay(10);                                                 │
│  }                                                                    │
└──────────────────────────────────────────────────────────────────────┘
                          │
                          ▼
                ┌─────────────────────┐
                │  音频 DAC (Codec)   │
                │  48kHz / 双声道     │
                │  16位 PCM           │
                └─────────┬───────────┘
                          │
                          ▼
                ┌─────────────────────┐
                │     扬声器           │
                └─────────────────────┘
                          │
                    ── 完成后 ──
                          │
                          ▼
                ┌──────────────────────────────┐
                │ voice_cleanup(graceful=true)  │
                │  → Opus 队列排空等待 (≤2s)     │
                │  → 环形缓冲区排空等待 (≤2s)     │
                │  → 停止播放任务                 │
                │  → 停止语音处理任务             │
                │     (发送 NULL 停止帧)          │
                │  → 关闭 Codec (释放共享资源)     │
                │  → 释放所有内存                 │
                └──────────────────────────────┘
```

---

## 11. 错误处理策略

| 错误场景 | 处理方式 | 返回消息 |
|----------|----------|----------|
| TTS 服务器未配置 | 立即返回 | `Error: voice server not configured` |
| WebSocket 连接超时 | 等待 10 秒后超时 | `Error: WebSocket connection failed` |
| Opus 解码器初始化失败 | 立即返回 | `Error: Opus decoder init failed` |
| 无音频输出设备 | 立即返回 | `Error: no audio output device available` |
| 输入 JSON 解析失败 | 立即返回 | `Error: invalid JSON` |
| 缺少 `text` 字段 | 立即返回 | `Error: text is required` |
| TTS 请求发送失败 | 立即返回 | `Error: failed to send TTS request` |
| 语音处理任务启动失败 | 立即返回 | `Error: failed to start voice processing task` |
| 播放任务初始化失败 | 立即返回 | `Error: failed to start playback (...)` |
| TTS 服务器返回错误 | 返回服务器错误消息 | `Error: <server_message>` |
| TTS 超时（45秒） | 超时返回, `voice_cleanup(false)` | `Error: TTS request timed out` |
| 推送 PCM 超时（2秒） | 记录日志警告 | `ESP_ERR_TIMEOUT`（非致命） |
| WebSocket 中途断开 | 标记错误并释放信号量, `voice_cleanup(false)` | `Error: ...` |
| Opus 帧队列满 | 记录警告，丢弃帧 | 不阻塞 WebSocket 回调 |
| ASR 无可用 ADC | 立即返回 | `Error: no audio ADC device available` |
| ASR 采集任务启动失败 | 立即返回 | `Error: failed to start capture task` |
| ASR WebSocket 断开 | 标记错误, 释放信号量 | `Error: WebSocket disconnected` |
| ASR 结果超时 | 超时返回 | `Error: ASR result timeout` |
| ASR 结果为空 | 立即返回 | `Error: no result` |

---

## 12. lua_module_audio — Lua 音频模块

`lua_module_audio` 是一个 Lua 原生 C 模块，通过 `require("audio")` 在 Lua 脚本中加载。它直接操作 `esp_codec_dev` 抽象层，提供音频输入（ADC/麦克风）和输出（DAC/扬声器）的完整 API，并集成了 ESP-DSP 库的 FFT 频谱分析能力。

### 12.1 架构概览

```
┌─────────────────────────────────────────────────────┐
│                  Lua 脚本层                           │
│  local audio = require("audio")                     │
├─────────────────────────────────────────────────────┤
│               lua_module_audio                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐             │
│  │ 输入句柄  │ │ 输出句柄  │ │ 频谱分析  │             │
│  │ new_input│ │new_output│ │read_spec │             │
│  │ record_  │ │ play_wav │ │ mic_read │             │
│  │ wav      │ │ play_tone│ │ _level   │             │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘             │
│       │            │            │                   │
│       └────────────┼────────────┘                   │
│                    │                                │
│         ┌──────────┴──────────┐                    │
│         │   esp_codec_dev     │                    │
│         │ (音频 Codec 抽象层)  │                    │
│         └─────────────────────┘                    │
│                    │                                │
│         ┌──────────┴──────────┐                    │
│         │  esp_board_manager  │                    │
│         │ (板级设备管理)       │                    │
│         └─────────────────────┘                    │
└─────────────────────────────────────────────────────┘
```

### 12.2 核心 API

#### 句柄管理

| API | 参数 | 返回值 | 说明 |
|-----|------|--------|------|
| `audio.new_input(codec_dev, sr, ch, bits [, gain_db])` | codec 设备句柄 + 音频格式 + 可选增益 | `handle` / `nil, err` | 创建并激活输入句柄（ADC） |
| `audio.new_output(codec_dev, sr, ch, bits [, volume])` | codec 设备句柄 + 音频格式 + 可选音量(0-100) | `handle` / `nil, err` | 创建并激活输出句柄（DAC） |
| `audio.close(handle)` | 句柄 | `true` | 关闭设备并释放句柄 |

句柄通过 Lua 元表（`AUDIO_HANDLE_METATABLE`）管理生命周期，`__gc` 元方法确保句柄被 GC 时自动调用 `esp_codec_dev_close`。

#### 播放与录制

| API | 说明 |
|-----|------|
| `audio.play_wav(output_handle, path)` | 播放 FATFS 上的 WAV 文件，自动解析 RIFF 头并校验格式匹配 |
| `audio.play_tone(output_handle, freq_hz, duration_ms [, vol_pct [, wait_done]])` | 实时生成正弦音调并播放，支持 16/32 位 PCM，无外部文件依赖 |
| `audio.record_wav(input_handle, path, duration_ms)` | 录制指定时长的音频并写入 WAV 文件，自动生成 RIFF 头 |
| `audio.loopback(input_handle, output_handle [, duration_ms])` | 将输入直通输出（回路监听），要求输入/输出格式一致 |

#### 音量与增益控制

| API | 说明 |
|-----|------|
| `audio.set_volume(output_handle, pct)` | 设置输出音量 (0-100) |
| `audio.get_volume(output_handle)` | 获取当前输出音量 |
| `audio.set_mute(output_handle, bool)` | 静音/取消静音 |
| `audio.set_gain(input_handle, db)` | 设置输入增益 (dB) |

#### 电平与频谱分析

| API | 说明 |
|-----|------|
| `audio.mic_read_level(input_handle [, duration_ms])` | 读取麦克风 RMS 和峰值电平，默认 100ms 采样窗口 |
| `audio.read_spectrum(input_handle [, fft_size] [, band_count])` | 采集一帧 PCM 数据，执行 FFT 并返回对数频带能量 |

**频谱分析实现细节：**

```
1. 采集 fft_size 帧 PCM 数据（通过 esp_codec_dev_read）
2. 多声道混合为单声道（取平均值）
3. 应用 Hann 窗函数 (dsps_wind_hann_f32)
4. 执行复数 FFT (dsps_fft2r_fc32) + 位反转 (dsps_bit_rev_fc32)
5. 对数频带划分（梅尔刻度风格）:
   - 低频密集划分，高频稀疏
   - start_bin = min_bin * ratio^band (ratio 基于对数间距)
6. 每频带取峰值幅度，转换为 dB，再映射为 0-255 的字节值
```

| 参数 | 默认值 | 范围 | 说明 |
|------|--------|------|------|
| `fft_size` | 512 | 64-4096（2的幂） | FFT 点数 |
| `band_count` | 16 | 1..fft_size/2 | 输出频带数 |
| dB 范围 | -90 ~ -20 dBFS | - | 映射到 0-255 的动态范围 |
| 窗函数 | Hann | - | 减少频谱泄漏 |

**返回值结构：**
```lua
{
  bands = { band1_level, band2_level, ... },  -- 0-255 的频带能量值
  peak_freq_hz = 1234.5,   -- 峰值频率 (Hz)
  peak_db = -15.3,         -- 峰值幅度 (dB)
  rms = 2048,              -- RMS 电平 (int16 范围)
  fft_size = 512,
  band_count = 16,
  sample_rate = 48000,
}
```

### 12.3 WAV 文件处理

模块内置完整的 WAV/PCM 解析器，支持：

- **RIFF/WAVE 容器解析**：逐 chunk 遍历（`fmt ` → `data`），兼容含额外元数据块的 WAV 文件
- **格式校验**：仅支持 PCM 格式（`audio_format = 1`），自动校验声道数、采样率、位深度与输出句柄的匹配
- **头生成**：`wav_build_header()` 根据数据大小、采样率、声道数、位深度生成标准 44 字节 WAV 头
- **路径安全**：禁止 `..` 路径穿越，强制 `.wav` 扩展名

### 12.4 与 cap_voice 和 cap_asr 的对比

| 维度 | cap_voice | cap_asr | lua_module_audio |
|------|-----------|---------|------------------|
| **调用方式** | LLM 工具调用 / Console / Event Router | LLM 工具调用 / Console / Event Router | Lua 脚本 (`require("audio")`) |
| **方向** | 仅输出（TTS → 扬声器） | 仅输入（MIC → ASR 文本） | 双向（录制 + 播放） |
| **音频设备** | DAC (ES8311, 48kHz stereo) | ADC (ES7210, 16kHz mono) | 按需（输入或输出） |
| **音频来源/去向** | 远程 TTS 服务器（WebSocket + Opus） | 麦克风 → WebSocket PCM → 云端 ASR | 本地文件（WAV）或实时生成（Tone） |
| **编解码** | Opus 解码（`esp_opus_dec`） | 无编解码（PCM 直通） | 无编解码（PCM 直通） |
| **频谱分析** | 不支持 | 不支持 | 支持（FFT + 对数频带） |
| **并发模型** | 多任务流水线（WebSocket → Queue → voice_proc → RingBuf → playback_task） | 单采集任务（asr_capture_task） | 同步阻塞（在 Lua 调用线程中执行） |
| **资源共享** | 每次调用 open/close DAC，通过 voice_ws_common 共享 WS | 每次调用 open/close ADC，通过 voice_ws_common 共享 WS | 通过 Lua GC 管理句柄生命周期 |
| **配置方式** | Kconfig（服务器 IP/端口） | Kconfig（服务器 IP/端口 + 录制参数） | 运行时参数（采样率/声道/位深） |
| **超时** | 45s | max_duration_ms + 20s | 由调用者控制 |
| **WebSocket** | 通过 voice_ws_common（tts_end, tts_start, error, 二进制 Opus） | 通过 voice_ws_common（asr_result, error, 二进制 PCM） | 无 |

### 12.5 关键常量

| 常量 | 值 | 说明 |
|------|-----|------|
| `AUDIO_CHUNK_BYTES` | 512 | I/O 读写块大小 |
| `AUDIO_DEFAULT_VOL` | 80 | 默认输出音量 |
| `AUDIO_DEFAULT_GAIN_DB` | 10.0 | 默认输入增益 (dB) |
| `AUDIO_SPECTRUM_MIN_FFT` | 64 | 最小 FFT 点数 |
| `AUDIO_SPECTRUM_MAX_FFT` | 4096 | 最大 FFT 点数 |
| `AUDIO_SPECTRUM_DEF_FFT` | 512 | 默认 FFT 点数 |
| `AUDIO_SPECTRUM_DEF_BANDS` | 16 | 默认输出频带数 |
| `AUDIO_SPECTRUM_DB_MIN` | -90.0 | 频谱 dB 下限 |
| `AUDIO_SPECTRUM_DB_MAX` | -20.0 | 频谱 dB 上限 |

---

## 13. 性能与安全考虑

### 13.1 内存使用

#### cap_voice 内存

| 分配 | 大小 | 位置 |
|------|------|------|
| PCM 环形缓冲区 | 384 KB (`192000 × 2 bytes`) | SPIRAM (PSRAM) |
| Opus 解码缓冲区 | 8 KB | Heap |
| 重采样缓冲（动态） | 每帧约 3.9 KB（最大） | SPIRAM (PSRAM) |
| Opus 帧队列 | 256 项（每项含 opus_frame_t 结构体） | Heap（FreeRTOS Queue） |
| WebSocket 缓冲区 | 16 KB | Heap |
| WebSocket 任务栈 | 12 KB | Heap |
| 语音处理任务栈 | 8 KB | Heap |
| 播放任务栈 | 3 KB | Heap |

> **注意：** PCM 缓冲区约 384KB 是主要内存开销。若无 SPIRAM，播放任务初始化可能因内存不足失败。同时 **8KB 的语音处理任务栈** 也占用内部 RAM。

#### cap_asr 内存

| 分配 | 大小 | 位置 |
|------|------|------|
| 采集缓冲区 (stereo) | 1280 bytes | Heap（malloc） |
| 采集缓冲区 (mono) | 640 bytes | Heap（malloc） |
| 采集任务栈 | 4 KB | Heap |
| WebSocket 缓冲区 | 16 KB | （与 cap_voice 共享 voice_ws_common） |

> **注意：** ASR 内存开销极小（< 30KB），无 SPIRAM 依赖。不保留持久环形缓冲区，PCM 数据直接写入 WebSocket。

#### voice_ws_common 内存

| 分配 | 大小 | 位置 |
|------|------|------|
| WebSocket 任务栈 | 12 KB | Heap |
| WebSocket 缓冲区 | 16 KB | Heap |
| 回调注册表 | ~100 bytes | BSS |

#### lua_module_audio 内存

| 分配 | 大小 | 位置 |
|------|------|------|
| FFT 窗函数缓冲 | `fft_size × 4 bytes` | Heap（动态分配，函数返回前释放） |
| FFT 复数缓冲 | `fft_size × 2 × 4 bytes` | Heap（动态分配，函数返回前释放） |
| PCM 采集缓冲 | `fft_size × channels × bytes_per_sample` | Heap（动态分配，函数返回前释放） |
| I/O 块缓冲 | 512 bytes | 栈（函数内局部变量，或 malloc 分配） |
| WAV 播放缓冲 | 512 bytes | Heap（malloc，函数返回前释放） |

> **注意：** lua_module_audio 的所有大块内存均在函数调用内部分配并在返回前释放，不存在持久化内存占用。FFT 初始化（`dsps_fft2r_init_fc32`）为全局一次性分配，大小取决于最大 FFT 点数。

### 13.2 网络带宽

```
TTS Opus 下行: ~2-4 KB/s（压缩比约 8:1 到 16:1）
ASR PCM 上行: 16000Hz × 2 bytes × 1 channel = 32 KB/s = 256 Kbps
```

ASR 上行带宽约 32 KB/s，在局域网 Wi-Fi 环境下完全可承受。

### 13.3 并发模型

**cap_voice 多阶段生产者-消费者流水线：**

```
                WebSocket 回调 (优先级较高上下文)
                ↓ (入队)
Stage 1:    [Opus 帧队列]  ← 队列深度 256
                ↓ (出队)
Stage 2:    [voice_proc 任务] (优先级 6, 栈 8KB)
                ↓ (推入)
Stage 3:    [PCM 环形缓冲区] ← 384KB SPIRAM
                ↓ (取出)
Stage 4:    [playback_task] (优先级 5, 栈 3KB)
                ↓ (写入)
Stage 5:    [esp_codec_dev] → [DAC]
```

| 阶段 | 生产者 | 消费者 | 同步机制 |
|------|--------|--------|----------|
| 1→2 | WebSocket 回调 | `voice_proc` 任务 | FreeRTOS Queue (`xQueueSend` / `xQueueReceive`) |
| 2→3 | `voice_proc` 任务 | 环形缓冲区 | 互斥锁 + `while` 等待（反压） |
| 3→4 | `voice_proc` 任务 | `playback_task` | 互斥锁 + 轮询 |

**lua_module_audio 同步模型：**

lua_module_audio 采用**同步阻塞**模型，所有 API 调用在 Lua 调用线程中同步执行。录制/播放操作按块大小轮询 `esp_codec_dev_read` / `esp_codec_dev_write`，单次调用内完成全部 I/O。由于不涉及多任务或中断上下文，无需互斥锁或队列机制。

### 13.4 安全设计

| 方面 | 策略 |
|------|------|
| 缓冲区溢出 | 反压等待（带 2 秒超时），不再静默丢弃 |
| 重入保护 | 每个 TTS/ASR 调用通过 `s_tts_active`/`s_asr_active` 标记确保串行执行 |
| 超时保护 | 所有 I/O 操作均有超时限制（连接 10s、发送 3s、TTS 45s、推送 2s、ASR 结果 15s） |
| WebSocket 重连 | 启用 `disable_auto_reconnect = false`，自动重连（间隔 5s） |
| Keep-Alive | 启用 TCP Keep-Alive（空闲 30s, 间隔 10s, 3 次探测）+ Ping 帧 15s |
| 输入验证 | 严格的 JSON Schema 校验（`text` 必填、类型检查） |
| 看门狗保护 | `voice_proc` 任务采用批量处理（≤10帧）+ 让出 CPU，避免 Task WDT 触发 |
| 资源泄露防护 | `voice_cleanup()`/`asr_cleanup()` 确保所有资源在调用完成后释放 |
| Ogg 容器头保护 | 跳过 `OpusHead`/`OpusTags` 非音频帧 |
| 帧大小验证 | 拒绝 <4 字节或 >65536 字节的二进制帧 |
| WebSocket 帧类型检查 | 仅处理 `op_code==0x02` 二进制帧（音频）和 `op_code==0x01` 文本帧（控制） |
| Lua 路径穿越防护 | `audio_path_valid()` 拒绝含 `..` 的路径，强制 `.wav` 扩展名 |
| Lua 句柄生命周期 | `__gc` 元方法 + 显式 `close()`，确保 Codec 设备正确关闭 |
| FFT 参数校验 | `fft_size` 必须是 2 的幂且在 64-4096 范围内 |
| 文本帧长度保护 | 仅处理 `<1024` 字节的文本帧，防止超大 JSON 内存分配 |

---

## 14. 依赖组件关系图

```
语音模块 (cap_voice + cap_asr + voice_ws_common + lua_module_audio)
    │
    ├── voice_ws_common (共享 WebSocket)
    │   ├── esp_websocket_client ── WebSocket 客户端
    │   │                           连接管理 / Keep-Alive / Ping / 自动重连
    │   ├── json (cJSON) ────────── JSON 解析
    │   │                           文本帧 type 字段提取
    │   └── FreeRTOS ────────────── Semaphore (连接同步)
    │
    ├── cap_voice (TTS)
    │   ├── claw_cap ──────────── Capability 框架核心
    │   │                          描述符注册、Group 管理、调用路由
    │   ├── voice_ws_common ───── 共享 WebSocket (替代直接 esp_websocket_client)
    │   ├── cJSON ─────────────── JSON 解析与序列化
    │   │                          请求构造、响应解析
    │   ├── esp_audio_codec ────── Opus 音频解码器
    │   │   └── esp_opus_dec        Opus 解码注册与解码
    │   ├── esp_board_manager ──── 板级设备管理器
    │   │                          获取板载 DAC 设备句柄
    │   ├── esp_codec_dev ──────── 音频编解码设备抽象
    │   │                          配置、打开、写入音视频数据
    │   ├── FreeRTOS ──────────── RTOS 基础
    │   │    ├── Task              播放任务、语音处理任务
    │   │    ├── Queue              Opus 帧队列
    │   │    ├── Semaphore          TTS 完成同步
    │   │    └── Mutex              环形缓冲区保护
    │   └── heap_caps ──────────── 堆内存分配器
    │                                SPIRAM 分配（环形 + 重采样缓冲）
    │
    ├── cap_asr (ASR)
    │   ├── claw_cap ──────────── Capability 框架核心
    │   │                          描述符注册、Group 管理、调用路由
    │   ├── voice_ws_common ───── 共享 WebSocket
    │   │                          PCM 二进制帧发送、ASR 文本帧接收
    │   ├── cJSON ─────────────── JSON 解析
    │   │                          输入参数解析、ASR 结果解析
    │   ├── esp_board_manager ──── 板级设备管理器
    │   │                          获取板载 ADC/DAC 设备句柄
    │   ├── esp_codec_dev ──────── 音频编解码设备抽象
    │   │                          ADC 读取 (I2S RX) + DAC MIC Bias
    │   └── FreeRTOS ──────────── RTOS 基础
    │        ├── Task              asr_capture 采集任务
    │        └── Semaphore          ASR 完成同步
    │
    └── lua_module_audio (通用音频 I/O)
        ├── cap_lua ────────────── Lua Capability 框架
        │                          模块注册 (cap_lua_register_module)
        ├── esp_codec_dev ──────── 音频编解码设备抽象
        │                          输入/输出设备的读写操作
        ├── esp_board_manager ──── 板级设备管理器
        │                          Lua 层通过 board_manager 模块获取设备句柄
        ├── esp-dsp ────────────── ESP 数字信号处理库
        │    ├── dsps_fft2r_init_fc32   FFT 初始化
        │    ├── dsps_fft2r_fc32        复数 FFT 运算
        │    ├── dsps_bit_rev_fc32      位反转重排
        │    └── dsps_wind_hann_f32     Hann 窗函数
        └── Lua C API ──────────── Lua 5.3+ C API
                                    元表、GC、错误处理
```

---

## 15. 配置项 (Kconfig)

### 15.1 Capability 配置

| 配置宏 | 说明 | 默认值 |
|--------|------|--------|
| `CONFIG_APP_CLAW_CAP_VOICE` | 启用语音 TTS Capability | `y`（当 `ESP_BOARD_DEV_AUDIO_CODEC_SUPPORT` 启用时） |
| `CONFIG_APP_CLAW_CAP_ASR` | 启用语音 ASR Capability | `y`（当 `ESP_BOARD_DEV_AUDIO_CODEC_SUPPORT` 启用时） |
| `CONFIG_APP_CLAW_VOICE_SERVER_IP` | 语音 WebSocket 服务器 IP 地址（TTS & ASR 共享） | `"10.21.126.59"` |
| `CONFIG_APP_CLAW_VOICE_SERVER_PORT` | 语音 WebSocket 服务器端口（TTS & ASR 共享） | `3000` |
| `CONFIG_APP_CLAW_ASR_MAX_DURATION_MS` | ASR 默认最大录制时长 (ms) | `10000` (范围 1000-30000) |
| `CONFIG_APP_CLAW_ASR_SILENCE_TIMEOUT_MS` | ASR 默认静音自动停止超时 (ms) | `1000` (范围 0-5000, 0=禁用) |

### 15.2 Lua 模块配置

| 配置宏 | 说明 | 默认值 |
|--------|------|--------|
| `CONFIG_APP_CLAW_LUA_MODULE_AUDIO` | 启用 Lua 音频模块（`require("audio")`） | `y`（当 `ESP_BOARD_DEV_AUDIO_CODEC_SUPPORT` 启用时） |

---

## 16. 扩展建议

### 16.1 可能的功能扩展

1. **语音识别 (ASR)** ✅ **已实现**：`cap_asr` 组件通过 I2S 麦克风采集 + WebSocket PCM 流式上传 + 云端 ASR 识别，已与 cap_voice 形成完整语音对话闭环
2. **语音唤醒**：集成 ESP-SR 等本地唤醒词引擎，作为 `EVENT_SOURCE` Capability，实现免按钮语音激活
3. **PTT 按键触发**：为 ASR 添加物理按键支持（参考 voice-changer 项目的 `button_mon` 任务），作为 `start_asr` 能力的补充触发方式
4. **流式 TTS 进度回调**：当前实现在 TTS 完成前已可播放音频；可进一步通过 `tts_progress` 事件通知调用者
5. **多语音服务器支持**：扩展为支持多个备选服务器地址，自动故障转移
6. **音频缓存**：对常用短句的 TTS 结果进行本地缓存，减少 WebSocket 通信
7. **频谱可视化**：将 `lua_module_audio.read_spectrum` 的频带数据与 LED 灯条或 LCD 显示联动，实现音乐可视化
8. **ASR 基于能量的 VAD**：当前使用简单的全零检测作为静音判断，可升级为基于能量阈值或 ESP-SR VAD 引擎的语音活动检测

### 16.2 当前架构的局限性

1. **无生命周期钩子**：WebSocket 连接在首次 `get_tts` 或 `start_asr` 调用时按需建立，无长连接预热机制，首次调用有额外延迟
2. **单线程处理**：TTS/ASR 请求的同步等待阻塞了调用线程，可能影响其他 Capability 的并发调用
3. **无音量控制参数**：当前 TTS 音量硬编码为 80%，用户无法通过 `get_tts` 工具参数调节（Lua 层可通过 `audio.set_volume` 控制）
4. **ASR 与 TTS 串行限制**：由于 I2S 总线共享，ASR 录制和 TTS 播放不能同时进行（但语音对话场景本身是串行的，影响有限）
5. **共享 Codec 的设计耦合**：由于音频 Codec 是系统级共享设备，每次调用都要重新打开并配置采样率，增加了延迟；资源清理必须确保非 graceful 路径也能正确释放 Codec，否则视频播放器将无法使用音频
6. **lua_module_audio 同步阻塞**：录制和播放操作在 Lua 调用线程中同步执行，长时间录制会阻塞 Lua 脚本执行
7. **ASR 无流式中间结果回调**：当前只返回最终 (`is_final=true`) 的 ASR 结果，中间结果仅更新内部缓冲区但不通知 LLM（可扩展为 `asr_partial_result` 事件）
8. **静音检测过于简单**：当前使用全零检测，可能对低噪声环境过于敏感；更准确的 VAD 需要基于能量或机器学习

---

## 17. 总结

语音模块由三个互补的组件构成，分别服务于不同的使用场景：

### voice_ws_common（共享 WebSocket 组件）

`voice_ws_common` 是从 `cap_voice` 中提取的 WebSocket 连接管理层，为所有语音能力提供统一的网络通道。关键设计特点：

1. **单连接共享**：语音服务器只接受单一 ESP32 连接，所有语音能力通过此组件共享连接
2. **多订阅者回调**：文本帧按 `type` 字段路由，二进制帧和状态变化广播给所有注册的回调（最多 4 个/类型）
3. **透明迁移**：cap_voice 从直接使用 `esp_websocket_client` 迁移到 `voice_ws_common` API，对外接口不变
4. **懒连接**：连接在首次语音能力调用时按需建立，后续复用

### cap_voice（TTS Capability）

`cap_voice` 是一个典型的 **CALLABLE Capability** 实现，展示了 AIRI 系统中 Capability 组件的标准架构模式。关键设计特点：

1. **解耦 WebSocket 接收与解码**：通过 Opus 帧队列将音频处理卸载到独立任务，大幅降低 WebSocket 回调的执行时间
2. **批量处理与 WDT 防护**：`voice_proc` 任务每批最多处理 10 帧后让出 CPU，确保不触发任务看门狗
3. **资源安全清理**：`voice_cleanup()` 函数确保音频 Codec、任务、队列、内存等所有资源在调用结束后释放，支持与视频播放器共享音频设备
4. **反压写入**：`push_pcm_async` 从静默丢弃改为带超时反压，提高音频播放完整性
5. **Ogg 容器头过滤**：跳过 `OpusHead`/`OpusTags` 等非音频元数据包
6. **扩展超时保护**：TTS 超时从 30s 延长至 45s
7. **通过 voice_ws_common 通信**：注册 `tts_text_handler`、`tts_binary_handler`、`tts_state_handler` 回调

### cap_asr（ASR Capability）

`cap_asr` 是语音识别能力组件，与 cap_voice 互补形成完整的语音对话闭环。关键设计特点：

1. **I2S 麦克风采集**：通过 `esp_codec_dev` 从 ES7210 ADC 读取 16kHz stereo 数据，软件 de-interleave 提取 mono PCM
2. **DAC 偏置机制**：共享编解码器板型先开 DAC 提供 MIC Bias，再开 ADC；独立 ADC 板型自动跳过此步骤
3. **PCM 流式上传**：640B（20ms）块通过 `voice_ws_send_binary` 发送到语音服务器
4. **静音自动停止**：连续 50 个全零块（1 秒）自动结束录制，支持可配置的 `silence_timeout_ms`
5. **最大时长保护**：默认 10 秒最大录制时长（可配置 1-30 秒）
6. **专用采集任务**：`asr_capture` 任务（优先级 5，栈 4096）独立运行，不阻塞调用者
7. **通过 voice_ws_common 通信**：注册 `asr_text_handler`、`asr_state_handler` 回调，与 cap_voice 共享同一 WebSocket 连接
8. **资源安全清理**：`asr_cleanup()` 确保 ADC 设备在调用结束后关闭，释放共享硬件
9. **LLM 工具调用**：`start_asr` 能力可通过 LLM 工具调用触发，返回识别文本

### lua_module_audio（通用音频 Lua 模块）

`lua_module_audio` 为 Lua 脚本提供了完整的音频 I/O 能力，是 ESP-Claw 平台上语音录制、音效生成、频谱分析的核心组件。关键设计特点：

1. **双向音频流**：同时支持输入（录制/电平检测/频谱分析）和输出（WAV 播放/音调生成/回路监听）
2. **FFT 频谱分析**：集成 ESP-DSP，支持可配置的 FFT 点数和对数频带划分，适用于音乐可视化
3. **安全的 Lua 集成**：通过元表 `__gc` 实现自动资源回收，路径穿越防护，严格的参数校验
4. **WAV 全支持**：内置 PCM WAV 解析器和生成器，兼容含扩展元数据的 WAV 文件
5. **板级抽象**：通过 `board_manager` Lua 模块获取设备句柄，不硬编码硬件参数

### 语音对话闭环

cap_asr（输入）和 cap_voice（输出）通过 voice_ws_common 共享 WebSocket，构成完整的语音交互流水线：

```
用户语音 → [cap_asr: start_asr] → 云端 ASR → 识别文本
  → LLM 推理 → [cap_voice: get_tts] → 云端 TTS → 扬声器播放
  → 用户听到回复
```

整体架构遵循以下设计原则：
- **组件化**：独立的目录结构、构建配置、头文件/实现分离
- **框架集成**：cap_voice 和 cap_asr 通过 `claw_cap_descriptor_t` 和 `claw_cap_group_t` 注册到核心系统；lua_module_audio 通过 `cap_lua_register_module` 注册到 Lua 运行时
- **共享基础设施**：`voice_ws_common` 作为 WebSocket 共享层，避免连接冲突
- **异步 I/O**：采用 WebSocket 异步通信 + FreeRTOS 多任务流水线
- **硬件抽象**：通过 `esp_board_manager` 和 `esp_codec_dev` 实现设备无关性
- **Skill 配套**：cap_voice 和 cap_asr 均提供 LLM 使用的 Skill 文档，遵循渐进式披露原则
- **平台无关**：依赖板级管理器获取设备句柄，可在不同 ESP 开发板上复用
- **多层 API**：LLM 层通过 Capability 工具调用 TTS/ASR，脚本层通过 Lua 模块进行精细音频控制
