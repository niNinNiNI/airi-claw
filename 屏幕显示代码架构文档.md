# ESP-Claw Edge Agent 屏幕显示代码架构

## 概述

本项目（Edge Agent）的屏幕显示部分**不包含本地 LCD 驱动代码**。ESP32-P4 开发板上的屏幕显示（触摸 LCD）由 **ESP Board Manager** 和 **LVGL** 等 managed 组件驱动。**Edge Agent 自身不直接操作屏幕硬件**，而是作为一个**嵌入式 Web 服务器**，向外提供一套**基于 SolidJS 的 Web 端管理界面**。用户通过浏览器访问设备 IP 即可看到完整的 GUI。

## 技术栈

| 层级 | 技术 |
|------|------|
| **前端框架** | SolidJS（响应式 UI，替代 React） |
| **CSS** | TailwindCSS v4 自定义主题 |
| **构建工具** | Vite |
| **图标** | lucide-solid |
| **国际化** | 自建 i18n（支持 en / zh-cn） |
| **HTTP 服务器** | ESP-IDF HTTP Server（`components/http_server/`） |
| **存储层** | FATFS 文件系统（`fatfs_image/`） |

## 项目文件结构

```
components/http_server/frontend_source/
├── index.html                      # 入口 HTML
├── package.json                    # 依赖声明
├── vite.config.ts                  # 构建配置
├── tsconfig.json                   # TypeScript 配置
├── src/
│   ├── index.tsx                   # 应用入口
│   ├── index.css                   # 全局样式 / Tailwind CSS 主题
│   ├── App.tsx                     # 根组件，路由控制
│   ├── api/
│   │   └── client.ts               # HTTP API 客户端
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Layout.tsx          # 主布局（侧边栏 + 顶栏 + 内容区）
│   │   │   ├── Sidebar.tsx         # 导航侧边栏
│   │   │   ├── StatusBar.tsx       # 顶部状态栏（Logo + 连接状态）
│   │   │   ├── TabShell.tsx        # 页面容器壳
│   │   │   └── LanguageSwitcher.tsx# 语言切换
│   │   ├── system/
│   │   │   ├── RestartOverlay.tsx  # 重启遮罩层
│   │   │   └── RestartConfirmModal.tsx # 重启确认弹窗
│   │   └── ui/
│   │       ├── Banner.tsx          # 通知横幅
│   │       ├── Button.tsx          # 按钮组件
│   │       ├── ConfigBlocks.tsx    # 配置区块（Static / Collapsible）
│   │       ├── ConfigTable.tsx     # 配置表格
│   │       ├── FormField.tsx       # 表单字段（TextInput, SelectInput）
│   │       ├── Modal.tsx           # 模态对话框
│   │       ├── PageHeader.tsx      # 页面标题栏
│   │       ├── SavePanel.tsx       # 保存操作面板
│   │       ├── Section.tsx         # 区域分割
│   │       ├── Switch.tsx          # 开关控件
│   │       └── ToastViewport.tsx   # Toast 消息容器
│   ├── pages/
│   │   ├── StatusPage.tsx          # 状态页（网络 + 存储状态）
│   │   ├── BasicPage.tsx           # 基础设置页（WiFi、AP、时区）
│   │   ├── LlmPage.tsx             # LLM 配置页
│   │   ├── ImPage.tsx              # IM 平台配置页
│   │   ├── SearchPage.tsx          # 搜索引擎配置页
│   │   ├── MemoryPage.tsx          # 内存管理页
│   │   ├── WebImPage.tsx           # Web IM 聊天页（通过 WebSocket）
│   │   ├── CapabilitiesPage.tsx    # 能力配置页
│   │   ├── SkillsPage.tsx          # Lua 脚本/Skills 配置页
│   │   ├── FilesPage.tsx           # 文件管理页
│   │   ├── SetupWizardPage.tsx     # 设置向导页（首次启动用）
│   ├── state/
│   │   ├── config.ts               # 全局状态管理（配置、状态）
│   │   ├── configTab.ts            # Tab 级状态管理
│   │   ├── dirty.ts                # 未保存状态跟踪
│   │   └── toast.ts                # Toast 消息状态
│   └── i18n/
│       ├── index.ts                # 国际化核心
│       ├── en.ts                   # 英文翻译
│       └── zh-cn.ts                # 中文翻译
```

## 核心显示模块详解

### 1. 页面布局（App.tsx + Layout.tsx + Sidebar.tsx）

`App.tsx` 是根组件，使用 Hash 路由控制页面切换，支持懒加载：

- **启动流程**：调用 `bootstrap()` 并行获取设备状态、能力列表、Lua 模块列表
- **错误处理**：启动失败显示 Banner 错误提示
- **重启流程**：`handleRestartRequest()` 发起重启请求 → 轮询等待设备恢复 → 自动刷新

`Layout.tsx` 实现**三栏式布局**：
- **侧边栏（Sidebar）**：可折叠，移动端为抽屉样式，含 10 个导航节点
- **顶栏（StatusBar）**：Logo + 在线状态指示器 + 语言切换
- **主内容区**：`max-w-5xl` 居中，响应式 padding

侧边栏导航结构：
```
Status（状态）
└── System Settings（系统设置，可折叠组）
    ├── Basic（基础设置：WiFi/AP/时区）
    ├── LLM（大语言模型）
    ├── IM（即时通讯平台）
    └── Search（搜索引擎）
Memory（记忆/知识库）
Web IM（即时聊天）
Capabilities（能力）
Skills（Lua 模块/Skills）
Files（文件管理）
```

### 2. 状态页（StatusPage.tsx）

显示设备运行状态：
- **网络状态**：WiFi 连接状态、IP 地址、WiFi 模式、AP SSID、AP IP
- **存储状态**：存储路径
- 提供"刷新"和"重启"按钮

### 3. 基础设置页（BasicPage.tsx）

配置 WiFi 网络和系统时间：
- WiFi SSID / 密码
- AP 名称 / 密码 / 行为模式
- 时区（POSIX TZ 格式，含中文英文双语文档提示）
- 使用 `configTab` 状态管理实现脏检测、保存/丢弃

### 4. Web IM 聊天页（WebImPage.tsx）

实时聊天接口，通过 **WebSocket** 与设备通信：
- 自动生成唯一 Chat ID（localStorage 持久化）
- WebSocket 心跳保活（15 秒间隔）
- 断线自动重连（2 秒延迟）
- 支持文件附件上传（图片 → `/inbox/webim/`）
- 消息气泡布局（用户右对齐，AI 左对齐）
- 支持 Ctrl+Enter 快捷发送
- 在线状态指示灯

### 5. 设置向导页（SetupWizardPage.tsx）

首次启动时的多步引导配置：
- **Step 0 LLM 配置**：预设 6 个 LLM 提供商（OpenAI / Bailian / DeepSeek / Anthropic / OpenAI Compatible / Anthropic Compatible），一键填充默认值
- **Step 1 IM 平台**：可增删 4 个 IM 平台（微信 / 飞书 / QQ / Telegram），微信支持二维码扫码登录
- **Step 2 搜索引擎**：配置 Tavily 搜索 API Key
- **Step 3 完成**：重启设备

微信集成为亮点功能：通过 `lean-qr` 库生成二维码，轮询扫码状态，完成自动取 Token。

### 6. CSS 主题设计（index.css）

使用 **TailwindCSS v4 的 @theme** 定义深色主题：

| 令牌 | 值 | 用途 |
|------|-----|------|
| `--color-bg-base` | `#0a0b0e` | 页面背景 |
| `--color-bg-surface` | `#111318` | 表面背景 |
| `--color-bg-card` | `#161920` | 卡片背景 |
| `--color-accent` | `#e8362d` | 品牌色（红色） |
| `--color-green` | `#68d391` | 在线/成功状态 |
| `--color-orange` | `#f6ad55` | 未保存警告 |
| `--color-danger` | `#f87171` | 危险操作 |

- 自定义滚动条（6px 宽，半透明滑块）
- 脉冲动画用于在线状态指示灯
- 输入框聚焦时显示品牌色边框

## 关键设计模式

1. **响应式布局**：侧边栏 Desktop 固定显示 / Mobile 抽屉菜单，内容区 `max-w-5xl` 居中
2. **状态管理**：全局状态（`state/config.ts`） + 页面级状态（`state/configTab.ts`）+ 脏检测（`state/dirty.ts`）
3. **国际化**：运行时切换 en/zh-cn，所有页面文本通过 `t()` 函数获取
4. **懒加载**：`lazy()` 分割 10 个页面，首屏仅加载当前页面
5. **Form 数据流**：Form → Dirty 检测 → Save API → 本地 Patch → Restart
6. **未保存提示**：Hash 路由切换时检查 `anyDirty()`，确认后丢弃

## 设备端集成

构建时 Vite 将前端打包为 `index.html.gz`，保存在 `build/` 中。ESP 的 HTTP 服务器在启动时解压并提供 `/` 路径的静态文件服务（对应 `build/index.html.gz.S` 等）。所有 API 请求（GET / POST / WebSocket）由 `api/client.ts` 统一封装，通过 `fetch()` 与设备通信。
