# 情绪视频切换系统 — 代码架构文档

## 概述

情绪视频切换系统允许 爱莉 角色在运行时根据 AI 对话语境动态切换屏幕上的角色动画视频。系统支持 10 种预设情绪（对应 10 个 MJPEG 视频文件），通过 HTTP API、Lua 脚本和 LLM Function Calling 三种途径触发切换。

核心设计目标：
- **命令驱动**：所有切换操作通过 FreeRTOS 队列异步投递，避免多生产者竞态
- **无缝过渡**：利用 PPA 硬件 Alpha 混合实现淡入淡出，掩盖文件切换延迟
- **与现有显示系统共存**：通过 display_arbiter 仲裁机制与 Lua 脚本绘图分享 LCD 所有权
- **AI 原生集成**：情绪能力作为 LLM 可见工具注册，LLM 可根据对话内容自主决策切换时机

---

## 一、整体架构

```
┌──────────────────────────────────────────────────────────────┐
│                    外部触发源（多生产者）                       │
│                                                              │
│  HTTP POST           Lua 脚本             LLM Function       │
│  /api/emotion        set_emotion()        Calling            │
│  {emotion: "calm"}                      "set_emotion"       │
└───────┬──────────────────┬──────────────────────┬───────────┘
        │                  │                      │
        ▼                  ▼                      ▼
┌──────────────────────────────────────────────────────────────┐
│                 emotion_video_controller（新增模块）           │
│                                                              │
│  ┌─────────────────┐  ┌────────────────┐  ┌───────────────┐ │
│  │ 情绪状态管理      │  │ 映射表          │  │ 切换逻辑       │ │
│  │ current_emotion  │  │ EMOTION_ → 文件 │  │ 停止→启动     │ │
│  │ is_playing       │  │ 10 个枚举 → 路径│  │ 过渡效果      │ │
│  └────────┬────────┘  └────────┬───────┘  └───────┬───────┘ │
│           │                    │                   │         │
│           └────────────────────┼───────────────────┘         │
│                                ▼                             │
│              emotion_video_set(emotion_video_t)               │
└────────────────────────────┬─────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│                    video_player（已实现层）                    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              s_video_cmd_queue (FreeRTOS Queue × 4)  │    │
│  │         VIDEO_CMD_SWITCH_FILE | VIDEO_CMD_STOP       │    │
│  └──────────────────────┬──────────────────────────────┘    │
│                         │                                    │
│                         ▼                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │           video_task（优先级 6，单消费者）              │    │
│  │                                                     │    │
│  │  等待命令 → acquire(VIDEO) → play_single_file()     │    │
│  │  → 播放循环(50ms 轮询) → EOS/切换/抢占检测           │    │
│  └──────────────────────┬──────────────────────────────┘    │
│                         │                                    │
│          ┌──────────────┼──────────────┐                    │
│          ▼              ▼              ▼                    │
│   app_stream_adapter  display_arbiter  ppa_blend            │
│   (MJPEG 解码管线)     (屏幕所有权)     (过渡效果)            │
└──────────────────────────────────────────────────────────────┘
```

### 架构分层

| 层次 | 模块 | 职责 |
|------|------|------|
| **触发层** | HTTP API / Lua 绑定 / LLM tools | 接收外部情绪切换请求，参数校验，转发到控制层 |
| **控制层** | `emotion_video_controller` | 情绪状态管理、情绪→文件映射、切换编排、过渡协调 |
| **执行层** | `video_player` | 命令队列、播放状态机、display_arbiter 交互、帧输出 |
| **硬件加速层** | `ppa_blend` / `app_stream_adapter` / `app_extractor` | PPA Alpha 混合、MJPEG 解码、文件解封装 |

---

## 二、情绪枚举与文件映射

### 2.1 情绪定义

```c
typedef enum {
    EMOTION_IDLE_SHAKE   = 0,  // 待机摇晃
    EMOTION_SWAY         = 1,  // 摇摆
    EMOTION_CALM         = 2,  // 平静
    EMOTION_WAVE         = 3,  // 挥手
    EMOTION_TIRED        = 4,  // 疲倦
    EMOTION_SLIGHT_SWAY  = 5,  // 轻微摇摆
    EMOTION_BREATHING    = 6,  // 呼吸
    EMOTION_HAND_MOVES   = 7,  // 手部动作
    EMOTION_NOD          = 8,  // 点头
    EMOTION_RELAXED      = 9,  // 放松
    EMOTION_COUNT        = 10,
} emotion_video_t;
```

### 2.2 映射表

映射表在编译期定义，运行时初始化时扫描 SD 卡验证文件存在性：

| 枚举 | 文件路径 | 角色动画描述 |
|------|---------|------------|
| `EMOTION_IDLE_SHAKE` | `/sdcard/videos/m01_idle_shake.mp4` | 待机时轻微摇晃 |
| `EMOTION_SWAY` | `/sdcard/videos/m02_sway.mp4` | 身体左右摇摆 |
| `EMOTION_CALM` | `/sdcard/videos/m03_calm.mp4` | 平静注视 |
| `EMOTION_WAVE` | `/sdcard/videos/m04_wave.mp4` | 举手挥手 |
| `EMOTION_TIRED` | `/sdcard/videos/m05_tired.mp4` | 疲倦打哈欠 |
| `EMOTION_SLIGHT_SWAY` | `/sdcard/videos/m06_slight_sway.mp4` | 轻微摇摆 |
| `EMOTION_BREATHING` | `/sdcard/videos/m07_breathing.mp4` | 自然呼吸 |
| `EMOTION_HAND_MOVES` | `/sdcard/videos/m08_hand_moves.mp4` | 手部动作 |
| `EMOTION_NOD` | `/sdcard/videos/m09_nod.mp4` | 点头 |
| `EMOTION_RELAXED` | `/sdcard/videos/m10_relaxed.mp4` | 放松姿态 |

视频文件规格：MJPEG 编码，RGB565 色彩，480×800 分辨率，15fps，存放于 SD 卡 `/sdcard/videos/` 目录。

---

## 三、核心组件详解

### 3.1 emotion_video_controller（控制层）

**新文件**: `components/video_player/emotion_video_controller.c` / `.h`

这是情绪系统的核心控制模块，负责将"情绪"这个高层抽象映射为底层的"播放指定视频文件"操作。

#### 职责

1. **状态管理**：维护当前情绪枚举值 `s_current_emotion` 和播放状态 `s_is_playing`
2. **映射解析**：将 `emotion_video_t` 枚举值查找为完整的 SD 卡文件路径
3. **切换编排**：停止当前播放 → (可选的 PPA 过渡) → 启动新情绪视频
4. **生命周期**：初始化时扫描视频目录验证文件，停止时释放资源

#### 公共 API

```c
// 初始化：扫描视频目录，建立映射表，预验证文件存在
esp_err_t emotion_video_controller_init(const char *video_dir);

// 切换到指定情绪（内部调用 video_player_switch_to）
esp_err_t emotion_video_set(emotion_video_t emotion);

// 获取当前情绪枚举值
emotion_video_t emotion_video_get_current(void);

// 停止播放并释放 display_arbiter 所有权
esp_err_t emotion_video_stop(void);
```

#### 内部状态

```
                    ┌─────────────┐
      初始化         │  UNINITED   │
    ───────────────▶ │             │
                    └──────┬──────┘
                           │ emotion_video_controller_init()
                           │ 扫描 /sdcard/videos/
                           │ 建立映射表
                           ▼
                    ┌─────────────┐
        set()       │   READY     │◀──────────┐
    ───────────────▶│  (已初始化)  │           │
                    └──────┬──────┘           │
                           │                   │
                           │ emotion_video_set()│
                           ▼                   │
                    ┌─────────────┐           │
                    │  PLAYING    │───────────┘
                    │  (播放中)    │  stop() / set()
                    └─────────────┘
```

### 3.2 video_player（执行层 — 已实现）

**文件**: [video_player.c](components/video_player/video_player.c) / [video_player.h](components/video_player/include/video_player.h)

`video_player` 是视频播放的执行引擎，采用**命令队列 + 单消费者状态机**架构。

#### 命令队列机制

所有播放操作（切换文件、停止）通过 FreeRTOS 队列投递，由 `video_task` 串行消费：

```c
// 命令类型（video_player.c:68-69）
typedef enum {
    VIDEO_CMD_SWITCH_FILE,   // 切换到指定文件播放
    VIDEO_CMD_STOP,          // 停止播放
} video_cmd_t;

// 命令消息（video_player.c:72-76）
typedef struct {
    video_cmd_t cmd;
    char path[MAX_FILENAME_LEN];   // 文件完整路径
    bool loop;                     // 是否循环播放
} video_cmd_msg_t;

#define VIDEO_CMD_QUEUE_LEN  4     // 队列深度：4 个槽位
```

**设计意图**：多个生产者（HTTP API handler、Lua 绑定、LLM cap）可以安全地向队列投递命令，无需额外的互斥锁。`video_task` 是唯一的消费者，天然串行。

#### video_task 状态机

`video_task`（优先级 6，栈 8KB）是播放中枢，运行一个命令驱动的循环状态机：

```
                          ┌──────────────────────────┐
                          │   等待命令（队列阻塞 100ms）│
                          └──────────┬───────────────┘
                                     │
                          收到 VIDEO_CMD_SWITCH_FILE
                                     │
                                     ▼
                          ┌──────────────────────────┐
                          │ display_arbiter_acquire   │
                          │ (VIDEO) — 轮询等待，       │
                          │ 500ms 间隔重试             │
                          └──────────┬───────────────┘
                                     │
                                     ▼
                          ┌──────────────────────────┐
                          │      播放循环              │
                          │                          │
                          │  play_single_file(path)   │
                          │   ├─ set_file()           │
                          │   └─ start()              │
                          │                          │
                          │  每 50ms 检查:             │
                          │   ├─ 新命令到达? → 切换    │
                          │   ├─ EOS 到达?  → 结束    │
                          │   ├─ 卡死检测(5帧) → 退出  │
                          │   └─ 所有权丢失?→ 重获取   │
                          └──────────┬───────────────┘
                                     │
               ┌─────────────────────┼──────────────────┐
               │                     │                  │
          loop=true             loop=false         被抢占/STOP
               │                     │                  │
               ▼                     ▼                  ▼
          重新播放              释放 VIDEO         stop + 等待
          同一文件              等待新命令         重新 acquire
```

#### 关键播放函数

| 函数 | 用途 |
|------|------|
| `video_player_play_file(path, loop)` | 切换到指定文件播放，`loop=true` 单文件循环 |
| `video_player_switch_to(path)` | `video_player_play_file(path, true)` 的快捷方式 |
| `video_player_play_playlist(paths, count, loop)` | 顺序播放文件列表 |
| `video_player_get_current(buf, len)` | 获取当前播放文件路径 |
| `video_player_stop()` | 停止播放，释放资源 |

#### 播放模式

`video_player` 支持两种播放模式（互斥）：

- **单文件模式**（`s_single_file_mode`）：播放指定文件，支持循环。由 `video_player_play_file()` 或 `video_player_switch_to()` 激活。
- **播放列表模式**（`s_playlist_mode`）：按顺序播放文件列表，支持整体循环。由 `video_player_play_playlist()` 激活。

情绪视频系统使用**单文件循环模式**——每个情绪视频持续循环播放直到被切换到另一个情绪。

#### 帧输出管线

```
SD 卡文件
  │ fopen/fread
  ▼
app_extractor（解封装层）
  │ ── 视频帧: JPEG 压缩数据
  │ ── 音频帧: 入队 → audio_task → codec_dev → 扬声器
  ▼
app_stream_adapter（流适配层）
  │ JPEG 硬件解码 → RGB565
  │ 双缓冲轮转 (2 × 768KB PSRAM)
  ▼
display_decoded_frame()（回调）
  │ display_arbiter_is_owner(VIDEO)? → 是: 继续; 否: 跳过
  │ esp_lcd_panel_draw_bitmap(panel_handle, buffer)
  │ 叠加触摸按钮 overlay
  │ xSemaphoreTake(s_vsync_sem) — 等待 VSync 防撕裂
  ▼
LCD 面板 (480×800 RGB565)
```

### 3.3 ppa_blend（硬件加速过渡层）

**文件**: [ppa_blend.c](components/video_player/ppa_blend.c) / [ppa_blend.h](components/video_player/ppa_blend.h)

利用 ESP32-P4 的 PPA（Pixel Processing Accelerator）硬件引擎实现 RGB565 帧间 Alpha 混合，用于情绪切换时的淡入淡出效果。

#### API

```c
// 初始化 PPA 混合客户端
esp_err_t ppa_blend_init(void);

// 两张 RGB565 帧按 alpha 混合: out = bg * (1-α) + fg * α
esp_err_t ppa_blend_rgb565(const uint8_t *bg_buf, const uint8_t *fg_buf,
                            uint8_t *out_buf, int width, int height, uint8_t alpha);
```

#### 在情绪切换中的使用

```
切换流程:
  1. 捕获当前帧 → bg_buffer (PSRAM)
  2. 启动新视频 → 读取第一帧 → fg_buffer (PSRAM)
  3. for alpha in [0→255] step 约 50:
       ppa_blend_rgb565(bg, fg, out, 480, 800, alpha)
       esp_lcd_panel_draw_bitmap(panel, 0, 0, 480, 800, out)
       vTaskDelay(≈50ms)
  4. 过渡完成 → 正常播放新视频
```

**设计考量**：
- PPA 硬件混合速度极快（< 1ms/帧），过渡耗时主要取决于 VSync 等待
- 过渡可在 200-300ms 内完成（4-5 帧），掩盖文件切换的 200-500ms 解封装初始化延迟
- 复用已有的双缓冲中的一个作为过渡输出缓冲，不增加 PSRAM 压力
- `ppa_blend` 模块代码已就绪但当前未集成到主播放流程，情绪切换系统将首次使用它

### 3.4 HTTP API 端点

**新文件**: `components/http_server/http_server_emotion_api.c`

提供 RESTful 接口供外部系统（Web 管理界面、移动端、MCP Client）控制情绪。

#### 端点定义

```
POST /api/emotion
  Content-Type: application/json
  Body: {"emotion": "calm"}          ← 按名称切换
        {"emotion": 2}               ← 按索引切换

  Response 200: {"emotion": "calm", "index": 2, "playing": true}
  Response 400: {"error": "unknown emotion"}
  Response 503: {"error": "video player not ready"}

GET /api/emotion
  Response 200: {"emotion": "calm", "index": 2, "playing": true}
  Response 200: {"emotion": "none", "index": -1, "playing": false}
```

#### 处理流程

```
HTTP POST /api/emotion
  │
  ├── 1. 解析 JSON body
  ├── 2. 校验 emotion 字段 (名称 或 索引)
  ├── 3. 名→枚举 查找 (遍历映射表)
  ├── 4. 调用 emotion_video_set(enum)
  └── 5. 返回当前状态 JSON
```

该端点注册在现有 HTTP 服务器框架中（[http_server.h](components/http_server/include/http_server.h)），复用已有的 JSON 解析和路由分发机制。

### 3.5 Lua 绑定

**新增 Lua 函数**（注册到 Lua 全局环境）：

```lua
-- 切换到指定情绪
set_emotion("wave")       -- 按名称
set_emotion(3)            -- 按索引

-- 获取当前情绪
local info = get_emotion()
-- 返回: {name = "calm", index = 2, playing = true}
```

Lua 绑定实现在现有 Lua 模块注册机制中，在 `cap_lua` 初始化时通过 `lua_register()` 注入。这使得 Lua 脚本可以通过情绪系统编排复杂的动画序列：

```lua
-- Lua 脚本示例：情绪序列
set_emotion("wave")
os.sleep(2)
set_emotion("nod")
os.sleep(1)
set_emotion("calm")
```

### 3.6 LLM Agent 集成（cap_emotion）

**新能力组**: `cap_emotion`

将情绪切换注册为 LLM 可见的能力工具，使 爱莉 能根据对话语境自主切换角色动画。

#### 能力描述符

```c
{
    .id = "set_emotion",
    .name = "Set Emotion Animation",
    .description = "Switch the character's emotional expression animation. "
                   "Choose the emotion that best matches the current conversational context.",
    .kind = CLAW_CAP_KIND_CALLABLE,
    .cap_flags = CLAW_CAP_FLAG_CALLABLE_BY_LLM,
    .input_schema_json = {
        "type": "object",
        "properties": {
            "emotion": {
                "type": "string",
                "enum": ["idle_shake", "sway", "calm", "wave", "tired",
                         "slight_sway", "breathing", "hand_moves", "nod", "relaxed"],
                "description": "The emotion animation to display"
            }
        },
        "required": ["emotion"]
    }
}
```

#### LLM 可见性

`cap_emotion` 的 `llm_visible_by_default = true`，系统启动后自动对 LLM 可见，无需通过 skill 激活。LLM 在每次推理的上下文中看到 `set_emotion` 工具，可根据对话内容自主决定何时切换情绪。

#### 系统提示词注入

通过 `claw_core_add_context_provider` 注入情绪上下文信息：

```
"Available character emotions: idle_shake(待机), sway(摇摆), calm(平静),
 wave(挥手), tired(疲倦), slight_sway(轻摇), breathing(呼吸),
 hand_moves(手势), nod(点头), relaxed(放松).
 Current emotion: calm. You may call set_emotion to change the character's
 animation to match the conversational tone."
```

---

## 四、display_arbiter 交互

情绪视频系统作为 VIDEO 所有者运行，与 Lua 脚本绘图共享 LCD 面板。核心机制参见 [屏幕显示内容确定机制-代码架构文档](屏幕显示内容确定机制-代码架构文档.md)，本节聚焦情绪切换系统中的交互细节。

### 4.1 所有权模型

```
                          ┌──────────┐
           默认启动         │  VIDEO   │◀──── 情绪视频播放占用
        ────────────────▶ │ (优先级低) │
                          └────┬─────┘
                               │
                      Lua 获取  │ (lua_depth: 0→1)
                      绝对优先  │
                               ▼
                          ┌──────────┐
                          │   LUA    │  ← Lua 脚本绘图
                          │ (优先级高) │
                          └────┬─────┘
                               │
                      Lua 释放  │ (lua_depth: 1→0)
                               │
                               ▼
                          ┌──────────┐
                          │   NONE   │──▶ video_task 500ms 轮询重获取
                          └──────────┘        恢复情绪视频播放
```

### 4.2 情绪切换期间的仲裁

```
emotion_video_set(EMOTION_WAVE)
  │
  ├── 当前正在播放 EMOTION_CALM，video_task 持有 VIDEO 所有权
  │
  ├── 发送 VIDEO_CMD_SWITCH_FILE("m04_wave.mp4", loop=true) 到命令队列
  │
  ├── video_task 接收到命令:
  │     ├── stop_current_playback() — 停止 MJPEG 解码
  │     ├── 更新 active_path = "m04_wave.mp4"
  │     ├── continue → 回到播放循环顶部
  │     └── 仍然持有 VIDEO 所有权（未释放）
  │
  └── play_single_file("m04_wave.mp4") — 开始新视频播放
```

**关键保证**：情绪切换不涉及 display_arbiter 所有权变更——整个过程在 video_task 内部完成，保持 VIDEO 所有权不释放。这避免了"短暂的黑屏"问题。

### 4.3 Lua 抢占与恢复

当 Lua 脚本执行 `display.init()` 获取 LUA 所有权时：

```
video_task 播放循环 (每 50ms 检查):
  │
  ├── display_arbiter_is_owner(VIDEO) → false (Lua 已获取)
  ├── stop_current_playback() — 停止 MJPEG 解码线程
  │
  ├── 进入重获取循环 (每 500ms):
  │     ├── 检查命令队列（可能收到新的 SWITCH_FILE 命令）
  │     ├── display_arbiter_acquire(VIDEO) — 直到 Lua 释放
  │     └── 成功后 break 回到播放循环
  │
  └── 重新 play_single_file(active_path) — 恢复之前情绪的播放
```

**设计意图**：Lua 脚本执行期间视频解码完全停止（节省 CPU 和内存带宽），恢复时从之前被中断的情绪视频重新开始循环播放。

---

## 五、线程安全模型

### 5.1 并发访问场景

```
                    ┌─────────────────────┐
   HTTP handler ───▶│                     │
                    │  s_video_cmd_queue  │───▶ video_task (唯一消费者)
   Lua binding ────▶│   (FreeRTOS Queue)  │     优先级 6, 8KB 栈
                    │                     │
   cap_emotion ────▶│   深度: 4           │
                    └─────────────────────┘
```

### 5.2 保护机制

| 资源 | 保护方式 | 说明 |
|------|----------|------|
| 情绪切换请求 | FreeRTOS Queue（`s_video_cmd_queue`） | 多生产者→单消费者，无需额外互斥锁 |
| `emotion_video_controller` 状态 | Queue 序列化 | 所有状态变更通过 Queue → video_task，天然互斥 |
| `display_arbiter.owner` | FreeRTOS Mutex（1s 超时） | 由 display_arbiter 模块内部保护 |
| 帧缓冲区 | 无竞态 | extract_task（优先级 5）写入, video_task（优先级 6）控制生命周期 |
| 视频文件切换 | video_task 串行 | stop → set → start 在同一任务中执行 |

### 5.3 涉及的任务

| 任务 | 优先级 | 角色 |
|------|--------|------|
| `video_task` | 6 | 命令消费、播放状态机、情绪切换执行 |
| extract_task | 5 | MJPEG 解封装 + JPEG 硬件解码 |
| `vid_touch` | 4 | 触摸轮询（播放控制按钮） |
| HTTP server tasks | 5 | 处理 `/api/emotion` 请求 |
| Lua runtime task | 动态 | 执行 `set_emotion()` Lua 调用 |

---

## 六、启动与初始化流程

```
app_main()
  │
  ├── esp_board_manager_init()        ← LCD 硬件初始化
  ├── video_player_init()             ← video_task 创建，等待命令
  ├── init_fatfs()                    ← SD 卡文件系统挂载
  ├── wifi_manager_init()             ← Wi-Fi 初始化
  ├── http_server_init/start()        ← HTTP server, 注册 /api/emotion 路由
  ├── app_claw_start()                ← LLM agent 启动，注册 cap_emotion
  │
  ├── emotion_video_controller_init("/sdcard/videos")
  │     └── 扫描视频目录，验证映射表文件存在
  │
  ├── emotion_video_set(EMOTION_IDLE_SHAKE)
  │     └── 发送 VIDEO_CMD_SWITCH_FILE("m01_idle_shake.mp4", loop=true)
  │           └── video_task: acquire(VIDEO) → play_single_file()
  │
  └── 最终屏幕：循环播放待机摇晃动画（默认情绪）
```

**关键时序**：
- `video_player_init()` 在 FATFS 挂载**之前**调用——它只初始化硬件（LCD panel、VSync、触摸），不访问文件系统
- `emotion_video_controller_init()` 在 FATFS 挂载**之后**调用——需要扫描 SD 卡目录验证视频文件
- 初始情绪设为 `EMOTION_IDLE_SHAKE`，提供默认的"呼吸感"动画

---

## 七、数据流总览

以"LLM 根据对话决定切换到开心挥手"为例的完整数据流：

```
┌─────────────────────────────────────────────────────────────────┐
│ 用户: "你好呀，今天天气真好！"                                     │
│                                                                 │
│ LLM 推理: 用户情绪积极愉快 → 决定切换到挥手动画                     │
│ LLM 输出: function_call("set_emotion", {emotion: "wave"})        │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
             claw_core → cap_emotion.execute()
                           │
                           ▼
                  emotion_video_set(EMOTION_WAVE)
                           │
                           ├── 查找映射表: EMOTION_WAVE → "/sdcard/videos/m04_wave.mp4"
                           │
                           ▼
                  video_player_switch_to("/sdcard/videos/m04_wave.mp4")
                           │
                           ▼
              xQueueSend(s_video_cmd_queue, {SWITCH_FILE, path, loop=true})
                           │
                           ▼
                       video_task:
                        ├─ stop_current_playback()
                        ├─ active_path = "m04_wave.mp4"
                        ├─ [可选] ppa_blend 过渡动画
                        ├─ play_single_file("m04_wave.mp4")
                        │    ├─ app_stream_adapter_set_file()
                        │    └─ app_stream_adapter_start()
                        │         └─ extract_task: MJPEG 解码 → LCD 显示
                        │
                        └─ 循环播放 m04_wave.mp4
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│ 屏幕：角色从平静变为挥手动画                                        │
│ LLM 回复: "今天确实是个好天气呢！☀️"                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 八、文件变更汇总

| 文件 | 操作 | 模块层 | 说明 |
|------|------|--------|------|
| `components/video_player/emotion_video_controller.h` | **新建** | 控制层 | 情绪控制器公共 API |
| `components/video_player/emotion_video_controller.c` | **新建** | 控制层 | 情绪状态管理 + 映射表 + 切换逻辑 |
| `components/video_player/video_player.h` | 修改 | 执行层 | 已实现：`play_file` / `switch_to` / `get_current` / `play_playlist` |
| `components/video_player/video_player.c` | 修改 | 执行层 | 已实现：命令队列 + video_task 状态机 + 双模式播放 |
| `components/http_server/http_server_emotion_api.c` | **新建** | 触发层 | POST/GET `/api/emotion` 端点 |
| `components/http_server/http_server.c` | 修改 | 触发层 | 注册 emotion API 路由 |
| `components/claw_capabilities/cap_emotion/` | **新建** | 触发层 | LLM 情绪能力组 (`set_emotion` 工具) |
| `components/common/app_claw/app_claw.c` | 修改 | 集成层 | 注册 cap_emotion 能力组 |
| `main/main.c` | 修改 | 集成层 | 启动流程中调用 `emotion_video_controller_init()` + `emotion_video_set(DEFAULT)` |
| `components/video_player/CMakeLists.txt` | 修改 | 构建 | 添加 `emotion_video_controller.c` 源文件 |
| `components/video_player/Kconfig` | 修改 | 构建 | 添加 `EMOTION_VIDEO_ENABLE` 配置项 |

---

## 九、错误处理与降级策略

| 场景 | 处理方式 |
|------|----------|
| SD 卡无视频文件 | `emotion_video_controller_init()` 返回 `ESP_ERR_NOT_FOUND`，系统正常运行但无情绪动画 |
| 切换到不存在的情绪名/索引 | `emotion_video_set()` 返回 `ESP_ERR_INVALID_ARG`，保持当前情绪不变 |
| 指定视频文件损坏/无法解码 | `video_task` 记录失败计数，连续失败 3 次后放弃，等待下一个命令 |
| Lua 脚本占用屏幕期间收到情绪切换 | 命令照常入队列，video_task 在重新获取 VIDEO 所有权后执行切换 |
| display_arbiter acquire 超时 | video_task 以 500ms 间隔持续重试，不丢弃命令 |
| 快速连续切换（命令风暴） | 队列深度 4 提供缓冲，超出时 `xQueueSend` 返回 `errQUEUE_FULL`，调用方可选择阻塞等待或丢弃 |
| PPA 硬件初始化失败 | `ppa_blend_init()` 返回错误，过渡动画降级为直接切换（无淡入淡出） |

---

## 十、关键设计决策

### 10.1 为什么情绪切换不需要改动 display_arbiter 所有权？

情绪切换是**同所有者（VIDEO）内的操作**，不需要重新 acquire。video_task 收到 SWITCH_FILE 命令后，先停止旧文件再启动新文件，整个过程保持 VIDEO 所有权不释放。这比"释放→切换→重新获取"的方式更快，也避免了切换瞬间被其他所有者抢占的竞态窗口。

### 10.2 为什么预留播放列表模式？

`video_player_play_playlist()` 虽然当前不被情绪系统使用，但保留它支持以下场景：
- 空闲时自动循环多种情绪动画（如 idle_shake → breathing → slight_sway）
- 特殊场合的情绪动画序列（如开机动画：nod → wave → calm）
- 用户自定义 playlist 播放

### 10.3 为什么 PPA 过渡是可选的？

PPA 过渡需要额外 768KB PSRAM（一个帧缓冲），在内存紧张时可禁用。且 PPA 引擎在 ESP32-S3 上不可用，需通过 Kconfig 条件编译。直接切换虽然没有过渡效果，但功能完整。

### 10.4 为什么采用命令队列而非互斥锁？

对比两种方案：

| 方案 | 并发控制 | 复杂度 | 潜在问题 |
|------|----------|--------|----------|
| 互斥锁 + 共享状态 | 多线程竞争锁 | 高（死锁风险，需要定义锁顺序） | 难以验证正确性 |
| **命令队列** | 生产者→消费者 | 低（FreeRTOS Queue 自带并发安全） | 队列满时需处理 |

在嵌入式系统中，FreeRTOS Queue 是经过充分验证的 IPC 原语，比手写互斥锁更可靠。4 槽位队列足以应对情绪切换的典型使用场景（罕见连续 4 次切换无人消费）。

---

## 十一、关键文件索引

| 组件 | 文件路径 |
|------|---------|
| **video_player 实现** | [video_player.c](components/video_player/video_player.c) |
| **video_player 头文件** | [video_player.h](components/video_player/include/video_player.h) |
| **video_player 架构文档** | [ARCHITECTURE.md](components/video_player/ARCHITECTURE.md) |
| **stream adapter** | [app_stream_adapter.c](components/video_player/app_stream_adapter.c) / [.h](components/video_player/app_stream_adapter.h) |
| **media extractor** | [app_extractor.c](components/video_player/app_extractor.c) / [.h](components/video_player/app_extractor.h) |
| **PPA blend** | [ppa_blend.c](components/video_player/ppa_blend.c) / [.h](components/video_player/ppa_blend.h) |
| **display_arbiter** | [display_arbiter.h](../../../../components/common/display_arbiter/include/display_arbiter.h) / [.c](../../../../components/common/display_arbiter/display_arbiter.c) |
| **app_claw 启动** | [app_claw.c](../../../../components/common/app_claw/app_claw.c) |
| **main.c 入口** | [main.c](main/main.c) |
| **HTTP server** | [http_server.h](components/http_server/include/http_server.h) |
| **总体架构文档** | [ARCHITECTURE_CN.md](ARCHITECTURE_CN.md) |
| **显示仲裁架构** | [屏幕显示内容确定机制-代码架构文档](屏幕显示内容确定机制-代码架构文档.md) |
