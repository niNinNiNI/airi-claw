# 项目存储利用评估报告

## 一、开发板存储资源概览（来源：`存储信息.md`）

| 存储类型 | 容量 | 说明 |
|---------|------|------|
| HP ROM | 128KB | 只读存储器 |
| LP ROM | 16KB | 低功耗只读存储器 |
| HP L2MEM (SRAM) | **768KB** | 高速缓存/SRAM |
| LPSRAM | 32KB | 低功耗SRAM |
| TCM | 8KB | 紧耦合内存 |
| PSRAM | **32MB** | 封装内叠封 |
| Nor Flash | **32MB** | 封装外集成 |

**实际可用可编程存储：32MB PSRAM + 32MB Nor Flash**

---

## 二、当前分区表配置（`partitions_8MB.csv`）

| 分区 | 类型/子类型 | 偏移量 | 大小 |
|------|-----------|--------|------|
| nvs | data/nvs | 0x9000 | 0x6000 (24KB) |
| otadata | data/ota | 0xF000 | 0x2000 (8KB) |
| phy_init | data/phy | 0x11000 | 0x1000 (4KB) |
| ota_0 | app/ota_0 | — | 3MB |
| emote | data/spiffs | — | 3MB |
| storage | data/fat | — | 1500KB |

**合计：~7.5MB / 32MB Flash（仅占 23%）**

---

## 三、各部分实际使用量

| 组件 | 使用量 | 分配量 | 利用率 |
|------|-------|--------|--------|
| **edge_agent.bin** (应用固件) | 3.0MB | 3MB (ota_0) | **≈100%** |
| **emote_assets.bin** (表情/UI资源) | 2.4MB | 3MB (emote) | 80% |
| **storage.bin** (FAT文件系统) | ~340KB | 1500KB | **23%** |
| **小计 (已分区)** | **~5.8MB** | **~7.5MB** | **77%** |
| **未分区浪费** | **0** | **~24.5MB** | **0%** |

### FAT存储分区详情（`fatfs_image/`）

```
fatfs_image/          总计 340KB
├── memory/           24KB    (身份、记忆索引等)
├── router_rules/     8KB     (路由规则)
├── scheduler/        8KB     (调度配置)
├── scripts/          192KB   (36个Lua脚本)
├── skills/           208KB   (技能模块)
└── static/           8KB     (静态资源)
```

---

## 四、关键发现的问题

### ❌ 问题1：Flash 容量严重浪费（最高优先级）

```
硬件容量:     32MB Nor Flash
已分区使用:   ~7.5MB (23%)
未使用:       ~24.5MB (76%)
```

- 项目使用了 `partitions_8MB.csv`（仅 8MB 分区表）
- 目录中已存在 `partitions_16MB.csv` 但未启用
- `sdkconfig` 中配置了 `CONFIG_ESPTOOLPY_FLASHSIZE="32MB"`，但实际分区表限制了使用

**建议：**

1. **切换到 `partitions_16MB.csv`** 即时释放额外 8MB 空间
2. 或 **创建自定义更大分区表** 充分利用 32MB Flash
3. 为未来功能扩展（更多技能、模型文件、日志等）预留空间

### ❌ 问题2：OTA 分区无冗余，无法安全升级

```
当前:  ota_0 (3MB)  — 仅一个OTA分区
建议:  ota_0 + ota_1 — A/B OTA 安全升级
```

- 只有单个 ota_0，**没有 ota_1 备用分区**
- 3.0MB 的 binary 已占满 3MB 分区（**利用率 ≈100%**）
- 升级失败将导致设备变砖，无法回滚

**建议：** 利用更大分区表，增加 ota_1 实现 A/B 安全升级

### ❌ 问题3：storage (FAT) 分区利用率极低

```
分配: 1500KB
实际: ~340KB (23%)
浪费: ~1160KB
```

**建议：**

1. 将分区缩小至 512KB~1MB，释放 0.5~1MB 空间
2. 或充实内容（增加 Lua 脚本、技能模块、配置模板等）
3. 考虑与 emote (SPIFFS) 分区合并

### ❌ 问题4：32MB PSRAM 利用不明确

- ESP32-P4 的核心优势之一
- 项目编译了 lvgl（171MB 源码）、freetype（14MB 源码）等图形组件
- 未发现显式 PSRAM 分配策略配置

**建议：**

1. 检查 `sdkconfig` 中 `CONFIG_SPIRAM` 相关设置
2. 将帧缓冲（frame buffer）、字体缓存（font cache）等大块数据显式分配到 PSRAM
3. 确认 lvgl 和其他图形组件已启用 PSRAM 支持

### ❌ 问题5：记忆系统部分文件为空

- `memory/memory_digest.log` — 空文件 (0 bytes)
- `memory/memory_records.jsonl` — 空文件 (0 bytes)

可能原因：记忆功能未启用或运行时记录未持久化。

---

## 五、总体评分

| 维度 | 评分 | 说明 |
|------|------|------|
| **硬件识别** | ✅ 10/10 | 正确识别了 32MB Flash + 32MB PSRAM |
| **分区规划** | ❌ 3/10 | 仅用 8MB 分区表，浪费 76% Flash |
| **空间利用率** | ❌ 5/10 | 已分区空间 77% 利用率，但总容量利用率仅 18% |
| **安全设计** | ❌ 2/10 | 无 A/B OTA，升级风险高 |
| **PSRAM 利用** | ❌ 4/10 | 未明确配置 PSRAM 策略 |
| **总分** | **5/10** | **存储利用严重不足** |

---

## 六、建议优先级

| 优先级 | 建议 | 预期收益 |
|--------|------|---------|
| 🔴 **P0 - 必须** | 切换到 16MB+ 分区表 | 释放 8~24MB 空间 |
| 🔴 **P0 - 必须** | 增加 OTA A/B 分区 | 实现安全升级，防止变砖 |
| 🟡 **P1 - 重要** | 缩小 storage 分区至 512KB~1MB | 释放 0.5~1MB 空间 |
| 🟡 **P1 - 重要** | 配置 PSRAM 使用策略 | 充分利用 32MB PSRAM |
| 🟢 **P2 - 建议** | 检查记忆系统状态 | 确保功能完整 |

---

## 七、附录：命令执行记录

```bash
# FAT存储实际使用量
$ du -sh fatfs_image/
452K  fatfs_image/        # 仓库中文件大小
$ du -sh build/storage.bin
1.5M  build/storage.bin   # 编译后镜像大小（含文件系统开销）
$ find fatfs_image -type f | wc -l
68    # 共68个文件

# 应用固件大小
$ du -sh build/edge_agent.bin
3.0M  build/edge_agent.bin

# emote资源大小
$ du -sh build/emote_assets.bin
2.4M  build/emote_assets.bin

# sdkconfig 关键配置
CONFIG_ESPTOOLPY_FLASHSIZE="32MB"
CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="partitions_8MB.csv"
```

---

*报告生成时间：2026-05-13*
*分析基于 commit: 4ade1db*
