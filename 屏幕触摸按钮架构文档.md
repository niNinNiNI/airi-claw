# ESP32-P4 屏幕触摸按钮架构文档

## 概述

本文档分析当前系统在硬件 LCD 屏幕上实现的**触摸按钮**（On-Screen Touch Button）。该按钮渲染在视频画面的左下角，呈现为**麦克风（microphone）图标**，通过电容触摸屏（GT911）检测用户点击，支持**多状态视觉反馈**（空闲/监听中/成功/错误），用于实时语音/WebIM 交互控制。

---

## 一、硬件基础

### 1.1 触摸控制器

| 参数 | 值 |
|------|-----|
| 芯片型号 | GT911 |
| 接口类型 | I2C |
| I2C 地址 | 0x5D / 0x14（地址探测确定） |
| 最大触摸点 | 5 点 |
| 坐标范围 | 480 × 800（与屏幕分辨率对应） |
| 中断支持 | 支持电平触发中断 |

### 1.2 触摸驱动栈

```
┌─────────────────────────────────────┐
│        video_player (应用层)          │
│   ┌─────────────────────────────┐    │
│   │   touch_task() — 轮询按钮   │    │
│   └──────────┬──────────────────┘    │
│              │ esp_lcd_touch_*       │
│              ▼                        │
│   ┌─────────────────────────────┐    │
│   │  esp_lcd_touch (抽象层)      │    │
│   │  read_data / get_data       │    │
│   └──────────┬──────────────────┘    │
│              │                        │
│              ▼                        │
│   ┌─────────────────────────────┐    │
│   │  esp_lcd_touch_gt911        │    │
│   │  (GT911 I2C 驱动)            │    │
│   └──────────┬──────────────────┘    │
│              │ I2C 总线               │
│              ▼                        │
│   ┌─────────────────────────────┐    │
│   │  GT911 触摸芯片 (物理硬件)    │    │
│   └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

### 1.3 触摸驱动初始化流程

```
esp_board_manager_init()
  └─ 解析 board_peripherals.yaml → 初始化 I2C 总线 (SDA=GPIO7, SCL=GPIO8)
  └─ 解析 board_devices.yaml → lcd_touch 设备初始化
      └─ dev_lcd_touch_i2c_init()
          ├─ esp_board_periph_ref_handle("i2c_master")  → 获取 I2C 总线句柄
          ├─ i2c_master_probe() → 探测设备地址 (0x5D/0x14)
          ├─ esp_lcd_new_panel_io_i2c() → 创建 I2C IO 句柄
          ├─ lcd_touch_factory_entry_t() → 板级工厂函数
          │   └─ esp_lcd_touch_new_i2c_gt911() → 创建 GT911 触摸句柄
          └─ 注册设备句柄到 Board Manager 内部表
```

---

## 二、触摸按钮实现（video_player.c）

### 2.1 按钮几何定义

```c
#define VIDEO_BTN_WIDTH        80    // 按钮宽度 (像素)
#define VIDEO_BTN_HEIGHT       50    // 按钮高度 (像素)
#define VIDEO_BTN_MARGIN       20    // 按钮距边缘距离 (像素)
#define VIDEO_BTN_RADIUS       10    // 圆角半径 (像素)
#define VIDEO_BTN_BORDER        2    // 边框宽度 (像素)

// 按钮位置：左下角
s_btn_x = VIDEO_BTN_MARGIN;                                          // x=20
s_btn_y = DISPLAY_HEIGHT - VIDEO_BTN_HEIGHT - VIDEO_BTN_MARGIN;      // y=730
```

### 2.2 按钮状态变量

```c
#define VIDEO_BTN_BUF_SIZE     (VIDEO_BTN_WIDTH * VIDEO_BTN_HEIGHT * 2)  // 80×50×2 = 8000 字节

// 按钮缓冲区 (PSRAM 分配)
static uint8_t *s_btn_buffer;         // 按钮叠加层的 RGB565 缓冲区

// 按钮位置
static int s_btn_x, s_btn_y;

// 按钮按下标记 (由 touch_task 更新)
static bool s_btn_pressed;

// 按钮状态: 0=idle, 1=listening, 2=success, 3=error
static int s_btn_state;
```

### 2.3 按钮颜色定义

```c
/* RGB565 colors */
#define VIDEO_BTN_COLOR_BG          0x18E3  /* 深灰 #181818 — 空闲态背景 */
#define VIDEO_BTN_COLOR_BORDER      0xFFFF  /* 白色 #FFFFFF — 边框/图标/文字 */
#define VIDEO_BTN_COLOR_ICON        0xFFFF  /* 白色 #FFFFFF — 麦克风图标 */
#define VIDEO_BTN_COLOR_PRESSED     0x7BEF  /* 浅灰 #7BEF7B — 按下态背景 */
#define VIDEO_BTN_COLOR_LISTENING   0xC841  /* 红色 #C84141 — 监听态背景 */
#define VIDEO_BTN_COLOR_SUCCESS     0x3E6B  /* 绿色 #3E6B3E — 成功态背景 */
#define VIDEO_BTN_COLOR_ERROR       0xD965  /* 红色 #D96565 — 错误态背景 */
```

| 状态 | `s_btn_state` | 背景色 (RGB565) | 图标 | 说明 |
|------|:---:|:---:|------|------|
| 空闲/Idle | 0 | `0x18E3` (深灰) | 麦克风 | 默认态；按下时变为 `0x7BEF` (浅灰) |
| 监听/Listening | 1 | `0xC841` (红) | 麦克风 + 录制圆点 | 正在录音/语音识别中 |
| 成功/Success | 2 | `0x3E6B` (绿) | 麦克风 + 勾号 | 语音处理成功完成 |
| 错误/Error | 3 | `0xD965` (红) | 麦克风 + X 标记 | 语音处理失败 |

### 2.4 按钮视觉设计

#### 空闲态 (state=0)

```
┌─────────────────────────────────┐
│                                 │
│       800px 屏幕                 │
│                                 │
│                                 │
│     ┌────────────────────┐      │
│     │    视频内容区域      │      │
│     └────────────────────┘      │
│                                 │
│   ┌──────────────────┐          │
│   │ ╔══════════════╗  │          │  ← 按钮 (80×50)
│   │ ║    🔊 麦克风  ║  │          │     背景: 0x18E3 (深灰)
│   │ ╚══════════════╝  │          │     按下: 0x7BEF (浅灰)
│   │ ── 20px ──        │          │     图标: 白色麦克风流
│   └──────────────────┘          │
│                                  │
└─────────────────────────────────┘
```

#### 监听态 (state=1) — 红色背景 + 录制圆点

```
┌─────────────────────────────────┐
│   ┌──────────────────┐          │
│   │ ╔══════════════╗  │          │  ← 背景: 0xC841 (红)
│   │ ║   🔊 ●       ║  │          │     图标: 麦克风 + 录制圆点
│   │ ╚══════════════╝  │          │     圆点位置: 麦克风头部右方
│   └──────────────────┘          │
└─────────────────────────────────┘
```

#### 成功态 (state=2) — 绿色背景 + 勾号

```
┌─────────────────────────────────┐
│   ┌──────────────────┐          │
│   │ ╔══════════════╗  │          │  ← 背景: 0x3E6B (绿)
│   │ ║   🔊 ✓       ║  │          │     图标: 麦克风 + 勾号
│   │ ╚══════════════╝  │          │     勾号: "V" 形 5 像素
│   └──────────────────┘          │
└─────────────────────────────────┘
```

#### 错误态 (state=3) — 红色背景 + X 标记

```
┌─────────────────────────────────┐
│   ┌──────────────────┐          │
│   │ ╔══════════════╗  │          │  ← 背景: 0xD965 (红)
│   │ ║   🔊 ✗       ║  │          │     图标: 麦克风 + X 标记
│   │ ╚══════════════╝  │          │     X: 直径 ±4 像素, \ 和 / 两条线
│   └──────────────────┘          │
└─────────────────────────────────┘
```

### 2.5 按钮渲染流程

```c
static void draw_button_overlay(bool pressed, int state)
{
    // 1. 根据状态选择背景色
    uint16_t bg;
    switch (state) {
    case 1:  bg = VIDEO_BTN_COLOR_LISTENING; break;  // 监听: 红色
    case 2:  bg = VIDEO_BTN_COLOR_SUCCESS;   break;  // 成功: 绿色
    case 3:  bg = VIDEO_BTN_COLOR_ERROR;     break;  // 错误: 红色
    default: bg = pressed ? VIDEO_BTN_COLOR_PRESSED    // 空闲按下: 浅灰
                         : VIDEO_BTN_COLOR_BG;         // 空闲松开: 深灰
    }

    // 2. 清除缓冲区 (透明黑色)
    memset(s_btn_buffer, 0, VIDEO_BTN_BUF_SIZE);

    // 3. 遍历像素绘制圆角矩形 (同之前)
    for (int dy = 0; dy < h; dy++) {
        for (int dx = 0; dx < w; dx++) {
            if (!in_round_rect(dx, dy, 0, 0, w, h, r)) continue;
            bool is_border = ...; // 边框检测
            *pixel = is_border ? VIDEO_BTN_COLOR_BORDER : bg;
        }
    }

    // 4. 绘制麦克风图标 (居中)
    //    4a. 麦克风头部 — 填充圆 (半径 7, 中心 x=40, y=19)
    //    4b. 麦克风主体 — 填充矩形 (5×12, 顶部 y=26)
    //    4c. 麦克风底座 — 水平线 (14×3, 顶部 y=39)

    // 5. 状态叠加图标
    //    5a. state=1 (监听): 录制圆点 — 小圆 (半径 3), 麦克风头部右方
    //    5b. state=2 (成功): 勾号 — "V" 形 5 像素
    //    5c. state=3 (错误): X 标记 — \ 和 / 两条对角线, 直径 4
}
```

### 2.6 麦克风图标像素级绘制

```c
/* 中心定位 */
int cx = w / 2;        // cx = 40 (按钮中心 x)
int mic_top = 12;      // 麦克风头部顶部 y = 12
int head_r = 7;        // 头部圆半径 = 7
int head_cy = mic_top + head_r;  // 头部圆心 y = 19

int body_w = 5;        // 主体宽度 = 5
int body_h = 12;       // 主体高度 = 12
int body_y = head_cy + head_r;   // 主体顶部 y = 26

int base_w = 14;       // 底座宽度 = 14
int base_h = 3;        // 底座高度 = 3
int base_y = body_y + body_h + 1;  // 底座顶部 y = 39

/* 4a. 麦克风头部 — 填充圆 */
for (int dy = -head_r; dy <= head_r; dy++)
    for (int dx = -head_r; dx <= head_r; dx++)
        if (dx * dx + dy * dy <= head_r * head_r)
            pixel[cx + dx][head_cy + dy] = VIDEO_BTN_COLOR_ICON;

/* 4b. 麦克风主体 — 填充矩形 */
int body_x = cx - body_w / 2;  // body_x = 37
for (int dy = 0; dy < body_h; dy++)
    for (int dx = 0; dx < body_w; dx++)
        pixel[body_x + dx][body_y + dy] = VIDEO_BTN_COLOR_ICON;

/* 4c. 麦克风底座 — 水平线 */
int base_x = cx - base_w / 2;  // base_x = 33
for (int dy = 0; dy < base_h; dy++)
    for (int dx = 0; dx < base_w; dx++)
        pixel[base_x + dx][base_y + dy] = VIDEO_BTN_COLOR_ICON;
```

### 2.7 状态叠加图标绘制

```c
/* 状态 1 — 录制圆点 (麦克风头部右上方) */
if (state == 1) {
    int dot_r = 3;
    int dot_cx = cx + head_r + 8;   // dot_cx = 55
    int dot_cy = head_cy - 3;       // dot_cy = 16
    for (int dy = -dot_r; dy <= dot_r; dy++)
        for (int dx = -dot_r; dx <= dot_r; dx++)
            if (dx * dx + dy * dy <= dot_r * dot_r)
                pixel[dot_cx + dx][dot_cy + dy] = VIDEO_BTN_COLOR_ICON;
}

/* 状态 2 — 勾号 ✓ (麦克风头部右方) */
if (state == 2) {
    int ck_cx = cx + head_r + 8;    // 勾号起始 x = 55
    int ck_cy = head_cy;            // 勾号 y = 19
    // "V" 形状: 左上到右下 5 像素 + 右上到左下 4 像素
    for (int i = 0; i < 5; i++) {
        pixel[ck_cx - 3 + i][ck_cy - 2 + i] = WHITE;  // 左臂
        if (i < 4) pixel[ck_cx + i][ck_cy + 2 - i] = WHITE;  // 右臂
    }
}

/* 状态 3 — X 标记 ✗ (麦克风头部右方) */
if (state == 3) {
    int x_cx = cx + head_r + 8;     // X 中心 x = 55
    int x_cy = head_cy;             // X 中心 y = 19
    int x_sz = 4;                   // 半尺寸 = 4
    for (int i = -x_sz; i <= x_sz; i++) {
        pixel[x_cx + i][x_cy + i]   = WHITE;  // \ 对角线
        pixel[x_cx + i][x_cy - i]   = WHITE;  // / 对角线
    }
}
```

### 2.8 按钮命中检测

```c
static bool in_round_rect(int px, int py, int rx, int ry, int rw, int rh, int r)
{
    // 矩形中间区域 (排除圆角)
    if (py >= ry + r && py < ry + rh - r)
        return (px >= rx && px < rx + rw);
    if (px >= rx + r && px < rx + rw - r)
        return (py >= ry && py < ry + rh);
    
    // 四角圆角检测 (距离圆心 ≤ 半径)
    int cx = (px < rx + r) ? (rx + r) : (rx + rw - 1 - r);
    int cy = (py < ry + r) ? (ry + r) : (ry + rh - 1 - r);
    int dx = px - cx;
    int dy = py - cy;
    return (dx * dx + dy * dy) <= r * r;
}
```

---

## 三、触摸轮询任务

### 3.1 任务创建

```c
// 在 video_player_start() 中创建 (仅在触摸硬件可用时)
if (s_touch && s_touch_task_handle == NULL) {
    xTaskCreate(touch_task, "vid_touch",
                2 * 1024,     // 堆栈大小: 2KB
                NULL,         // 参数
                4,            // 优先级
                &s_touch_task_handle);
}
```

### 3.2 任务主循环

```c
static void touch_task(void *arg)
{
    bool was_pressed = false;

    while (s_running) {
        // 1. 从 GT911 读取触摸数据
        esp_lcd_touch_read_data(s_touch);                    // I2C 读取
        esp_lcd_touch_get_data(s_touch, &point, &count, 1); // 解析坐标

        // 2. 命中检测
        bool hit = false;
        if (count > 0) {
            uint16_t tx = DISPLAY_WIDTH - point.x;   // 水平镜像
            uint16_t ty = DISPLAY_HEIGHT - point.y;  // 垂直镜像
            hit = in_round_rect(tx, ty,
                                s_btn_x, s_btn_y,
                                VIDEO_BTN_WIDTH, VIDEO_BTN_HEIGHT,
                                VIDEO_BTN_RADIUS);
        }

        // 3. 更新按下状态 (供 render 线程读取)
        s_btn_pressed = hit;

        // 4. 首次按下瞬间触发回调 (防重复触发)
        if (hit && !was_pressed && s_button_cb) {
            s_button_cb(s_button_cb_data);
        }

        was_pressed = hit;
        vTaskDelay(pdMS_TO_TICKS(TOUCH_POLL_PERIOD_MS));  // 30ms
    }

    s_touch_task_handle = NULL;
    vTaskDelete(NULL);
}
```

### 3.3 坐标映射

触摸坐标转换：

| 触摸物理坐标 | 屏幕显示坐标 |
|-------------|-------------|
| (0, 0)      | (480, 800) — 右上角 |
| (480, 800)  | (0, 0) — 左下角 |
| (460, 780)  | (20, 20) — 按钮左上角 |

**转换公式**：
```c
uint16_t tx = DISPLAY_WIDTH  - point.x;  // 水平镜像: 480 - x
uint16_t ty = DISPLAY_HEIGHT - point.y;  // 垂直镜像: 800 - y
```

---

## 四、按钮状态控制 API

### 4.1 状态设置

```c
// 公开 API — 供外部模块 (如 WebIM/voice) 动态切换按钮状态
void video_player_set_button_state(int state)
{
    s_btn_state = state;
    // state: 0=idle, 1=listening, 2=success, 3=error
}
```

### 4.2 回调注册

```c
// 回调类型
typedef void (*video_player_button_cb_t)(void *user_data);

// 注册点击回调
esp_err_t video_player_set_button_callback(video_player_button_cb_t cb, void *user_data)
{
    s_button_cb = cb;
    s_button_cb_data = user_data;
    return ESP_OK;
}
```

### 4.3 回调触发时序

```
touch_task 轮询 (每 30ms):
            │
            ├─ 未检测到触摸 → s_btn_pressed=false, was_pressed=false
            │
            ├─ 检测到触摸 (首次) → s_btn_pressed=true
            │   ├─ was_pressed == false → 触发 s_button_cb()
            │   └─ was_pressed = true
            │
            └─ 检测到触摸 (保持) → s_btn_pressed=true
                └─ was_pressed == true → 不触发回调 (防重复)
```

---

## 五、按钮叠加层渲染

### 5.1 渲染时序

按钮叠加层在**每一帧视频画面渲染后**绘制：

```c
static esp_err_t display_decoded_frame(uint8_t *buffer, uint32_t buffer_size,
                                       uint32_t width, uint32_t height,
                                       uint32_t buffer_index, void *user_data)
{
    // 1. 检查显示所有权
    if (!display_arbiter_is_owner(DISPLAY_ARBITER_OWNER_VIDEO))
        return ESP_OK;

    // 2. 渲染视频帧到屏幕
    esp_lcd_panel_draw_bitmap(s_panel, 0, 0, width, height, buffer);

    // 3. 叠加按钮 (在视频帧之上)
    if (s_btn_buffer) {
        draw_button_overlay(s_btn_pressed, s_btn_state);  // 传入 pressed + state
        esp_lcd_panel_draw_bitmap(s_panel,
                                  s_btn_x, s_btn_y,
                                  s_btn_x + VIDEO_BTN_WIDTH,
                                  s_btn_y + VIDEO_BTN_HEIGHT,
                                  s_btn_buffer);
    }

    // 4. 等待 VSync (防撕裂)
    xSemaphoreTake(s_vsync_sem, 0);          // 清除残留信号
    xSemaphoreTake(s_vsync_sem, portMAX_DELAY);  // 等待下一帧同步
    return ESP_OK;
}
```

### 5.2 视觉叠加原理

```
帧缓冲区 (480×800 RGB565)
┌────────────────────────────────────┐
│                                    │
│         视频帧内容                   │
│                                    │
│                                    │
│                                    │
│                                    │
│                                    │
│   ┌──────────────────┐             │
│   │  按钮叠加层        │             │  ← 第二次 draw_bitmap 覆盖
│   │  (80×50)          │             │     在按钮区域覆盖渲染
│   │  (透明黑色背景)    │             │     仅按钮图案覆盖视频
│   └──────────────────┘             │
└────────────────────────────────────┘

注意：按钮区域外的像素为透明黑色 (0x0000)，
仅按钮本身的圆角矩形边框+背景+图标覆盖视频内容。
```

---

## 六、与系统其他模块的关系

### 6.1 依赖关系

```
video_player_init()
  ├─ esp_board_manager_get_device_handle("display_lcd")
  │   → 获取 LCD 面板句柄 (帧渲染)
  ├─ esp_board_manager_get_device_handle(ESP_BOARD_DEVICE_NAME_LCD_TOUCH)
  │   → 获取触摸句柄 (if available)
  ├─ display_arbiter — 显示所有权仲裁 (VIDEO 所有者)
  ├─ app_stream_adapter — 视频解码流适配器
  └─ vsync 信号量 — 帧同步

外部消费者 (状态控制):
  ├─ cap_voice / WebIM — 调用 video_player_set_button_state()
  │   在语音采集开始/结束/成功/失败时切换按钮状态
  └─ Lua 脚本 (lua_module_lcd_touch 独立模块)
      共享同一触摸硬件，但不共享触摸状态
```

### 6.2 触摸句柄获取

```c
void *touch_handle = NULL;
ret = esp_board_manager_get_device_handle(ESP_BOARD_DEVICE_NAME_LCD_TOUCH, &touch_handle);
if (ret == ESP_OK && touch_handle) {
    dev_lcd_touch_i2c_handles_t *th = (dev_lcd_touch_i2c_handles_t *)touch_handle;
    s_touch = th->touch_handle;  // → esp_lcd_touch_handle_t
}
```

### 6.3 Lua 模块中的触摸访问

```lua
-- Lua 触摸 API (lua_module_lcd_touch)
local touch = require("lcd_touch")
local x, y, pressed = touch.read()  -- 独立读取触摸坐标
```

Lua 触摸模块与 `video_player` 的触摸按钮**共享同一硬件句柄**，但属于独立的消费者——两者不共享触摸状态。系统通过 `display_arbiter` 协调显示所有权，但不协调触摸输入。

---

## 七、配置与条件编译

### 7.1 按钮启用条件

```c
// video_player_init() — 触摸硬件检测
if (ret == ESP_OK && touch_handle) {
    s_touch = th->touch_handle;
    ESP_LOGI(TAG, "Touch controller ready for on-screen button");
} else {
    s_touch = NULL;  // 无触摸硬件 → 按钮完全禁用
    ESP_LOGW(TAG, "No touch controller — on-screen button disabled");
}

// video_player_init() — 按钮缓冲区分配
s_btn_buffer = heap_caps_aligned_alloc(64, VIDEO_BTN_BUF_SIZE, MALLOC_CAP_SPIRAM);
if (!s_btn_buffer) {
    ESP_LOGW(TAG, "Failed to allocate button buffer — button disabled");
}

// video_player_start() — 触摸任务创建
if (s_touch && s_touch_task_handle == NULL) {
    xTaskCreate(touch_task, "vid_touch", 2 * 1024, NULL, 4, &s_touch_task_handle);
    if (touch_ret != pdPASS) {
        ESP_LOGW(TAG, "Failed to create touch task — button disabled");
        s_touch_task_handle = NULL;
    }
}
```

### 7.2 Kconfig 依赖

| 配置项 | 影响 |
|--------|------|
| `CONFIG_ESP_BOARD_DEV_LCD_TOUCH_SUPPORT` | 触摸设备初始化开关 |
| `CONFIG_ESP_BOARD_DEV_DISPLAY_LCD_SUPPORT` | 显示面板初始化开关 |
| `CONFIG_ESP_LCD_TOUCH_MAX_POINTS` | 最大触摸点数 (需 ≥1) |

---

## 八、数据流总结

```
外部模块 (cap_voice / WebIM)
  │
  ├─ 开始录音 ──→ video_player_set_button_state(1)  → 按钮变红 + 圆点
  ├─ 识别成功 ──→ video_player_set_button_state(2)  → 按钮变绿 + 勾号
  └─ 识别失败 ──→ video_player_set_button_state(3)  → 按钮变红 + X
                        │
                        ▼ (状态异步更新, next frame)
                  draw_button_overlay() 读取 s_btn_state

用户触摸屏幕 (左下角)
  │
  ▼
GT911 触摸芯片
  │ I2C 每 30ms 轮询
  ▼
touch_task()
  ├─ esp_lcd_touch_read_data(s_touch)
  ├─ esp_lcd_touch_get_data(s_touch, &point, &count, 1)
  ├─ 坐标转换: tx=480-x, ty=800-y
  ├─ in_round_rect() 命中检测
  │
  ├─ 命中 → s_btn_pressed=true
  │   └─ 首次按下 → s_button_cb(user_data) → 触发外部逻辑
  │
  └─ 未命中 → s_btn_pressed=false

                ▼ (下一帧)
display_decoded_frame()
  ├─ esp_lcd_panel_draw_bitmap(视频帧)
  ├─ draw_button_overlay(s_btn_pressed, s_btn_state)  ← 读取状态
  └─ esp_lcd_panel_draw_bitmap(按钮叠加层)
```

---

## 九、设计要点总结

| 特性 | 说明 |
|------|------|
| **硬件触摸** | GT911 电容触摸屏，I2C 接口，5 点触控 |
| **按钮位置** | 屏幕左下角 (x=20, y=730) |
| **按钮尺寸** | 80×50 像素，圆角矩形 (半径 10) |
| **图标** | 麦克风 (头部圆 + 主体矩形 + 底座横线) |
| **状态反馈** | 4 种视觉状态: 空闲(灰) / 监听(红+圆点) / 成功(绿+勾) / 错误(红+X) |
| **轮询机制** | 独立 FreeRTOS 任务 (vid_touch)，30ms 间隔，2KB 栈，优先级 4 |
| **触发方式** | 按下瞬间单次触发回调 (防重复) |
| **坐标转换** | 480-x, 800-y (GT911 镜像配置) |
| **渲染方式** | 视频帧之上的像素覆盖叠加，每帧重绘 |
| **缓冲区** | PSRAM 分配 8000 字节 (80×50×2) |
| **状态 API** | `video_player_set_button_state(0~3)` |
| **回调 API** | `video_player_set_button_callback(cb, user_data)` |
| **条件禁用** | 无触摸硬件 / PSRAM 不足 / 任务创建失败时自动降级 |
| **线程安全** | `s_btn_pressed` 由 touch_task 写入、render 线程读取 (单写多读) |
| **适用场景** | WebIM 语音输入控制，AI 语音交互的视觉反馈 |
